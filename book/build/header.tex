\usepackage{fontspec}
\usepackage{newunicodechar}
\newfontfamily\symbolfont{DejaVu Sans}
\newunicodechar{★}{{\symbolfont ★}}
\newunicodechar{→}{{\symbolfont →}}
\newunicodechar{✓}{{\symbolfont ✓}}
\newunicodechar{✗}{{\symbolfont ✗}}
\newunicodechar{≈}{{\symbolfont ≈}}
\usepackage{fancyhdr}
\usepackage{fvextra}
\usepackage{graphicx}
\usepackage{float}
\usepackage{microtype}
\usepackage[table]{xcolor}
\usepackage{booktabs}
\usepackage{longtable}
% Long code lines are wrapped in the text by build.py (with a continuation mark): LaTeX's own line
% breaking in code blocks (fvextra breaklines) is not compatible with tagged PDF.
\DefineVerbatimEnvironment{Highlighting}{Verbatim}{commandchars=\\\{\},fontsize=\footnotesize}
\DefineVerbatimEnvironment{verbatim}{Verbatim}{fontsize=\footnotesize,frame=leftline,framesep=8pt,rulecolor=\color{black!35}}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[LE]{\small\nouppercase{\leftmark}}
\fancyhead[RO]{\small\nouppercase{\rightmark}}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.6em}
\setlength{\emergencystretch}{3em}
\makeatletter
\def\maxwidth{\ifdim\Gin@nat@width>\linewidth\linewidth\else\Gin@nat@width\fi}
\def\maxheight{\ifdim\Gin@nat@height>0.85\textheight0.85\textheight\else\Gin@nat@height\fi}
\setkeys{Gin}{width=\maxwidth,height=\maxheight,keepaspectratio}
\makeatother
% Pandoc's framed-based shaded box does not work with tagged PDF: use a plain rule beside the code instead.
\AtBeginDocument{%
  \hypersetup{bookmarksdepth=2}%
  \renewenvironment{Shaded}{\par\medskip}{\par\medskip}%
  \RecustomVerbatimEnvironment{Highlighting}{Verbatim}{commandchars=\\\{\},fontsize=\footnotesize,frame=leftline,framesep=8pt,rulecolor=\color{black!35}}%
}
% Long inline code (set with \path by book.lua) may break after any letter, digit or punctuation mark.
\AtBeginDocument{%
  \def\UrlBreaks{\do\a\do\b\do\c\do\d\do\e\do\f\do\g\do\h\do\i\do\j\do\k\do\l\do\m\do\n\do\o\do\p\do\q\do\r\do\s\do\t\do\u\do\v\do\w\do\x\do\y\do\z%
    \do\A\do\B\do\C\do\D\do\E\do\F\do\G\do\H\do\I\do\J\do\K\do\L\do\M\do\N\do\O\do\P\do\Q\do\R\do\S\do\T\do\U\do\V\do\W\do\X\do\Y\do\Z%
    \do\0\do\1\do\2\do\3\do\4\do\5\do\6\do\7\do\8\do\9}%
  \def\UrlBigBreaks{\do\/\do\.\do\:\do\-\do\_\do\(\do\)\do\=\do\,}%
  \def\UrlOrds{\do\*\do\-\do\~}%
  \def\UrlSpecials{\do\ {\Url@space}}%
}
