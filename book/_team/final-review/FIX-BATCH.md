# Final fix batch (from the four final reviews and the three Part VII reviews)

Sources (read the ones that concern you): `01-senior-fullstack-engineer.md` (FSE-nn), `02-software-architect.md` (SA-nn), `03-technical-lead.md` (TL-nn), `04-accessibility.md` (A11Y-nn), `05-part7-senior-fullstack-engineer.md` (P7-nn, FSE), `05-part7-software-architect.md` (PA-nn), `05-part7-technical-lead.md` (P7-nn, TL). All in `book/_team/final-review/`. Also `book/_team/reviews/40-*.md`.

## Rules for everyone (phase 1: writers, research, build; phase 2: editor's mechanical passes; phase 3: rebuild and re-review)
- Edit only the files you own (listed below). If a finding belongs to someone else, message the owner (agent ids in the roster) or append it to `book/_team/requests.md`. Never run a script that rewrites many files you do not own. Use the Edit tool for prose edits (Edit refuses if the file changed since you read it: re-read and retry).
- Verify every changed technical claim against the code at the named tag (`git show <tag>:<path>`), or against the official docs; do not invent. Keep listings exact (`listingcheck.py`).
- Do not commit; the coordinator commits. On a temporary rate-limit error, wait a little and retry; do not stop the whole task.
- Update your progress file when done, listing each finding ID as Fixed / Not applicable (with reason) / Deferred (with reason).

## Rules that apply to every chapter you own (accessibility and house style)
1. **Figure text descriptions.** Every figure gets, right after its italic caption, a paragraph `*Text description:* ...` (1-3 sentences: what the diagram shows, the nodes, the direction of flow, what to notice; do not just repeat the caption; do not rely on colour). Chapters 32-37 and 39-41 already have some; check all. (A11Y-03, TL-07.)
2. **Positional references.** Replace "above"/"below"/"the following figure/table/listing" style references to figures, tables and listings with numbers ("Figure 5.2", "Listing 12.3"). (about 85 cases, TL minor.)
3. **Table captions.** House rule: bold caption ABOVE the table, `**Table N.M — Title**`. Convert the italic-below captions in your chapters (about 24 in total: FSE-06, TL-09). Remove duplicate bold stubs (Tables 27.1 and 31.2).
4. **Long sentences.** Split sentences over about 40 words where it helps a beginner (494 in the book, A11Y-17), starting with the Beginner tier.
5. **Banned tone words**: see the list in `03-technical-lead.md` (minor) and STYLE.md; remove them.
6. **Heading levels**: never skip a level (H2 to H4). (A11Y-07.)
7. Exercises keep the `### Exercise N.M ★ Title` form (the build adds "one star" wording for screen readers). Chapter and Appendix C exercise titles must match exactly.

## Owner sections

### writer-foundations (<agent>): Ch 1-10, Part I intro is the EDITOR's
- FSE-11: Ch 2 "two levels at once" is really three.
- FSE-12 and SA-14: Ch 4 says the canvas wording is "stale in the repository": qualify it (PR #13 on main, after the tag, fixed the README and the PageInfo comment; the tags still say canvas). Anywhere the book says "PRs 1 to 12" (Ch 7 and others: grep), add that PR #13 exists and what it did.
- TL-06 / FSE-04: the Setup chapter (find it: Ch 1 or the front matter setup section; if it is in front matter tell the editor) installs only JDK 25 and says "You do not install Maven": add that tags m0-m4 need JDK 21 and an installed Maven (no `mvnw` before m5) and point to Table IV.3 in Part IV.
- Part VII beginner review: Ch 8 status-code table lacks 412 (add it with one sentence: "precondition failed"; the app can return 412 in the replace flow; check Ch 8 and Ch 14 for consistency).
- Apply the common rules to Ch 1-10 (figure descriptions, positional references, table captions, long sentences, tone words).

### writer-backend (<agent>): Ch 11-18 and Ch 38
- FSE-03: Example 12.2 (curl upload) needs `cookies.txt` and a CSRF token that no chapter shows how to obtain. Add a short verified sequence (GET to receive XSRF-TOKEN cookie, login with cookie jar and header, then the upload). Verify against the code at the tag; use placeholder passwords only, never print real ones.
- FSE-09: Ch 14 says `@Transactional` "rolls back if it throws": correct it (by default only unchecked exceptions and errors roll back; `rollbackFor` for checked); verify the app's usage.
- FSE-13 belongs to production (Ch 35).
- Apply the common rules to Ch 11-18 and Ch 38.
- Ch 38: any finding in `02-software-architect.md` addressed to it (SA-10 caption of Listing 38.4).

### writer-frontend (<agent>): Ch 19-24
- TL-05: Ch 24 must let a reader run the end-to-end tests: `npx playwright install chromium` (verify), where `E2E_ADMIN_PASSWORD` comes from (env var the reader sets; say so without any real password), the commands per OS.
- FSE-07: `npm test` / `ng test` may open watch mode: verify in `frontend/package.json`/`angular.json` at book-m6-final and state the non-interactive command for CI or exercises.
- TL minor: 18 exercise titles in Ch 20, 21, 23, 24 differ from Appendix C: make chapter and solutions titles identical (the editor regenerates Appendix C from the solutions files).
- Apply the common rules to Ch 19-24.

### writer-app (<agent>): Ch 25-31 and Appendix B blueprints
- FSE-08: Exercise 25.3: tell readers to change a MIDDLE character of the token payload (the last character can decode to the same bytes).
- FSE-02: Exercise 29.4 is underspecified (`stepY` depends on the number of watermark lines): state the assumed number in the exercise.
- TL-02: remove internal file names from reader-facing text: "as listed in `book/OUTLINE.md`" and "Solutions are in `NN-....solutions.md`" (use "Appendix C").
- A11Y-04: Ch 27 has a bare `<username>` (manuscript line ~15203) that becomes an HTML tag in EPUB/HTML: put it in backticks; grep all your chapters for other bare `<word>` outside code.
- SA-05: Ch 29 (~line 697) and Ch 30 (~line 1081) write "the product owner's (re-)review" for the AI reviewer: say "the AI product-owner reviewer" consistently (the human owner is "the product owner"/"you").
- SA-07: Ch 30 "six times faster" contradicts Ch 37.8's 4.4x: verify against the dossier/measurements and fix the wrong one (tell writer-production if Ch 37 is the one to change).
- SA-08: in Part IV the Blueprint and Decisions sections come after "Try it", but the template puts them before "In this project": check STYLE.md sections 5-7 and make the chapters and the template agree (prefer changing the chapters if cheap; otherwise update STYLE and say so; tell the editor).
- SA-09: blueprints v5 and v6 drop SignedUrlService, WatermarkService, AuditLogService and SessionKeys: either add them or state in the blueprint text that v5/v6 show the deployment view.
- A11Y-07: heading levels in Part IV skip (H2 to H4): fix. TL minor: Ch 26's H3 "In this project".
- SA-14: if Ch 31 says PRs 1 to 12, mention PR #13.
- Apply the common rules to Ch 25-31 (including the blueprint figures' text descriptions).

### writer-production (<agent>): Ch 32-37, 39, Part VII (Ch 40, 41, part intro)
- SA-03: Ch 32.11 "what is not defended" is incomplete: add (a) tiles at rest are unwatermarked and unencrypted, also in backups (Ch 34.11 needs protection advice for off-machine copies), (b) username discovery through the share picker (the README lists it), (c) tokens in the URL query string can appear in logs. Verify against the README Limitations and code.
- TL: Ch 32 forward reference in Prerequisites.
- FSE-10: Ch 34 backup commands use `-p"$MYSQL_ROOT_PASSWORD"` (visible in the process list): use the `MYSQL_PWD` approach taught in Ch 10 (verify the command runs inside the container that way).
- FSE-13: Ch 35: say how to view `/actuator/prometheus` (it needs the right access; verify).
- TL-08: Ch 36 says a red CI run "blocks the pull request", but branch protection is unavailable on this private repo: verify (`gh api repos/.../branches/main/protection`) and reword truthfully (CI status is shown on the PR; enforcement needs branch protection or rulesets). SA-15: Table 36.1 says OSV proves "no published advisory": soften to what it checks.
- SA-04: Ch 39.5-39.6: the code has two package cycles (service<->document via StorageJanitor/DocumentRepository; account<->security via UserAccountService/KnownDevices), AuthController has 13 collaborators, UserAdminController revokes sessions itself, Fig 39.2 omits the security package: say so and fix the figure. SA-06: Fig 39.4 caption and 39.9 "capability URL" wording: the token scopes and expires the request, it does not by itself grant access (TileController re-checks access). SA-16: Ch 39.11 says only the switch happens under the lock; Ch 40 says commit runs inside the transaction: reconcile. SA-18: the worked lockout example ignores its own "do nothing" rule. SA-11: bulkhead claim ignores shared CPU and heap. SA-12: 37.17 inventory of in-memory state/scheduled jobs incomplete (six scheduled methods is CORRECT: one uses the fully qualified annotation; a plain grep finds five). SA-13: Ch 39 "first appears" column imprecise. SA-10/SA-16: Fig 37.1 prints the target above today in the PDF (fix the order/direction). SA-13 etc.
- P7-03 (TL) and PA-16: Ch 37.13 and 37.17 say health-check `/actuator/health`, Ch 40 says use readiness: add a pointer/correction in Ch 37; "Ch 40 says Figure 41.1 is at the end of this chapter": fix; Ch 41 network layout comes after a Ch 40 figure that assumes it: reorder or explain; "take the last address" is credited to Ch 32 which does not teach it: fix the reference.
- PART VII (Ch 40, 41, intro), from `05-part7-*.md`: PA-01/P7-01(FSE) keep-alive (nginx keepalive_timeout must exceed the ALB idle timeout; nginx.conf sets none: say what to set); PA-02 restore drill must not run StorageJanitor against the production bucket (scratch service settings); PA-03 PITR rewinds the version counter while S3 keeps newer prefixes: add the runbook step to raise the counters above the highest existing prefix; PA-04 Redis/S3 as hard dependencies, readiness excluding them, AZ-only design, RTO/RPO statement; PA-05 weigh alternatives (Spring Session JDBC + counters in MySQL vs ElastiCache; EFS vs S3; Aurora; DynamoDB) briefly and fairly; PA-06 where the "smallest useful first step" instance runs; PA-07 retention risk of versioned bucket + backups (deleted documents stay recoverable 30-35 days); PA-12 overlapping runs/leader-lock split-brain; PA-14 ALB stays directly reachable unless restricted (CloudFront prefix list/security group); P7-02(FSE) graceful shutdown: Fargate stopTimeout cap (VERIFY in the docs; see research follow-up `40-research-followup.md` if present) and what to do if the 3-minute render cannot fit (for example a durable job or making the upload async); P7-03(FSE) `s3:ListBucket` for 403 vs 404 mapping (verify; adjust Listing 41.2 and Solution 41.3); P7-04..12(FSE) Terraform expired_object_delete_marker, add `discard` to Listing 40.5 or say it is omitted, Lua sketch vs Table 40.2 clock/id, both proxy_pass lines and a concrete regex map key example, Figure 40.1 (ALB inside the VPC/public subnets; endpoints), Exercise 40.1 wording vs solution, duplicate sentence in the Ch 40 callout, CloudFront timeout wording, WAF "protection pack", Spring Session property name on Boot 4.1.1 (verify); TL P7-01: `build.py` strips HTML comments so "documentation page is named in a source comment" is untrue for readers: add a visible "Sources" list at the end of each chapter (from your source comments) and fix the sentences and Exercise 40.2; TL P7-02: rename the six teaching sketches (Listings 40.5, 40.9, 41.1-41.4) from "Listing" to "Example" (own numbering; update references; the book reserves "Listing" for tagged repo code); TL P7-08..11: define sidecar, CDN, access analyzer at first use; Table 40.1 "does not add HSTS" (verify or soften); Ch 40 length (8,058 words, paragraphs of 220-350 words): trim and split long paragraphs, and move attribute-level detail out of the Intermediate tier; the duplicate opening sentence.
- Apply the common rules to Ch 32-37, 39, 40, 41 (figure descriptions everywhere, table captions above, positional references).

### research (<agent>)
Verify from official docs/code and write `book/_team/reviews/40-research-followup.md`, then message writer-production (<agent>): (1) Fargate `stopTimeout` maximum and behaviour; (2) S3 GetObject on a missing key without `s3:ListBucket` (403 vs 404) and how the app would see it; (3) Spring Boot 4.1.1: default graceful shutdown (`server.shutdown`), `spring.lifecycle.timeout-per-shutdown-phase`, Spring Session Data Redis property names (`spring.session.data.redis.*`, `configure-action`, namespace) as of Boot 4.x, which modules; (4) ALB HSTS/response headers (does an ALB add or modify response headers?); (5) CloudFront origin response timeout default/max and request quotas; (6) AWS WAF console naming ("protection pack (web ACL)"?); (7) ALB idle timeout vs backend keep-alive guidance; (8) Terraform `aws_s3_bucket_lifecycle_configuration` with `expired_object_delete_marker` and `noncurrent_version_expiration`, `depends_on` versioning; (9) `git grep` proof that the app has six scheduled methods at book-m6-final (list them with file:line); (10) anything else marked "unverified" in `05-part7-*.md`. Quote doc page names and give suggested wording.

### editor (<agent>): PHASE 1 (now)
- TL-03: `book/build/order.txt`: add `appendices/index-terms.md` after Appendix E (back matter), make it read as a real index (term then chapters), keep how-to-use's promise true. Acknowledgments (empty heading, front matter): write it (no personal names; thank the open-source projects and tools used; state plainly that the book was drafted with AI agents under the author's direction; the author may personalise), and add an "About this edition" page text (code version: tag book-m6-final; book version; build date is added by the build). The title page needs author, edition, licence: do NOT invent them; prepare `book/build/metadata.yaml` with those keys blank and tell the coordinator, who will ask the author.
- TL-02: internal file names in front matter/appendices: how-to-use (`book/OUTLINE.md`), Appendix E ("Writers add entries through `book/_team/requests.md`"): rewrite for readers.
- TL/FSE-05: "Table 1" (milestone tags, Parts I, II, V, VI intros) and "Figure 1" (Part II) are ambiguous: rename to Table I.1, II.1 etc. (Part IV already uses IV.1-IV.3); you own all part introductions for this.
- TL-01: Appendix C heading structure: one H1 "Appendix C", then H2 per chapter, H3 per exercise; the solutions files' own H1/H2 must be demoted by the generator (do not touch the chapters' own solutions files unless writers' exercise titles changed; regenerate at phase 2). Chapters 19-24 appear twice; fix the generator.
- SA-17: epilogue overclaims ("hardened application", "made it real"): rewrite honestly (what was and was not defended, design not deployment for Part VII). Stale "closes the book" wording in OUTLINE item 6 and the end of Ch 39: coordinate with writer-production (you fix OUTLINE and the epilogue; production fixes Ch 39).
- Update OUTLINE for any structure changes the writers report.
- WAIT for phase 2 (I will message you) before regenerating the glossary, Appendix A, C and the index: writers are still editing. In phase 2 you also: improve the glossary (TL-04: 309 terms; add the missing bolded terms (unit test, integration test, UUID, SBOM, XSS, CORS, ARN, KMS, EventBridge Scheduler, AWS Backup, Managed Service for Prometheus, ...), rewrite the mechanically extracted fragment definitions into proper one-sentence definitions, un-bold repeated bolds), fix "task role" first-defined chapter, run all checks.

### coordinator (me): build and PDF
Tables clipped (SA-01, A11Y-02, FSE-01), inline code overflow, diagram size (SA-02, A11Y-11), PDF tagging/metadata (A11Y-01), figure alt text from descriptions (A11Y-03), star equivalents (A11Y-10), dead .md links (A11Y-05), EPUB accessibility metadata (A11Y-06), table semantics (A11Y-08), HTML skip link/main/focus (A11Y-09), link colours (A11Y-13), code line breaking (A11Y-12), contents depth (TL-15), title page/metadata (TL-14).
