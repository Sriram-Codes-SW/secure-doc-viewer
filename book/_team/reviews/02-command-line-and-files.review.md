# Review: 02-command-line-and-files.md (window 3)

Verified at book-m6-final: Listing 2.1 matches `.env.example` lines 5-10 exactly (password lines empty, no secrets). `.gitignore` has `.env` under "Local secrets". `application.yml` has `port: 8080` and `${DB_HOST:localhost}`. docker-compose publishes `127.0.0.1:3306` and `127.0.0.1:8081`. Solutions are correct (`src/main/resources` is three levels down). No secrets. No blockers.

1. **major** - Promise not kept: 2.1 says "Where PowerShell differs, the book shows both", but the chapter never shows a PowerShell form. The commands most likely to differ for a Windows reader are `export GREETING="hello"` / `echo $GREETING` (PowerShell: `$env:GREETING = "hello"`; `echo $env:GREETING`), `cp .env.example .env` (works as alias), `rm -r` (works as alias with prompts), `cat`, `ls`, `pwd` (aliases work). Exercise 2.2 also uses `export`. Fix: add a PowerShell variant for Example 2.2 and Exercise 2.2, or change the promise to "use Git Bash for every command in this book" (CHARTER/environment for the reader assumes Git Bash on Windows) and delete the sentence.
2. **major** - Concepts used before taught: `.gitignore`, "version control", "committed", "Git" and "history" appear in 2.5 and the Exercise 2.3 but Git is Chapter 7. The forward pointer only covers line endings. Fix: in 2.5 add one definition ("version control is a tool that records every change to a project's files; Chapter 7 teaches it") and a note that `.gitignore` is a list of files Git must not record. Also "build", "tests", "automated checks", "Docker", "compose file", "database" are used without gloss in the opening and 2.6 (Chapter 10 is referenced for compose, fine).
3. **minor** - STYLE 9: defined terms are bold, but this chapter (like Chapter 1) uses italics (*terminal*, *shell*, *path*, *process*, *port*, *environment variable*). Fix: bold at first definition and add glossary requests (requests.md has Ch 2 terms already).
4. **minor** - 2.4: says `.env.example` "lists them, with empty values where a secret belongs" - true, but the block shown also has non-secret defaults (`DB_HOST=localhost`); add "the others have safe defaults". Also say why a variable set with `export` is invisible to the app started from another window (ties to Exercise 2.2).
5. **minor** - 2.2: absolute path `/c/dev/secure-doc-viewer` is Git Bash for Windows specific and the reader's clone may be elsewhere; the text says so ("on Git Bash for Windows"), good. Add "your path will differ".
6. **minor** - Analogy (phone call to a literal assistant) has its breakdown, good. The sentence "commands such as delete run immediately and usually can't be undone" is repeated after Table 2.2; fine.
7. **minor** - Table 2.1/2.2 are referred to before they appear (good). Example 2.1 references `secure-doc-viewer` as the working folder without saying the reader must have cloned it (clone is taught in Chapter 7). Fix: "If you haven't cloned the project yet, use any folder; Chapter 7 shows how to get the project."
8. **minor** - Objective "Explain why the project keeps secrets in a `.env` file" is met. Metadata says `tag: none` yet Listing 2.1 and Section 2.6 quote `book-m6-final`; change the metadata tag to book-m6-final for consistency with STYLE 11.
9. **minor** - Exercise 2.1 presumes the reader has the repository (Chapter 7 teaches cloning). Prerequisites lists only Chapter 1. Add a setup pointer to the front-matter setup chapter or mark exercise "after Chapter 7".
10. **minor** - 2.6: "Two programs can't listen on the same port on the same address" is correct; a beginner may not know "address". Add "(the address `localhost` means this computer)" before it, since the definition of localhost comes after.
