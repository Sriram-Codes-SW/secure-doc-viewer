# Review: 08-how-the-web-works.md (window 5, full prose pass)

Verified at book-m6-final: Listings 8.1 (`GlobalExceptionHandler.error`) and 8.2 (`application.yml` session block) match the files; Table 8.1 methods match `DocumentController` (`PutMapping("/{documentId}/file")`, `DeleteMapping("/{documentId}")` returning 204 via `noContent()`); cookie name `SDV_SESSION`, `http-only`, `same-site: strict`, `${SESSION_COOKIE_SECURE:false}`, 30m timeout all real; `Cache-Control: no-store` rationale is in TileController. Table 8.2 codes 200/204/400/401/403/404/409/413/429/500 confirmed by handlers seen earlier; 410 and 503 not re-grepped here (both appear in Chapter 32/README, fine). No secrets; URLs and ids are placeholders. No blockers, no majors. Good chapter.

1. **minor** - 8.7 sample output shows `Content-Type: application/json` for `/actuator/health`. Spring Boot's Actuator normally answers with a vendor type (`application/vnd.spring-boot.actuator.v3+json`), so a reader will see something different. The text says "something like", but the mismatch invites a bug report. Fix: show the real header (verify by running the app, allowed for the author) or write `Content-Type: application/...json` with a note.
2. **minor** - Terms used before defined: "JavaScript" (parenthetical gloss present, good), "cache"/"shared cache" (Chapter 1 defines cache; `Cache-Control` explanation should say who caches: browsers and proxies), "certificate" (8.6 only glosses inside the TLS sentence), "proxy" (defined in 8.5 but used in 8.5's first sentence flow; fine), "forged-request attacks" (points to Chapter 16), "handler" (`GlobalExceptionHandler` mentioned before Chapter 13), "Map"/"ResponseEntity" (glossed).
3. **minor** - "Figure 8.1" and "Figure 8.2"-style items are plain-text blocks (the request/response); STYLE 13.1 reserves figures for diagrams and wants captions in italics below (done). Consider "Example 8.1" for text blocks and reserve Figure for the real diagram. Note "Example 8.1" is already used for the JSON object: numbering collision risk if renamed; check.
4. **minor** - The key-card analogy ("The cookie is the reader's key card") appears inside a bolded sentence; its breakdown is present ("checked once at a door vs every tile request"). Good. The wristband analogy in STYLE 8.1 for signed URLs is not yet used; fine at this chapter.
5. **minor** - 8.2: `401 Unauthorized` is documented as "Not signed in, or an invalid tile token"; the HTTP name is misleading (it means unauthenticated). One sentence saying so would prevent a common beginner confusion. Also `403` row: "You may see it but not change it" is app-specific; add "(in general: known but not permitted)".
6. **minor** - Table 8.1/8.2 captions are italic below the tables while Chapter 5 uses bold above; unify with STYLE 13.2. Tables are referred to by number before they appear (good).
7. **minor** - Exercises reference "Appendix C" solutions (editor compiles). Exercise 8.2/8.3 not fully read in this pass; check they need no real credentials (chapter warns to only test your own copy: good).
