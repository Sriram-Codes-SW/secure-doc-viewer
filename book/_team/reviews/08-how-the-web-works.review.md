# Review: 08-how-the-web-works.md (window 7, expanded text, full prose review)

Verified (dossier/Grep): the three incidents match the dossier (A3 the `Accept`-header 500; C1/C2 the CSRF cookie deleted at sign-in and the test helper that hid it; D6/TM2-7 duplicated nginx CSP headers and the `/api/*.js` regex); the claim "no CORS configuration in the backend at m6" is true (no `cors`, `CrossOrigin` or `allowedOrigins` anywhere in `src/main`); status codes in Table 8.2 all have handlers (including 415); safe/idempotent explanation is correct; the AI-agent wording for the technical-manager reviewer is present; the dossier appears only in source comments. Listing exactness is covered by the coordinator's script. No secrets (placeholder tokens and made-up ids; `1789776000` is a plain example timestamp). No blockers.

## Findings

1. **major** - Exercises and commands need a running app the book has not yet taught. Section 8.10 (`curl http://localhost:8080/...`) and Exercises 8.3 and 8.5 say "with the app running locally" / "sign in through the browser", but Chapter 6 told the reader that the app stops at startup without a database and secrets, and running the database and the full stack is Chapter 10. A reader working in order cannot do these. Fix: mark 8.10's app-dependent steps and Exercises 8.3 and 8.5 "do this after Chapter 10 (`docker compose --profile full up -d`)", and keep a `curl` example that works against any public site (for example `curl -i https://example.com`) so 8.10 has a step the reader can do now.
2. **minor** - 8.11, first incident: "The project's records do not say how the failure was first noticed" but the research team's note (requests.md) says it was found in the first Chrome UI test pass and fixed in commit 1f111ef (2026-09-18, PR #1, first present at `book-m1-accounts`). Update the "How it was found" line and say it was already fixed by milestone 1 (this also corrects the Chapter 13 timing).
3. **minor** - Terms used before defined: "public key" (8.9: "a public key belongs to a given domain"), "script injected into the page" (8.6; the attack is cross-site scripting, XSS; name it once), "allow-list", "embed/frame" (8.7), "prefetch" (8.2), "Referer" (a historic misspelling of "referrer"; add one clause), "Angular development server" (pointer only), "Docker stack" and "nginx"/"Caddy" (Chapters 10 and 33 pointers are present), "boundary marker" (8.5), "malformed".
3b. **minor** - Analogies without a breakdown: DNS as "the internet's phone book" (8.1) and the protocol as "the rules of a phone call": add one clause each ("a phone book is static; DNS answers change and are cached"). The coat-check analogy (8.6) has two breakdowns: good.
4. **minor** - Pacing: the Advanced tier (8.7 to 8.11) carries security headers, same-origin, HTTPS, traffic inspection and three incidents in about 2,000 words. It is accurate and well organized, but a beginner will find 8.7 (CSP directives) dense. Consider moving the incidents to their own Chapter 13/16 pointers and shortening 8.7's bullets to one line each.
5. **minor** - 8.10 sample output says `Content-Type: application/json` for `/actuator/health`; Actuator normally answers with a vendor media type (`application/vnd.spring-boot.actuator...+json`). The text says "something like", but write the header as `Content-Type: application/...json` or verify by running the app before publishing.
6. **minor** - 8.2 uses an example UUID (`123e4567-e89b-12d3-a456-426614174000`) which is the well-known documentation UUID: fine. Add one sentence that the app's real ids are random UUIDs so they cannot be guessed (ties to Chapter 1's "Guesser" row).
7. **minor** - Listing 8.8 (`Caddyfile`) uses tabs inside the code block and a Caddy-specific `{$HSTS_POLICY:max-age=31536000}` environment syntax; explain that the braces-dollar form reads an environment variable with a default (Chapter 2 taught the idea for `application.yml`).
8. **minor** - Exercises 8.1, 8.2, 8.4 and 8.6 are solvable from the text now; 8.6 is a good synthesis task. Format complies. Solutions file not re-read.
9. **minor** - "Figure 8.1" is a text block (an example, not a diagram); STYLE reserves "Figure" for diagrams. Rename to "Example 8.0"/"Example 8.1" style or keep with a note; numbering currently collides with "Example 8.1" for JSON, so use Listing/Example consistently.
