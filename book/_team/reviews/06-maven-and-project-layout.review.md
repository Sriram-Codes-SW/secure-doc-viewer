# Review: 06-maven-and-project-layout.md (window 7, expanded text, full prose review)

Verified (dossier/Grep/arithmetic): the upgrade rationale and the "latest GA versions checked on Maven Central" wording (decisions.md line 69, TM-16); the missing Dockerfile/CI/wrapper finding (TM-14); the Tomcat 11.0.24 advisories and the 11.0.26 pin (bugs-and-findings G9, commit 7484f4f); Maven 3.9.16 and wrapper added in commit 08f3879. Table 6.2 now states correctly that the project has no separate integration-test plugin and that `verify` adds nothing beyond `package` (the earlier major about `verify` is RESOLVED). Listing 6.3's caption honestly says the three dependencies come from different places, in file order. Listing exactness is covered by the coordinator's script. Reviewer nature (AI agent) is stated once in 6.7. No secrets (image and action digests are public pins). No blockers.

## Findings

1. **minor (downgraded from major after reading `front-matter/c-setting-up-your-machine.md`, whose Step 7 already tells the reader to clone the repository and lists the seven tags)** - Sequencing: the fix is only to point to that step. Chapter 6 says "Chapter 7 shows how to get it" and Exercise 6.5 depends on `git show`; change the pointer to "Step 7 of Setting up your machine" (and Chapter 7 for tags). Original finding: Chapter 6 asks the reader to run `./mvnw compile` and `./mvnw package` in "the project folder" (6.2, 6.8) and Exercises 6.2, 6.4 and 6.5 require a clone and `git show <tag>:pom.xml`, but cloning and tags are taught in Chapter 7, the next chapter (and Chapter 2's Exercise 2.1 has the same forward problem). The chapter says "Chapter 7 shows how to get it" but a reader working in order cannot do the practical steps. Fix: add a short "Getting the code" box to the front matter (repository address, clone command, `book-m6-final` checkout) and refer to it from Chapters 2, 4, 6 and 7; or reorder Chapters 6 and 7 and update OUTLINE. Without this the first hands-on Maven session fails for a beginner.
2. **minor** - 6.2 says the first `./mvnw compile` downloads libraries; it does not warn that the test libraries are only fetched by `test`/`package`, or that the first `package` needs Docker for one test (or skips it). Add: "If Docker is not running, one integration test is skipped (Section 6.5); the build still succeeds."
3. **minor** - 6.8: `./mvnw package` "runs every test, so it takes longer": add an expected duration ("a minute or two on a laptop") and what "Tests run: N" looks like, so a reader can tell a hang from a slow build. Also note that on first run the H2 tests need no setup.
4. **minor** - Terms used before defined: "cache folder `.m2/repository`" (fine), "Git" and ".gitignore" (Chapter 2 defined them), "repository" (Maven Central) versus Git repository (both used; add "this is a different kind of repository from a Git repository"), "job"/"steps"/"runs-on" and "OSV", "lockfile", "GHSA", "digest" (Listing 6.7; Chapter 36 walks through it: add a two-line gloss now), "launcher", "manifest" (`MANIFEST.MF` appears in the jar listing), "Docker image build" (Chapter 10 pointer).
5. **minor** - Listing 6.7 (a GitHub Actions job with a `docker run` and two long digests) is heavy for Chapter 6's beginner; it is properly placed in the Advanced tier, but the reader is told nothing about YAML syntax (Chapter 8 defines YAML). Add "the indentation shows nesting; Chapter 36 explains each line" before the listing.
6. **minor** - 6.9: "it does not claim a reason from the project's history, because none is recorded" is the writer's honesty about sources, but it speaks to the book process rather than the reader. Reword: "The project does not record why Maven was chosen over Gradle; this book only explains the difference."
7. **minor** - Tomcat pin story (6.11): "The project's fifth pull request went through several rounds of review, and one of them found the problem" leaves out that the scan came from the reviewer's/CI's dependency scan; fine, but say which (dossier: found in round 2 probe / OSV scan, commit 7484f4f) or leave unspecific. Not required.
8. **minor** - Exercise 6.5 (★★★) needs `git show` of two tags and judgment about features per dependency; add a hint listing where Flyway, JPA and the MySQL driver appear (m1 and m2), or the solution must list the actual diff (check the solutions file matches `book-m2-documents`'s dependencies: Flyway, MySQL driver, JPA, validation, security).
9. **minor** - The analogy (kitchen assistant) has two breakdowns; good. Table captions italic below; Table 6.1 (scopes) refers to `h2` and `spring-boot-starter-test` before Listing 6.4 shows them: fine, since Listing 6.4 follows immediately.
10. **minor** - Chapter length is now about 4,000 words with 13 sections; the Advanced tier holds a supply-chain section, a milestone comparison and layout. The layout (6.13) is the most useful reference for a beginner and belongs earlier (Beginner tier), right after 6.3.
