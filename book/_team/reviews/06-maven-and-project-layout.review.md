# Review: 06-maven-and-project-layout.md (window 5, full prose pass)

Verified at book-m6-final and m0: Listings 6.1 to 6.4 match `pom.xml` and `.mvn/wrapper/maven-wrapper.properties`; versions correct (Boot 4.1.1, Java 25, PDFBox 3.0.8, Tomcat 11.0.26, Maven 3.9.16, wrapper 3.3.4). 84 Java files (all under `src/`) is right. `finalName`, Dockerfile `./mvnw -B -q -DskipTests package` and `dependency:go-offline`, CI `./mvnw -B verify`, `scope` test entries, m0 having three dependencies, and the wrapper first added in commit 2d10e07 (m5) all check out. Tomcat advisory IDs come from the real pom comment. No secrets. No blockers.

1. **major** - Table 6.1 says `./mvnw verify` "also runs the integration checks" and `test` "runs the unit tests". The pom has no surefire/failsafe configuration and no `*IT` classes; the four `*IntegrationTest` classes (including `MySqlIntegrationTest`, which needs Docker) run in the `test` phase like every other test. `verify` adds nothing test-wise beyond `package`. Fix: "`test` runs all tests (the ones that use Docker are skipped when Docker is not running, per the README); `verify` is the command CI uses and also runs every earlier phase". Confirm the Docker-skipping behavior before writing it (README line 338 says "when Docker is running").
2. **minor** - Terms used before or without definition: "repository" (Maven Central) vs Git repository (Chapter 7); "cache on your computer" (say where: `~/.m2`); "integration checks" (see item 1); "batch mode" (defined); `.gitignore` (defined in Chapter 2); "unit tests" (Chapter 18); "web server"/Tomcat (first use in Listing 6.2; one clause: "the program that listens for web requests, Chapter 11").
3. **minor** - Analogy (recipe-following assistant) has its breakdown, good; but "ingredients are other people's code, which can carry security flaws" would be stronger with a real example - the Tomcat override in Listing 6.2 is exactly that; add "as Listing 6.2 shows".
4. **minor** - Listing 6.1/6.2/6.3 are excerpts of one file with different indentation kept from the original (four spaces) and no `// ...`/`<!-- ... -->` markers at the cuts; add a one-line note "these three excerpts are from different parts of one file".
5. **minor** - 6.4: "The wrapper was added when the project moved to Java 25" is correct per git; the source comment cites a commit hash in a reader-facing HTML comment only: fine. State that `mvnw` on macOS/Linux may need `chmod +x mvnw` (the Dockerfile does this for a reason).
6. **minor** - Exercises: 6.2 says "With Java 25 installed and the project cloned" but Git cloning is Chapter 7 (later). Add a pointer or move exercises 6.2 to after Chapter 7. No "Appendix C" solutions yet (editor).
7. **minor** - Tables 6.1 and 6.2 have captions in italics below the table; STYLE 13 wants numbered captions; format is acceptable, but confirm STYLE 13.2 prefers the caption above tables (Chapter 5-style bold above is used in Chapter 3). Make consistent across chapters.
