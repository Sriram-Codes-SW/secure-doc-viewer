# Review: 33-deployment-and-tls.md (window 7, expanded text, full prose review; supersedes the listing-only pass)

Verified (dossier/Grep): the earlier major is RESOLVED: Listing 33.3 now shows the full `Content-Security-Policy` value (no authored `...`) and the `proxy_read_timeout` line; the caption says "comments omitted". Facts agree with the dossier and earlier chapters: fixed subnet 172.28.0.0/24 with nginx at .10 and Caddy at .11 (D11, TM2-6, commit a51674c), the overwrite fix (2d82253), Caddy TLS (D12), HSTS `includeSubDomains` opt-in (F4, commits 66f7152 and 5aa0f3c), `mem_limit` values, `depends_on` health ordering, the `MYSQL_PWD` health check note (agrees with Chapter 10), the go-live checklist wording (README), the AI-agent wording for the technical-manager reviewer, and dossier citations only in HTML comments. Listing exactness is covered by the coordinator's script for the listings that have path lines. Exercise 33.2's answer is derivable from Listing 33.3 (the spoofed header is ignored because only Caddy's address may set the real IP, so the app sees the real connection address). No secrets (`.env` is described, never quoted). No blockers, no majors.

## Findings

1. **minor** - Header `status: expanded-draft` (see Chapter 34, item 1). Listing format: Listings 33.1 to 33.4 have no italic `*Path: ...*` line after the block (the house format and the coordinator's script), and three code blocks are unnumbered and uncaptioned (the three `docker compose` start commands in 33.5, the `MYSQL_PASSWORD` lines in 33.11, and the `cookie:` YAML in 33.12). Add captions and paths, and note when a block is an excerpt.
2. **minor** - Duplication with Chapter 10: Listings 33.1 and 33.2 (the two Dockerfiles) and their line-by-line explanations repeat Listings 10.6 and 10.7 almost word for word. Repeating is defensible for a reader who jumps straight to Part V, but two copies will drift. Suggest: keep the full listing in Chapter 10, and here show only what is new for deployment (non-root, `.dockerignore`, why the order matters) with "as in Section 10.8" for the rest.
3. **minor** - Terms used before defined: "upstream" (33.9: "no upstream proxy is trusted"), "DNS name" (Chapter 8 defines DNS), "firewall" (33.15), "public interface" (33.5: the network connection reachable from the internet), "zstd/gzip" (compression), "TCP"/"downgrade attack" (defined in 33.12), "renewal", "subdomain" (33.12), "SPA fallback" (defined), "cache-control `immutable`" (explained), "hashed bundles" (explained), "start_period" (explained in Chapter 35).
4. **minor** - 33.5 says the compose file "is identical at `book-m5-platform` and `book-m6-final`" and defines "four services"; but 33.4 says "You should see three services: mysql, app, and web" (correct for the `full` profile without `tls`). Add "four with the `tls` profile" to avoid a moment of doubt.
5. **minor** - 33.10 says "The two-address live test in Chapter 32 checked the nginx and direct-to-app links, and a separate check of the `tls` profile confirmed that Caddy ignores a spoofed header". Chapter 32's summary of the live test says three paths; make the two chapters agree, and cite the dossier's "18/18 two-address checks" precisely.
6. **minor** - 33.12 says "The CSRF cookie carries `Secure` behind Caddy as well; the live test in PR #5 confirmed it": I found no matching statement in the dossier files I searched; ask the writer for the source (PR #5 body) or remove the claim.
7. **minor** - 33.4 tells the reader to open `.env` and "fill in `SIGNING_SECRET` (at least 32 random characters)". Point to the way to generate one (`openssl rand -hex 32`, taught in Chapter 2, 2.9) so the reader does not invent a weak one.
8. **minor** - Analogy (shop front) has a breakdown; good. "Forward proxy vs. reverse proxy" is a helpful definition. Table 33.1 is numbered and referenced. Exercises 33.1 to 33.6 are in the right format; 33.6 (★★★, plan for your own domain) needs a solution that says "one good plan" and includes the DNS record and firewall steps; a solutions file exists (not re-read).
9. **minor** - Pacing: about 5,000 words; the Advanced tier (33.10 to 33.14) is well organized and the go-live checklist works as a summary. Consider adding a one-paragraph "if you only remember three things" (HTTPS, do not publish the app port, trust only the proxy's address).
