# Review: 37-engineering-tradeoffs.md (window 3)

Method: each "What the project chose", Pro, Con and Alternative was checked against `book-m6-final` README.md and `book/_team/dossier/`. Most project facts are grounded (list below). The problems are (a) one factual error inherited from a stale README line, (b) industry claims and "enterprise alternative" content that the chapter's own rule ("statements cite the README, a PR or a commit; reasoning that isn't recorded is marked as the book's reading") does not cover, and (c) reader-facing citations of internal dossier IDs. No secrets. One blocker.

Grounded and correct (spot-checked): 512-px tiles, 180 per 60 s, about 15 pages a minute, 33 minutes vs 2.4 hours (dossier decisions.md, TM final review); product-owner acceptance dated 2026-09-19 (README Limitations); red 20% opacity, brick pattern, 1.5x text-height gaps, `watermark-opacity` up to 0.6 and `watermark-spacing` down to 0.5 (README lines 52, 68); `Cache-Control: no-store` for shared caches (README line 47); `sdv_tiles_served_total` and `sdv_tiles_rate_limited_total` (README); in-memory sessions and counters, Spring Session + Redis, S3 + CloudFront presigned-URL mirroring, per-(tile, viewer) cache and coarser watermark, per-document sensitivity levels, back-to-back tile-urls alerting (README Limitations and Possible next steps); go-live checklist "One app instance"; backup runbook stops the app (dossier F1); H2 in MySQL mode plus MySQL 8.4 via Testcontainers (README line 338; TM2-8); UTC timestamp test (dossier C6); Dependabot LTS rules PR #10; OneDrive locks (dossier C4, PR #2); DB choice MySQL not H2/Postgres (dossier D2); BCrypt 72-byte and lockout DoS incidents (dossier G2, E1).

1. **blocker** - 37.1 says "The browser reassembles the tiles on a `<canvas>`." At `book-m6-final` the Angular viewer uses absolutely positioned `<div>`s with CSS `background-image` from `blob:` URLs; `git grep canvas book-m6-final -- frontend/src` finds nothing. Only the m0 static page used a canvas (Chapter 25). The chapter is copying README.md line 6, which is stale (README also says the same in the intro). Fix: "The browser paints the tiles as positioned elements (Chapter 21)", and ask the owner of the README (coordinator/editor) to correct line 6. Also 37.10 correctly says "paints tiles as CSS backgrounds", so the chapter contradicts itself.
2. **major** - Unsupported "enterprise" and industry claims presented as fact, which the chapter's own rule forbids. Examples: 37.1 "Commercial readers use this same pattern, often with a document-rights service, and add a text layer... served only when policy allows"; 37.2 "Larger systems move stamping to an image service at the edge"; 37.3 "invisible forensic marks... supplied by a specialist library or service"; 37.6 the list "Keycloak, Microsoft Entra ID, Okta"; 37.9 "Postgres is a common equal choice; the trade is mostly team familiarity, not capability"; 37.11 "Kubernetes", "cloud load balancer that terminates TLS". None of these is in the README or dossier (README mentions only S3/CloudFront, Redis, coarser watermarking, per-tile cache, sensitivity levels). Fix: either move each into a clearly marked sentence "This is general industry practice, not something the project recorded" (with an official-docs Further reading pointer), or cut. The alternatives that ARE in the README (S3 + CloudFront, Spring Session + Redis, coarser/cached watermark, sensitivity levels, back-to-back alerting) should be labeled as such.
3. **major** - 37.8 says both "sensitivity labels" and "anomaly detection" are "in the README's next steps". The README says only "per-document sensitivity levels with tighter limits" (Limitations) and "log/alert on the specific pattern of every tile-urls page fetched back-to-back" (next steps). "Anomaly detection" is the book's wording; also "sensitivity labels that select limits, watermark strength, and access rules" goes beyond the README (limits only). Fix: quote the README precisely.
4. **major** - 37.1 says the project "deletes the source PDF after ingest ... (PR #3 deletes the source before the tiles commit)". The README says the PDF "stops existing as a servable file after ingest"; I found no dossier entry about deletion order or PR #3. Fix: use the README wording, or add a dossier-backed source (ask research), or remove the PR #3 parenthetical.
5. **major** - Reader-facing text cites internal identifiers the reader cannot resolve: "dossier D1", "(PO-7, PO2-1)", "TM2-8", "PR #1" and commit hashes without explanation. STYLE 14 wants incidents told as a story with PR/commit references, but "dossier D1" is an internal file. Fix: say "the product owner chose built-in accounts when asked" and cite the PR/commit; explain TM/PO once (they were AI review agents, see Ch 32 review item 2).
6. **major** - Structure and pedagogy: no metadata comment; no `<!-- source: ... -->` comments (STYLE 17); "Prerequisites: everything before it" is not usable for a reader (list the specific chapters: 15, 16, 17, 30-36); terms used undefined: CDN, OIDC, SSO, MFA, DRM, Redis, presigned URL, load balancer, TLS terminating, point-in-time recovery, LTS. Fix: gloss on first use or point to the chapter that teaches it.
7. **minor** - 37.6 mixes sourced and inferred content in the same paragraph ("The recorded outcome is sourced; the reasoning is the book's reading: a self-contained app..."). Good practice, but the dossier tags D1 [SOURCED] for the options; check decisions.md D1 before calling the reasoning inferred, and drop the sentence if it is sourced.
8. **minor** - 37.6 "a role change ends the user's sessions (PR #1)": dossier has related lines (demotion/role) but I did not find this exact claim; verify (`git grep -n 'invalidate\|expire' book-m1-accounts -- src/main`) or cite the commit.
9. **minor** - 37.4 "rotating it ... makes accounts' recognised devices be forgotten, per the README": README line 314 says a restore under a different secret loses something; confirm the recognised-device wording matches before keeping "per the README".
10. **minor** - Try it: exercises are not in `### Exercise 37.N ★` format and there is no solutions file; exercise 4 depends on Chapter 35 metrics (fine, but name the metric: `sdv_tiles_rate_limited_total`).
11. **minor** - Further reading entries have no URLs and no titles; Spring Session and Flyway docs need links (docs.spring.io, documentation.red-gate.com/flyway or flywaydb.org).
12. **minor** - Six-heading template repeated 11 times is good for consistency, but 37.10's "When you'd switch: Never for security" and 37.3 "Nothing in this chapter is a mistake" overclaim, given that the same chapter lists real defects found by reviews. Soften to "Each choice was reasonable at the time".
