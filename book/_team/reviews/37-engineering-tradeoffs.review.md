# Review: 37-engineering-tradeoffs.md (window 8, expanded text, full read; replaces the window-3 review, whose blocker and majors are RESOLVED)

Location: `book/tradeoffs/37-engineering-tradeoffs.md` (37.1 to 37.19, Table 37.1, six exercises).

Verified against `book-m6-final`, the dossier and the PR text: session cookie `SDV_SESSION`, `same-site: strict`, 30-minute idle timeout, 12-hour maximum lifetime; watermark opacity 0.2 (from 0.28, PR #4, `60e104a`), spacing 1.5, `watermark-opacity` range 0.05 to 0.6; `@Scheduled` jobs on `AuditLogService` (two), `KnownDevices` (device purge), `LoginThrottle` and `StorageJanitor` (so 37.17 Step 4 is right); `SessionKeys` is keyed by the signing secret (Step 5 right); Tomcat 11.0.24 had three critical advisories, pinned to 11.0.26 (dossier G9); "Flyway's MySQL 8.4 untested warning is gone" is in the PR #5 body; the three lockout counters and the 30-day recognised-device rule; 18 of 18 and the 33-minute versus 2.4-hour figures; Exercise 33.3 exists and is about the address drift, as 37.13 says. The chapter now cleanly separates recorded fact from "general industry practice" throughout (the rule set in the earlier review is applied consistently), cites the dossier only in HTML comments, and does not present the AI reviewers as people. No secrets. No blockers.

## Findings

1. **major** - Wrong version fact in 37.16: "bringing Spring Security 7, Jackson 3, Hibernate 7, and Flyway 11". The PR #5 description says Flyway 11 (and the chapter quotes it), but the Boot 4.1.1 BOM manages Flyway 12.4.0 and the project does not override it (verified in the BOM and the local Maven repository). Rewrite: "bringing Spring Security 7, Jackson 3, Hibernate 7 and Flyway 12 (the pull request description says Flyway 12; the dependency actually resolved to 12.4.0)". This is the same error as in Chapter 30, Table 30.1; see `30-m5-platform.review.md`, item 1.
2. **minor** - Header still says `status: expanded-draft`; change to `expanded`.
3. **minor** - 37.11, "The enterprise alternative": the first sentence ends "a managed database, The README names shared sessions ..." (a comma and a capital where a full stop belongs; a clause is missing after "managed database"). Fix.
4. **minor** - Formatting: the "Try it" section runs the exercises together (no blank line before `### Exercise 37.2` and `### Exercise 37.3`), and none of the six exercises has a `*Solution:*` line, unlike other chapters (check the solutions file exists: `37-engineering-tradeoffs.solutions.md`).
5. **minor** - 37.6 and 37.12 both say (correctly) that the project has no recorded comparison against browser-held tokens; 37.12 says it again in the enterprise alternative. Once is enough in each section, and Chapter 32 (32.10) also repeats it. Fine for accuracy; trim two of the three.
6. **minor** - 37.1 says tiles are "512-pixel", which is right at m5/m6; Chapter 25 uses 256-pixel tiles at m0 (and 26 to 29 as well). Add "(256 at the first milestone)" so the reader does not think the chapters disagree.
7. **minor** - 37.4 Cons: "restoring a backup under a different secret still works, but every account's recognised devices are forgotten": correct per the README, but a beginner needs one clause on why (the device hashes are keyed by the secret); 37.14 gives it, so add "(37.14)".
8. **minor** - Terms used before defined: "DRM" (defined in line), "CDN" (glossed in the header), "Redis" (glossed), "point-in-time recovery" (37.9: one clause: "restoring the database to any chosen moment"), "leader lock" (37.17 Step 4: one clause), "stateless" (37.11), "HSTS" and `includeSubDomains` (37.13, Chapter 33).
9. **minor** - Pacing and use: the chapter has no code and no analogy, which suits a decision chapter, but sixteen sections in identical six-heading form make a long read. The ordering of decisions is by topic, not by which limit is hit first, although a learning objective promises "which limits you will hit first, in what order". Table 37.1 has "First limit you'll hit" per row but no ranking; add a two-sentence answer after the table (for example: memory state and disk first, because both block a second instance; tile CPU second).
10. **minor** - Exercise 37.4 and 37.6 are open-ended (★★★); the solutions file should show one worked answer each. Exercise 37.2 is solvable from 37.7 plus 37.17 Step 3. Exercise 37.3 ("order the decisions") has its answer in 37.17 (Steps 1 to 3 before 6), so the section spoils the exercise; say "attempt Exercise 37.3 before reading 37.17".
