# Review: 16-spring-security-defenses.md (window 9, expanded text, full read; replaces the window-5 pass)

Method: full read; `listingcheck.py 16` (all listings verified); code claims checked at `book-m6-final`: `csrfTokenRepository` (`withHttpOnlyFalse`, `sameSite("Strict")`), `SpaCsrfTokenRequestHandler` (plain versus xor by header presence), the `authorizeHttpRequests` rules and headers, `SessionLifetimeFilter` (12-hour default), `PasswordChangeRequiredFilter`, `LoginThrottle` limits (5, 20, 20 over 15 minutes) and `checkAllowed` order (account+ip, ip, then account-wide only for unrecognised devices), `reserve`/`succeeded`, `forward-headers-strategy` and `internal-proxies` in `application.yml`; every test named in the text exists in `SecurityIntegrationTest` (`stateChangingRequestsNeedACsrfToken`, `aSessionEndsAFixedTimeAfterSignInHoweverActive`, `anAdminCanSignAUserOutEverywhere`, `anAdminSetPasswordMustBeChangedBeforeAnythingElseWorks`, `aBurstOfParallelWrongPasswordsGetsNoMoreThanTheLimit`, `readersCannotReachAdminOrUpload`, the `from("198.51.100.61")` helper and its quoted comment), plus `CsrfCookieFlowTest` and `SecurityHeadersTest`; Chapter 18's Sections 18.8 and 18.10 and Listing 15.7 exist. Figures 16.1 and 16.2 are true of the code (16.2 is what Listing 39.1 registers). History (`1ce2c8b`, `82c24b6`, `2d82253`, `a51674c`, TM2-1, TM3-1, D7, G1) matches the dossier. The BREACH gloss (earlier major) is present; analogies (mail tray) have their breakdown. The AI-agent wording is right. No secrets. No blockers, no majors.

## Findings

1. **minor** - Both figures have source comments that name `book-m6-final`; good. The chapter is otherwise the best-verified in Part II.
2. **minor** - 16.6 says the sweep "every five minutes drops counters whose failures have aged out (Chapter 14)": right for `LoginThrottle.sweep`, which is in Table 14.2; but the same milestone has `TileRateLimiter.sweep`, absent from Table 14.2 (see the Chapter 14 review, item 1).
3. **minor** - 16.7: the incident says "The description of the pull request ... was corrected in place" and "later the app trusts the header only from the proxy's fixed address". The compose fixed address (`172.28.0.10`) is never quoted in this chapter, so the reader cannot check "fixed address"; add it in the listing's comment or a sentence ("`172.28.0.10`, set in `docker-compose.yml`; Chapter 33").
4. **minor** - 16.2: "A foreign page can make the browser send cookies, but it can't read this cookie" is right; add the phrase "same-origin policy" (Chapter 8's term) so the reader connects the rule to the earlier chapter.
5. **minor** - 16.3, 404 versus 403: the text says the `403` is "reserved for the case where you can see the document but may not change it, for example a reader trying to share a document that is visible to them". At `book-m6-final`, `ForbiddenException` is also thrown for a demoted publisher who can still read. Add "(or a formerly permitted user)" or leave it: the current text is a correct example, not a complete rule.
6. **minor** - Exercise 16.1's second question ("`GET /somewhere-else` from anyone") has different answers for a signed-in user (403 from `denyAll`) and an anonymous one (401 from the entry point); the solution should say both (I did not re-read the solutions file this window).
7. **minor** - Terms used before defined: "double-submit cookie" (bold, defined), "clickjacking" (defined), "password spraying" (defined in Table 16.2), "IPv6 /64 prefix" (16.6: gloss "the first half of the address, which identifies the network, not the device"), "SPA" (defined), "rolling window" (defined), "race condition" (defined).
8. **minor** - Pacing: about 5,500 words in three tiers; 16.6 (throttling) is the longest section (about 1,100 words) but every paragraph has a job. The Advanced tier note ("Part IV tells the milestones") is helpful.
