# Review: 16-spring-security-defenses.md (window 5, full prose pass)

Verified at book-m6-final: `withHttpOnlyFalse()` CSRF cookie, `SpaCsrfTokenRequestHandler`, `SessionLifetimeFilter` with `Duration.ofHours(12)` default, `PasswordChangeRequiredFilter`, the 403 message "Missing or invalid CSRF token. Reload the page and try again." (in `SecurityErrorResponses`), tests `stateChangingRequestsNeedACsrfToken` and `anAdminSetPasswordMustBeChangedBeforeAnythingElseWorks`, `SecurityHeadersTest`. The chapter correctly says `X-Content-Type-Options: nosniff` is a Spring Security default (no explicit config in the source) and that CSP/Referrer-Policy are set explicitly. Listings match their files (script check). The three LoginThrottle rules (5 per account+IP, 20 per IP, 20 account-wide, 15 minutes) match the dossier. The "CSRF cookie that vanished" incident matches dossier C1/C2 and Chapter 26. No secrets. No blockers, no majors.

1. **minor** - Terms used before defined: "BREACH-protected" (16.1: BREACH is an attack on compressed HTTPS responses; gloss or drop the phrase for a beginner), "clickjacking" (parenthetical only, no definition), "password spraying" (glossed), "synchronized" (a Java keyword never taught; add "only one request at a time may run this block"), "reverse proxy" (Chapter 10/30 territory; define at 16.5), "rolling window", "keyed hash", "double-submit", "SPA" (16.1 says "Single-Page-App handler", should define once: a web app that loads one page and updates it with JavaScript), "opaque handle".
2. **minor** - 16.3 title lists `X-Frame-Options` but the text says CSP `frame-ancestors 'none'` does the framing job; `X-Frame-Options` is added by Spring's defaults (Chapter 28 says the same). Say so explicitly in one sentence, or drop it from the title.
3. **minor** - The unquoted revoked-session message ("Your session has ended. Please sign in agai...") was not found by my grep at m6 (possibly wording or line-wrapping in `SecurityErrorResponses`); verify the exact text before printing it.
4. **minor** - Several code excerpts (16.1 cookie repository, 16.2 rules, 16.3 headers, 16.5 YAML, 16.6/16.7 filters) are unnumbered and captioned only by a parenthetical; STYLE 11.2 wants numbered Listings. Listing 16.2 (`LoginThrottle.reserve`) is properly captioned.
5. **minor** - Chapter says the throttling, lifetime and forced-password code "arrived in later milestones" but does not name which (m1: throttle basics; m5: atomic counting; forced change/known devices: m5-m6 per dossier). Add a sentence with milestones so Part IV chapters (26, 30) and this chapter agree.
6. **minor** - Exercises: inline `(★)` format; exercise 5 is excellent but its answer must not describe a working attack recipe beyond the concept (it is fine: header forging to dodge lockout, which the book already discusses). Solutions file not checked. Analogy in 16.1 (the "where breaks down" paragraph is attached to the CSRF secret, not an analogy): the chapter has no real analogy; add one for CSRF (e.g., a forged letter using a stolen stamp) with a breakdown, or drop the label "Where the analogy breaks down" since no analogy is stated.
7. **minor** - Prerequisites list Chapters 8 and 15 only; the chapter also needs Chapter 10-level knowledge of "proxy" (or Chapter 8 8.5) and Chapter 14 (`REQUIRES_NEW` audit of denials); add pointers.
