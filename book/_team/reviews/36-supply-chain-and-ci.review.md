# Review: 36-supply-chain-and-ci.md (window 7, expanded text, full prose review; supersedes the window-4 light pass)

Verified (Grep/dossier): the earlier major is RESOLVED: Exercise 36.6 is now explicitly a hypothetical and the solution refers only to Angular 22's real peer range. CI facts match `ci.yml` (four jobs; `permissions: contents: read`; `concurrency` with `cancel-in-progress`; `needs: [backend, frontend]`; a 60-iteration health loop with `sleep 5`; `retention-days: 7`; `openssl rand -hex` throwaway secrets; OSV and Trivy steps), and Dependabot facts match the file (`maven` and `angular` groups; Docker ignore rules for node 25/27/29 and temurin 26-28). The Tomcat story (G9, commit f682716), PRs #6 to #12, the LTS rules (D13, commit 8cdb129), the flaky test (PR #9, ec6c1c5) and "jsdom 30 raises the minimum Node version" (versions.md, from the PR #8 release notes) are all in the dossier. The pinning explanation is labeled as general practice, "not a project incident" (good honesty). Listing 36.3/36.4 disclose shortened digests and reformatting. No secrets (public digests and SHAs; secrets are generated at run time and masked). No blockers, no majors.

## Findings

1. **minor** - Header `status: expanded-draft` (see Chapter 34, item 1). Listing format: captions give the file in the caption but no `*Path:*` line after the block (house format and the coordinator's script). Three code blocks are unnumbered and uncaptioned: the `permissions`/`concurrency` block (36.4), the `rand()` script (36.7), and the pinned `uses:` line (36.8). Number them as Listings 36.2 to 36.4 (renumbering the rest), or as Examples if they are simplified.
2. **minor** - Terms used before defined: "runner" (Table 36.1: "because the runner has Docker"; define "the machine GitHub lends the job"), "the workflow's token" (36.4: define "a temporary credential GitHub gives the job"), "hexadecimal" (36.7), "registry" (`ghcr.io`), "docker.sock" (explained in prose), "advisory database", "severity", "`--ignore-unfixed`" (explained), "batch mode" (explained), "least privilege" (defined), "supply chain" (defined).
3. **minor** - Listing 36.2's real comment says "Unit + integration tests against in-memory H2 (MySQL mode); no services needed", while Table 36.1 and the text say the same job also runs a MySQL 8.4 Testcontainers test. Both are true (the comment predates the Testcontainers test), and the text reconciles them, but tell the reader explicitly that the comment in the file is out of date (as Chapter 4 does for the canvas comment), so they do not think the book contradicts the source.
4. **minor** - 36.7 says the failing e2e step "prints the last 200 lines of the service logs"; I could not find the number 200 with a quick search of `ci.yml` (only the loop, sleep and retention lines matched). Confirm the figure against the workflow (`docker compose logs --tail=...`) or drop the number.
5. **minor** - 36.9 says Dependabot groups "the `maven` group and the `angular` group each bundle related updates"; correct per the file, but the chapter never shows that YAML; Chapter 20 (Listing 20.8) shows the npm groups. Add "see Listing 20.8 for the npm side".
6. **minor** - Pedagogy: 36.11 (a decision procedure for a failed scan) is the best part; it ends with "someone has to remember to look" for unfixed findings. Add one sentence naming where a reader would record that (an issue, a dated TODO) so the advice is actionable.
7. **minor** - Analogy (ingredients in a kitchen) has three breakdowns: good. Exercise 36.4 ("describe one situation each flag hides") is a strong ★★ task; check the solution names one hidden situation per flag (unfixed HIGH; MEDIUM vulnerabilities; non-fatal output).
8. **minor** - The chapter says the Docker `node` and `eclipse-temurin` ignore rules live under the `docker` entry with `directories: [/, /frontend]`; Listing 36.5 matches the file. Chapter 31 shows the same rules (Listing 31.3, "added lines only") in a different excerpt; cross-reference so the two are read as one policy.
