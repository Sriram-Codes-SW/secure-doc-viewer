# Review: 10-docker-and-compose.md (window 5, full prose pass; supersedes the light pass)

Verified at book-m6-final: Listing 10.1 (`docker-compose.yml` mysql service, including the digest, healthcheck and `$$` note), Listing 10.2 (header comment) and Listing 10.3 (Dockerfile, caption now says digests replaced by `<digest>` and lists the omitted ENV values: window-4 item resolved) match the files. Facts: `container_name`, `restart: unless-stopped`, `profiles: ["full"]`, `env_file: .env`, `depends_on ... service_healthy`, ports bound to `127.0.0.1`, volume `mysql-data`; m1 compose held only MySQL (Chapter 26 agrees). MySQL 8.4 and Java 25 images correct. The digest is a public image digest, not a secret; passwords appear only as `${...}` placeholders. Exercise 10.2 correctly says to choose your own passwords. No blockers, no majors.

1. **minor** - 10.5 says "Chapter 37 compares the options" for secrets stores; Chapter 37 (11 decisions) has no secrets-store section (grep finds none). Fix the pointer: Chapter 33 or 34 (deployment/operations), whichever covers secrets, or drop the sentence.
2. **minor** - Terms used before defined: "port" and "localhost" (Chapter 2 pointers present), "argv" (in the healthcheck comment in the listing: gloss "the program's command-line arguments"), "hardens its images" (10.1 forward to Chapter 33; fine), "registry" (defined), "layer" (defined in 10.6), "stage" (10.6 uses "two stages"; define "a stage is one FROM block, a separate build environment"), "TLS front end" (Chapter 8 defined TLS).
3. **minor** - 10.1 "shares your computer's operating system core" is accurate on Linux; on Windows/macOS Docker Desktop runs containers in a lightweight VM. Add "(on Windows and macOS, inside a small Linux virtual machine that Docker Desktop manages)" so readers are not surprised by the resource use.
4. **minor** - Shipping-container analogy has its breakdown; good. The "It works on my machine" line is an adage, not a claim; fine.
5. **minor** - 10.3 says "at book-m1-accounts ... the file held only MySQL"; add the exact command for that tag or say "the rest of this chapter uses the final file" to prevent readers from running m1's file with final-file instructions.
6. **minor** - Listing 10.1 is an excerpt starting at `services:` and stopping mid-file; the `volumes:` declaration at the bottom is mentioned in text ("declared at the bottom") but not shown. Add one line of listing or say "not shown".
7. **minor** - Exercise 10.2 requires Docker Desktop and a `.env`; add a pointer to the setup chapter and a note that on Windows Docker Desktop needs WSL 2 or Hyper-V (dossier: the product owner needed several steps). No solutions in the repo yet ("Appendix C").
8. **minor** - Exercise 10.3 answer: `"3306:3306"` publishes on all interfaces so other machines on the network could connect; solution should mention firewall/LAN exposure. Confirm the solutions file says so.
