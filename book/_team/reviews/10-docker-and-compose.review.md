# Review: 10-docker-and-compose.md (window 7, expanded text, full prose review)

Verified (dossier/Grep): the MySQL choice against the implementer's H2 recommendation and the Docker/WSL/BIOS setup (D2); Dependabot's decision on MySQL 8.4 versus 26.7 and the "26.7 is an Innovation release; 9.7 the next LTS" wording (D13, PR #10); TM-14 (no Dockerfile/CI/wrapper) and the round-2 hardening (non-root nginx, digest pinning, commit 7484f4f); the X-Forwarded-For incident (D1, commits 65f2530 and 5fc0faf, `172.28.0.10`); Listing 10.1's early health check (`-p${DB_ROOT_PASSWORD}`) versus the final `MYSQL_PWD` form. The earlier caption issue with `<digest>` is fixed: Listings 10.6 and 10.7 now say "image digests replaced by `<digest>`" and the multi-line `ENV` is shown in full (RESOLVED); the coordinator's script reports the only non-exact listing lines are those placeholders. The AI-agent wording for the technical-manager review is present; the dossier is cited only in source comments. No secrets (passwords appear only as `${...}` placeholders; the image digest in Listing 10.2 is a public pin). No blockers.

## Findings

1. **minor** - Wrong internal reference: 10.1 says the project "hardens its images (Section 10.10)", but hardening is Section 10.9 (10.10 is "Why Docker and not the obvious alternatives"). Fix to 10.9.
2. **minor** - The cross-reference "Chapter 37 compares the options" for secrets stores (10.7) still points to a chapter that has no secrets-store section (raised in window 5; re-check after Chapter 37's expansion). Point to Chapter 33 or 34, whichever covers secrets in deployment, or add the section to 37.
3. **minor** - Sequencing with Chapter 9: Chapter 9's hands-on SQL (9.3) depends on the container this chapter teaches. Add a sentence at 10.4 ("If you came here from Chapter 9's hands-on section: this is the command it needed") and, since the tables only exist after the migrations run, the app or the migration files must be applied (see the Chapter 9 review, item 1).
4. **minor** - Terms used before defined: "root" (10.8 `USER app` versus root: define "the all-powerful administrator account of a computer"), "host" and "host name" (Figure 10.1 "the host"; 10.6 "host name `mysql`"), "subnet" (10.6, 10.11), "heap" (10.6, 10.8: "the memory a Java program uses for its objects"), "daemon" (10.12 error text), "reverse proxy" (10.11 describes nginx passing requests on; name it), "unprivileged", "headless" (explained), "argument list/argv" (explained), "digest" (defined), "layer" (defined), "build context" (defined).
5. **minor** - 10.4 tells the reader the owner "had to install Docker, update the Windows Subsystem for Linux and enable hardware virtualization in the BIOS". Correct per the dossier, but it is personal setup detail and the same sentence appears in 10.12. Say it once, in 10.12, as practical advice.
6. **minor** - Pacing: Listing 10.4 (about 30 lines) and Listing 10.6 (a full Dockerfile) are long for a beginner; both are explained bullet by bullet, which is good. Consider a short table for the Compose keywords (`image`, `build`, `ports`, `expose`, `volumes`, `environment`, `env_file`, `depends_on`, `healthcheck`, `profiles`) as a reference (Table 10.1) so the reader can look them up later.
7. **minor** - `docker compose exec mysql mysql ...` (used in Chapter 9) and `docker compose logs mysql` (10.4) depend on the service name `mysql`; add that service names, not container names, are used by Compose commands (a common beginner error, since `container_name` is also set).
8. **minor** - Exercises 10.1 to 10.6 are solvable from the text; 10.2 requires Docker installed (10.3 shows how). 10.5 (what happens without `depends_on`) is good. The Windows/macOS versus Linux difference is noted only in one line; Docker Desktop's licensing terms are not mentioned (optional).
9. **minor** - Analogy (shipping container) has two breakdowns: good. "Image is the blueprint and container is one live copy, like Chapter 4's class and objects" is a helpful callback; state that the analogy is only partial (images are layered, read-only).
