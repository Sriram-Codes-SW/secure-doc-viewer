# Review: 31-m6-final.md (window 7, expanded text, full prose review)

Verified (dossier/arithmetic/Grep): the milestone facts (four paths changed between m5 and m6; PRs #9 to #12; Vitest 5.0.1 and jsdom 30.0.1 only at m6; the Dependabot rules and reasons in D13; MySQL 26.7 Innovation release with 9.7 the next LTS; the failed group PR #8 with TypeScript 7 against Angular's peer range `>=6.0 <6.1`); the flaky-test story (PR #9, commit ec6c1c5, the 5-second bounded wait) matches Chapter 18; the last-round stories match the dossier (G1 nine parallel guesses through a limit of five, G2 the 72-byte BCrypt 500, G9/G7 Tomcat 11.0.24 and the pin, G12 the admin page that never expired, G13 the endless 410 loop, D14 the ultrareview refusal: 165 files and 22,096 lines against limits of 500 and 8,000); `LoginThrottle.checkAllowed` and `reserve` are real (a public synchronized method); exercise answers are right (`^5.0.1` accepts 5.4.0 but not 6.0.5; `~6.0.2` accepts 6.0.5 but not 5.4.0; a 30-character, 4-bytes-each password is 120 bytes and is refused with the byte-limit message). The AI-agent wording is present and consistent; the dossier is cited only in source comments. Listing exactness is covered by the coordinator's script. No secrets. No blockers.

## Findings

1. **major** - Listing caption format: `*File: ...*` before each code block instead of the house `*Path: ...*` after it (same finding as Chapter 28, item 1; applies to Listings 31.1 to 31.7). Listing 31.1 is a diff and 31.3 shows "added lines only"; both are disclosed, but use `# ...` / `// ...` markers consistently (Listing 31.3 has a bare `...` in a YAML comment line, 31.5 a bare `...` line).
2. **major** - Requested blueprint still missing: the editor's request (requests.md, editor to writer-app) asks Chapter 31 to include the Blueprint v6 Mermaid block and the "what changed: nothing structural" note; the chapter says "Blueprint v6 has no diagram of its own" and points to Figure 30.1. Either copy the v6 block (the request stands) or, if the writer prefers the pointer, agree it with the editor; every other Part IV chapter has its own figure.
3. **minor** - Wrong chapter reference: 31.11 says "BCrypt, the hashing algorithm from Chapter 26"; BCrypt is taught in Chapter 15 (Chapter 26 only mentions it). Also "the sign-in throttle from Chapter 26" is better pointed to Chapter 16, where the three lockout rules are explained.
4. **minor** - Terms used before defined: "OSV scan" and "Trivy" (31.3: name and one-line gloss: scanners for known vulnerabilities in dependencies and in container images), "Testcontainers" (Chapter 18), "peer dependency" (defined), "race condition" (defined), "atomic" (31.10 lesson: "atomic step"), "synchronized" (explained), "manifest"-free, "GHSA" (explained), "ultrareview" (defined), "probe" (defined by use), "render slot" (Chapter 30 pointer), "LTS" (defined), "CVE" (defined), "Flyway validated" (31.16: "checked that the migrations still match"), "restore drill" (Chapter 34).
5. **minor** - 31.15 quotes the product owner's own words from the build conversation at length ("since it is an ultra review, would it not be beneficial ...", "let us merge PR #5 ..."). The dossier (D14) supports it, but it is private chat text and adds little; paraphrase ("the owner asked for the review to run on the whole codebase, then decided to merge first") and keep the numbers (165 files, 22,096 lines vs 500 and 8,000).
6. **minor** - 31.14 names a specific tool command (`/code-review high`) and 31.15 describes "a cloud multi-agent code review offered by the coding tool used to build the project". Consistent with the neutral wording elsewhere, but the command name should not be a reader-facing detail: say "a local code-review dry run".
7. **minor** - 31.16 lists an "honest list of what is open" including "retiring the stale copy of the project left in the old OneDrive folder": a personal housekeeping detail of the owner's machine. Keep "executing the go-live checklist" and the sensitivity-levels question; drop the OneDrive item or state it generally ("clean up old copies").
8. **minor** - Pacing: the Beginner tier (31.1 to 31.4) is short and effective; the chapter's length is mostly stories in the Advanced tier. The template's "Architecture blueprint vN" then "Decisions and challenges" order is followed. Good use of two analogies (car, tires), both with breakdowns.
9. **minor** - Exercise 31.4 embeds a code snippet inside the exercise; fine as an Example, but make it a numbered "Example 31.1" so STYLE's Examples rule (no tag, numbered per chapter) is followed. Exercise 31.6 (★★★) needs a solution that says "one good answer"; not re-read.
10. **minor** - Table 31.1 (race order) is an excellent teaching device; keep. Tables 31.2 and 31.3 are referred to right after they appear.
