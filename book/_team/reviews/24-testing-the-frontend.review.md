# Review: 24-testing-the-frontend.md (window 7, expanded text, full prose review)

Verified (Grep/arithmetic/dossier): all `idleState` test values are right (26 minutes gives 240 seconds left; a 120-second timeout shrinks the warning window to 60 seconds, so 90 left is `active` and 50 left is `warning`); Example 24.1's boundary tests are correct (`secondsLeft` 300 warns, 301 does not, given `<=` in `idle.ts`); Exercise 24.1's answer is `active` (500 seconds left vs a 300-second window). The e2e suite really asserts `[401 x 5]` then `429`, skips itself without `E2E_ADMIN_PASSWORD` (no default password in code), and expects the text "hasn't been shared with you"; the CI e2e job has `needs: [backend, frontend]` as the chapter says; the X-Forwarded-For incident (TM2-1, fix 65f2530) matches the dossier and Chapters 10, 22 and 32 also tell it; the axe/accessibility story (G11, commit 7484f4f, `--on-accent` and darker `--muted` in `styles.css`) matches the dossier; the AI-agent wording is present. The chapter tells the reader to read credentials from the environment and never quotes any (compliant with the charter's secret rules). Listing exactness is covered by the coordinator's script. No blockers, no majors. An excellent chapter; the worked boundary-test example (24.3) is the model of what a beginner needs.

## Findings

1. **minor** - Exercises lack the "*Solution:* Appendix C, Exercise 24.N" lines used in earlier chapters (the closing sentence points to the solutions file instead). Use one house format (same note as Chapters 20 and 23).
2. **minor** - Terms used before defined: "provider" (24.4: "which providers to use (the services and settings, Chapter 22)" is a gloss; good), "fixture" and "stub" (defined in 24.5), "flush" (defined by use in 24.4), "injection context" (pointer to Chapter 22), "Chromium" and "headless" (24.10), "trace" (explained), "WCAG" (Chapter 21 pointer), "regression" (defined 24.13), "watch mode" (explained), "polling" (24.14 uses it before an explanation: "polls the server every five seconds" needs "asks again and again on a timer"), "jsdom" (defined in 24.2), "spec" (defined), "Playwright context" (`browser.newContext()`, 24.11: "a separate browser profile with its own cookies").
3. **minor** - 24.11 says the project chose Playwright "for reasons visible in the code". The dossier records that the e2e suite was added in Phase 5 but no comparison with Cypress or Selenium; the sentence lists capabilities the code uses, not recorded reasons. Reword ("features the project's tests rely on") and keep the honesty sentence used in Chapter 6.9.
4. **minor** - Pacing: about 5,500 words, and the Beginner tier alone runs to 24.5 (a full component test with `TestBed`, `ActivatedRoute` stubs and `HttpTestingController`). A first-time reader meets `TestBed`, providers, fixtures, stubs and `flush` in one sitting. Consider moving 24.5 (the viewer's `create` helper) into the Intermediate tier and keeping 24.4's guard spec as the only component-level example in the Beginner tier.
5. **minor** - 24.10 tells the reader that the suite needs a running full stack and an admin account "supplied through `E2E_ADMIN_USER` and `E2E_ADMIN_PASSWORD`", but not how a reader without the owner's credentials should get an admin (the bootstrap admin password is printed once in the log, Chapter 15). Add a pointer ("use the bootstrap admin from Chapter 15") or mark the e2e run as optional for readers; also warn not to reuse a real password.
6. **minor** - 24.12: `expectAccessible` runs axe in both themes; add that `emulateMedia` needs the app to define both palettes (Chapter 21) and that the check is a floor (the chapter says so: good). Exercise 24.4's answer is in the text.
7. **minor** - 24.14 (`shouldPoll`, `POLL_IDLE_MS`) is a good pure-function example but the admin polling design is introduced only by a pointer to Chapter 22. Say in one sentence why the poll must stop when idle ("polls count as activity and would keep an idle admin signed in": the test name already says it).
8. **minor** - Restaurant analogy has a proper breakdown. Callout Notes are used sparingly and are correctly typed. Version note (Vitest 5 only at m6) agrees with Chapter 20.
9. **minor** - Common mistakes: "Forgetting `http.verify()`" refers to a call that "(used in the viewer spec)" but Listing 24.3 does not show it; add it to the excerpt or say "not shown in the excerpts".
