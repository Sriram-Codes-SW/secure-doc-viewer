# Review: 12-rest-controllers-and-json.md (window 3)

State: partial draft (12.4 onward unwritten as of this pass). Verified at book-m6-final: Listing 12.1 and 12.2 match `DocumentController.java` (lines 23-25, 41-45, 47-51, 70-75; the quoted phrase "only adapts HTTP" is from the class Javadoc, "this layer only adapts HTTP"). `Viewer` class exists (`document/Viewer.java`). No secrets. No blockers.

1. **major** - Listing 12.1 contains an invented line: `// ... constructor as in Listing 11.2 ...` is not in the source and the listing has no closing brace. The label says "simplified: the start of the class and one method", but a comment written by the author inside a listing must be marked as such. Fix: use a neutral marker (`// ...`) and say in the caption "simplified: the constructor, fields and nested records are omitted (see Listing 11.2)". Also add italic path line per STYLE 11.2.
2. **major** - "Listing 12.2 shows all of them in one class" is untrue: the listing shows a path variable and a request body, but `@RequestParam` appears only in prose. It also places the nested record `UpdateDocumentRequest` above methods although in the file it sits at the top of the class next to `TransferOwnershipRequest`. Fix: "Listing 12.2 shows the first two; the third is in the next paragraph", and caption "simplified: three declarations from different places in the class".
3. **major** - Objectives promise Jackson, binary responses, status codes and headers (12.4-12.7); those sections are not yet written, so the chapter cannot be accepted. Also Try it, Summary, Further reading and an In-this-project section are missing (the tag list in OUTLINE also names `book-m1-accounts`; the writer's note says to verify m1 listings by `git show`).
4. **minor** - Terms used but not defined: `Authentication`, `Viewer`, `HttpServletRequest`, `actors.of(...)` (Chapter 11 mentions `RequestActors` only in passing), "JSON array", "path prefix", "query string". Fix: one sentence each, or say "you can ignore `HttpServletRequest`; Chapter 15 explains it".
5. **minor** - Library-desk analogy: the breakdown is given (sessions bend statelessness), good; "REST-style" honesty is good. The phrase "the academic definition" is fine but cite Roy Fielding's dissertation in Further reading.
6. **minor** - PATCH is used before Chapter 8's list of HTTP methods is confirmed; check that Chapter 8 defines PATCH, or define it here (12.1 does mention it, fine).
7. **minor** - "If a required one is missing, Spring raises an error, which Chapter 13 turns into a clean JSON 400": verify against `GlobalExceptionHandler` at m6 before publishing (MissingServletRequestParameterException mapping).
