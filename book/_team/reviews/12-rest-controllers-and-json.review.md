# Review: 12-rest-controllers-and-json.md (window 9, expanded text, full read; replaces the window-3 review, whose majors are RESOLVED)

Method: full read; `listingcheck.py 12` (all listings verified); code claims checked at `book-m6-final`: `DocumentService.update` changes only non-null title and visibility (PATCH semantics as taught); `TileUrlGrid` keys (`page`, `rows`, `cols`, `tileSize`, `tileVersion`, `tileUrls`) match the JSON sketch in 12.5; seven controllers with the prefixes in Table 12.4; `PageTileUrlController` maps `/api/documents/{documentId}/pages/{page}/tile-urls`; `TileController.loadTile` matches Listing 12.7 (410 only when the document has moved on, else 500); Jackson 3 (`tools.jackson`) is used by `SecurityErrorResponses`. Figure 12.1 is true (the four requests in the order the Angular viewer makes them). The library-clerk analogy has its breakdown sentence. Examples carry no tag and use placeholders (`<csrf-token>`, `<signed-token>`). The Chapter 13 forward pointers ("which Chapter 13 turns into a clean JSON 400") are pointers, not prerequisites. No secrets, no dossier citations. No blockers, no majors.

## Findings

1. **minor** - Figure 12.1 has no `<!-- source: ... at book-m6-final -->` comment; add one naming the four controller methods.
2. **minor** - Table 12.3 omits statuses the API does return and Chapter 13 lists: `405` (`HttpRequestMethodNotSupportedException`), `415`, `500` (the catch-all) and `413`'s wording is "Content Too Large" (Spring's current name; the reader may know it as "Payload Too Large"). Add the missing rows or say "the most common".
3. **minor** - 12.10 "Put the rule in the service (Chapter 16 shows `DocumentService`)": `DocumentService` is shown in Chapters 14 and 17 (transactions, upload) and its rules in 16 only by comment. Point to Chapter 14 (Section 14.6) and 27.
4. **minor** - 12.4: the claim "the key appears with the value `null`" for `sharedWithCount` is Jackson's default; the project sets no inclusion rule (I checked `application.yml`). Say "by default Jackson writes null as `null`", so a reader who changes the setting is not surprised.
5. **minor** - Terms used before defined: "endpoint" (used in 12.1 as if known; define: "one path plus method the server answers"), "MIME type / content type" (12.6, defined by use), "multipart" (defined), "serialization" (defined), "resource" (defined), "idempotent" (not used, good), "enum" (defined in 12.4 in passing; Chapter 4 also).
6. **minor** - Exercise 12.3 needs the app running with data, which at `book-m6-final` means the full setup; add a pointer ("Part IV's box, 'How to run an older milestone', lists what you need; for `book-m6-final` use `docker compose --profile full up -d --build`"). Exercise 12.5 (design an endpoint) is open-ended; the solutions file should say "one good answer".
7. **minor** - Pacing: the Beginner tier (12.1 to 12.3) is right for a beginner, with a good worked example. 12.5 (four requests) and 12.6 (bytes) are Intermediate; 12.8 to 12.9 are Advanced. All good; 12.9 (two small design habits) could move into 12.2 as a callout.
