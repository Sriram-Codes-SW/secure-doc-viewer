# Review: 14-jpa-and-flyway.md (window 5, full prose pass)

Verified at book-m6-final (and m2 where labeled): all listings match the files (`AppUser`, `AppUserRepository`, `UserAccountService.create` signature, `DocumentService` `TransactionTemplate`, `V1` SQL, `DocumentRepository.findByIdForUpdate`, `AuditLogService.record` with `REQUIRES_NEW`). Table 14.1 matches the five `@Scheduled` methods exactly: audit purge cron `0 30 3 * * *` (property-placeholder default confirmed), `sweepThrottled` every 3,600,000 ms, known-device purge cron `0 45 3 * * *`, `LoginThrottle.sweep` 300,000 ms, `StorageJanitor` initial delay PT2M then PT6H. V3 adds `tile_version`, `must_change_password`, `last_sign_in_at` and creates `account_known_ip` (confirmed). `MySqlIntegrationTest` really sets the container to `-03:00` and the JVM to Asia/Kolkata. `ddl-auto: none`, H2 in MySQL mode, Hibernate 7 / Spring Boot 4 correct. No secrets. No blockers, no majors. One of the best chapters so far.

1. **minor** - 14.4 says `KnownDevices.purgeExpired` deletes rows "older than 30 days" (Table 14.1). I did not verify the 30-day figure in code/config (the dossier says "recognised device 30 days"); add the property name or a source comment.
2. **minor** - The Advanced tier heading says "Isolation, cleanup and real databases", but no transaction isolation levels are taught. Rename ("Cleanup, separate transactions and real databases") to avoid promising a concept the chapter does not cover.
3. **minor** - Terms used before defined: "rollback" (used in 14.3 immediately after defining transaction; add "undo everything the transaction did"), "commit" (same), "connection"/"starve other requests" (a database connection pool is never defined), "JDBC URL" (14.9), "H2" (14.8 first mention: "an in-memory database" is a gloss, good), "Testcontainers" (only named in Chapter 18), "wrapper/proxy" (14.6, backward pointer to 11.8: add "(Section 11.8)").
4. **minor** - Cron schedules in Table 14.1 run in the server's time zone by default; the app runs in UTC in Docker but a laptop may differ (the chapter's own 14.9 theme). One sentence: "Spring evaluates cron in the server's zone unless configured."
5. **minor** - Listing 14.1's `// ... must_change_password and last_sign_in_at columns omitted ...` is an authored comment inside a listing; STYLE 11.2 wants omissions marked plainly. It is clear and acceptable; keep but ensure the caption also says so (it says "getters and setters omitted" only). Add "and two columns".
6. **minor** - The unnumbered transaction snippets in 14.3, 14.5 and 14.6 are real code but carry only a parenthetical tag; STYLE requires numbered, captioned listings. Number them (Listings 14.4-14.8) so Table/Listing cross-references work.
7. **minor** - Exercises: `(★)` inline format instead of `### Exercise 14.N ★ ...` (STYLE 12); solutions file exists. Exercise 5 needs a scratch copy warning (never edit a migration in the reader's real database). Objective "compare `@Transactional` with `TransactionTemplate`" is tested by exercise 4: good.
8. **minor** - Metadata says `tag: book-m2-documents` while listings are `book-m6-final`; STYLE 11.5: Parts I-III quote m6 by default. Set the metadata tag to book-m6-final or explain the mix in a note (the chapter does say V1 is identical at m6).
