# Review: 14-jpa-and-flyway.md (window 9, expanded text, full read; replaces the window-5 pass)

Method: full read; `listingcheck.py 14` (all listings verified); code claims checked at `book-m6-final`: `AppUser`, `AppUserRepository` (the `findTop20...` method), `Document` relationship annotations, `findVisibleTo`, `findByIdForUpdate` with `PESSIMISTIC_WRITE`, V1, V2, V3 migrations (V3 first appears at `book-m5-platform`), `open-in-view: false`, `ddl-auto: none`, `hibernate.jdbc.time_zone: UTC`, the JDBC URL with `connectionTimeZone=UTC`, `AuditLogService.record` with `REQUIRES_NEW` and a second `TransactionTemplate` with `PROPAGATION_REQUIRES_NEW` for `recordAtMostEvery`, `MySqlIntegrationTest` exists. Figure 14.1 matches `replaceFile` step for step (I compared it with Listing 39.2). History (audit rows lost in a rolled-back transaction, C3, commit `ba00693`; audit times five and a half hours ahead, C6; janitor rule, F1, `66f7152`) matches the dossier. The index-card/rooms analogy has its breakdown sentence. Pattern note pointers (38.2, 38.5) are right. The dossier is cited only in HTML comments. No secrets. No blockers, no majors.

## Findings

1. **minor** - 14.10 says "The project has five" scheduled sweeps and Table 14.2 lists five, but the code at `book-m6-final` has six `@Scheduled` methods: it also has `TileRateLimiter.sweep` (fixed delay 300,000 ms, "discards idle windows"). Add the row (and "six", "the most careful of the six"). Chapter 38 (38.9) points to Chapter 14 for that sweep and Chapter 16 (16.6) mentions only `LoginThrottle`'s.
2. **minor** - Figure 14.1's source comment names the tag correctly. The figure and Listing 39.2 differ only in that this figure adds the first short transaction ("may this caller manage it") and the audit record; both are in the code (Listing 39.2 omits them, and says so). No change needed; note it if the two chapters are ever merged.
3. **minor** - 14.5: Listing 14.5 is captioned "`book-m2-documents`, identical at `book-m6-final`", but the chapter's opening note says the chapter "belongs to milestones 1 and 2" and `V1` exists at `book-m1-accounts` too (the file is identical). Say "first appears at `book-m1-accounts`".
4. **minor** - Terms used before defined: "session" in "database session" (14.3: says "a database *session* is still open"; Chapter 8 defined web sessions, so add "(unlike a web session)"), "JPQL" (defined in 14.4), "N+1" (defined), "dirty checking" (defined), "propagation" (defined), "cron" (defined), "pool" (defined in 14.6), "H2" (14.11: defined by use).
5. **minor** - Exercise 14.5 asks the reader to add a `V4` migration to a scratch database; at `book-m6-final` that needs MySQL from Docker and `./mvnw`. It says "never your real one" (good); add the pointer to Chapter 10 and to the "How to run an older milestone" box in the Part IV introduction.
6. **minor** - 14.9: the incident says the fix included "show UTC in the admin screen so it matches the watermark and the CSV export"; fine. But "a test that runs the JVM in Asia/Kolkata against a MySQL server set to `-03:00`" is described without saying where (`MySqlIntegrationTest`, Table 14.3 lists only the concept). Add the class name (it is named in 14.11).
7. **minor** - Pacing: about 5,400 words; the Beginner tier (14.1 to 14.4) is a good size. 14.6 (transactions) could use a small diagram of the "short transaction around slow work" idea, but Figure 14.1 covers it. The Advanced tier is dense; 14.10 (sweeps) is a table plus two paragraphs and stands alone.
