# Review: 38-design-patterns.md (window 9, new chapter, full read)

Method: full read; `listingcheck.py 38` (8 listings verified, 1 flagged, see item 3); every "where it lives" claim checked against `book-m6-final`; every cross-reference to Chapters 11 to 17 and 37 checked against the chapter files; both figures checked against the code; the "Pattern note" pointers that other chapters now make to Sections 38.2, 38.3, 38.4, 38.5, 38.7, 38.8, 38.9, 38.10 all resolve to the right section.

Verified true at `book-m6-final`: `Viewer` is a record with a static `of(Authentication)`; `WatermarkService.Layout.of` and `UserSummary.of` exist; `SessionLifetimeFilter` and `PasswordChangeRequiredFilter` exist (and, as Chapter 39 says, arrive at `book-m5-platform`); `SpaCsrfTokenRequestHandler` picks `plain` or `xor` by whether the header has text; `SecurityConfig` has `fromAddresses` and a `CompositeSessionAuthenticationStrategy` of `ChangeSessionIdAuthenticationStrategy` and `RegisterSessionAuthenticationStrategy`; `HttpSessionEventPublisher` with the quoted comment; `SessionMetadata` has the two `@EventListener` methods; `TileGenerationService` has `RenderState { RUNNING, DONE, ABANDONED }` with an `AtomicReference` and the two `compareAndSet` calls; `TileRateLimiter` is a per-user timestamp log with `Retry-After` computed from the oldest entry, and a `@Scheduled` sweep every 300,000 ms; `TileWorkLimiter` is a fair `Semaphore` with a `WAIT_MILLIS = 2_000` (so Figure 38.2's "no permit within two seconds" is right); `TileController` has the `refund` on `ServiceBusyException`; `TileGenerationService` has its own render semaphore; `safeReturnUrl` exists in `login.component.ts`; `roleGuard` matches Listing 38.9 and every route in `app.routes.ts` has exactly one `canActivate` guard (so "no multi-guard chains" is right); `MAX_CONCURRENT_TILE_FETCHES = 6`; `shouldPoll` exists; `ViewerMetrics` uses `Counter.builder(...)`. The "patterns the project does not need" list is consistent with the code (no circuit-breaker library). History (refund arrived with the work cap, commit `156ee56`, dossier G10) is right. Every pattern follows the five-part shape, the carpenter analogy has its breakdown sentence, every "approximates" claim is stated plainly (chain of one, bulkhead by hand, adapter in spirit, reserve-then-compensate with no standard name). The AI-agent and dossier rules are respected (dossier only in an HTML comment). No secrets. No blockers, no majors.

## Findings

1. **minor** - Stale section number: the Prerequisites bullet ("needed only for Section 38.12") and the end of 38.7 ("Section 38.12") both point at "Adapter and facade". The Angular section is 38.13. Fix both.
2. **minor** - 38.9 says the sweep that discards idle windows every five minutes is described in "Chapter 14", but Table 14.2 in Chapter 14 lists only five sweeps and omits `TileRateLimiter.sweep` (the code has six `@Scheduled` methods). Either add the row to Table 14.2 (and fix its "the most careful of the five") or drop the pointer here.
3. **minor** - Listing 38.4 combines two files in one block with a fake first line `// DocumentService.java` and a `// AuditLogService.java` separator, so `listingcheck.py` cannot match it (6 lines "not in order"), and the `*Path:*` line names two paths. Split it into Listing 38.4 (DocumentService) and Listing 38.5 (AuditLogService), or turn the two snippets into two captions, which also fixes the number sequence for later listings.
4. **minor** - The `ViewerMetrics` builder snippet in 38.6 is a code block with no caption and no `*Path:*` line (only "(book-m6-final, ViewerMetrics.java, excerpt.)" in prose). Give it a listing number and Path line like the others.
5. **minor** - Figures: both are numbered in sequence, announced in the text before they appear, and their labels are true of the code (Figure 38.1 has exactly the three moves the code makes; the solution to Exercise 38.4 correctly says no move is missing). Two small gaps: the "source" comments name no line-level detail, and Exercise 38.4 ("Is there a move the figure doesn't show?") invites the reader to hunt for a move that does not exist; add "(there may be none)" so it does not feel like a trick.
6. **minor** - Terms used before defined: "semaphore" (38.9: gloss "a counter of permits; a thread takes one to proceed and must wait if none are left"; Chapter 17 introduced it, so add "(Chapter 17)"), "CIDR" (38.4 Listing 38.3 comment), "lambda" (defined, Chapter 5), "atomic" (38.8: defined by use; one clause), "idempotent" not used (good), "inversion of control" (38.5, points to Chapter 11, fine), "sliding window log" and "token bucket" (defined), "compensate" (defined), "pure function" (38.13; Chapter 19).
7. **minor** - 38.4 says `PasswordEncoder` is Listing 15.2 and that `createDelegatingPasswordEncoder()` "chooses another strategy from the `{bcrypt}` label"; correct, but the reader has not been told that the stored hash starts with `{bcrypt}`. One clause pointing to Chapter 15 would close the gap.
8. **minor** - Pacing: about 6,000 words, above the 3,500 to 4,500 the brief asked for. The Beginner tier (38.1, 38.2) is fine. The Intermediate tier packs five patterns; 38.6 (builder) and 38.12 (adapter) are thin and could be single paragraphs inside a "small patterns" section. 38.13 (Angular) and 38.14 (list of patterns not used) are well placed.
9. **minor** - Exercise 38.3 says "list every bean whose type is an interface from Spring Security": at `book-m6-final` `SecurityConfig` also returns `SecurityContextRepository`, `SessionRegistry` and `AuthenticationEntryPoint`-like handlers (via `SecurityErrorResponses`); the exercise is solvable but open-ended, so its solution should say "one good answer". I did not re-derive the solutions file this window.
10. **minor** - Exercise 38.1(b) uses `tx.execute(status -> ...)`, which is a template method with a callback (38.5) and (a) `Viewer.of(authentication)` a factory method: both are taught. (d) `@EventListener` is the observer: taught. Fine.
