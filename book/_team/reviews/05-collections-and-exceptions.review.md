# Review: 05-collections-and-exceptions.md (window 5, full prose pass)

Verified at book-m6-final: Listings 5.1 to 5.5 match the sources (`DocumentService.list`, `AppUserRepository.findByUsername`, `UserAccountService` line 155, `ResourceNotFoundException`, `DocumentController.upload`). Facts: `Set<AppUser> sharedWith = new HashSet<>()` (Document.java:68), `Map<String, Deque<Instant>> failures` (LoginThrottle.java:61), eleven types in `exception/` (count correct), `ForbiddenException` comment ("existence isn't revealed"), `Files.move(..., ATOMIC_MOVE)` in `FileOperations`. Java 25 and no secrets. No blockers.

1. **major** - Examples 5.1, 5.2 and 5.3 are bare statements (and Example 5.1 starts with `import java.util.*;` followed by statements). Pasted into a `.java` file they do not compile (statements must sit inside a method, Chapter 3). Beginners will try. Fix: one sentence before Example 5.1 ("Put these lines inside a `main` method, as in Example 3.1, or run them in `jshell`, Java's built-in scratchpad") and repeat the import note for `List` in Example 5.2.
2. **minor** - Terms used before defined: `NullPointerException` (5.4 used as a name; define "a crash caused by using nothing as if it were something"), `RuntimeException` and "inheritance" (glossed in passing, fine), "transaction" (forward pointer to Chapters 9/14 is present), `Path`/`Files`, "stream of bytes", `HashSet`/`ArrayList`/`HashMap` (implementations vs interfaces `List`/`Set`/`Map`, unexplained; add one sentence: "`List` is the promise, `ArrayList` is one way to keep it, Chapter 4"), the diamond `<>` in `new HashSet<>()`, `ConcurrentHashMap` (not in text, fine).
3. **minor** - The Optional "box" description is an analogy without a breakdown (STYLE 8 requires "Where the analogy breaks down"). Add: "Unlike a real box, an `Optional` should never itself be `null`, and you should not use it for fields or parameters."
4. **minor** - 5.1: a `HashSet` and `HashMap` do not keep order; the text says lists keep order and sets keep uniqueness but never tells the reader sets/maps may print in any order, which will confuse the first time they print one. One sentence.
5. **minor** - Example 5.3 depends on `TileGrid.tileCount`, which the reader does not have unless they cloned the repository (Chapter 7 comes later; Chapter 3 gave the code in Listing 3.3). Say "the `tileCount` method from Chapter 3, copied into your own file".
6. **minor** - Objective "Explain why the app stores times in UTC" is supported by 5.7, but the commit reference `2d82253` inside a source comment is fine; the reader-facing sentence "A server in Docker runs in UTC, while a developer's laptop may run in local time" is a plausible explanation but the real incident (audit timestamps in IST, found by review) is a better teaching story and is in dossier bugs C6; add it in one sentence.
7. **minor** - Exercises: format matches STYLE 12; 5.3 (★★) mixes two tasks (throw/catch and custom exception): fine, but it needs the solution to show `class PageOutOfRange extends RuntimeException`. Confirm the solutions file does. Also "Appendix C" solutions do not exist yet (compiled by editor).
8. **minor** - `5.6`: "For files on disk Java offers `Path` ... `Files.move` to rename a folder in one step" - true, but "in one step" should say "atomically (all or nothing)" and Chapter 17 does not necessarily cover FileOperations; verify the forward pointer.
