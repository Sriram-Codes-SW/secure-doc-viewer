# Review: 05-collections-and-exceptions.md (window 7, expanded text, full prose review)

Verified (arithmetic, dossier, Grep): `Instant.parse("2026-09-19T00:00:00Z").getEpochSecond()` is 1,789,776,000 (20,715 days x 86,400); Example 5.8 leaves 2 entries; the four incidents match the dossier (G1 nine parallel wrong passwords and the atomic reserve fix, commit 708fd8c; G2 the 72-byte BCrypt 500; C6 audit rows in IST vs UTC; the eleven exception types); the limits (5 per account and address, 20 per address, 20 account-wide from unrecognized devices, 15-minute window) match the dossier numbers. The reviewers are called "the reviewer"/"the product-owner reviewer"; the AI-agent nature is stated in Chapters 1 and 4, acceptable, but see item 6. The dossier is cited only in source comments. The "characters vs bytes" thread closes correctly (resolves Chapter 2 and Chapter 3 forward references). Listing exactness is covered by the coordinator's script. No secrets. No blockers.

## Findings

1. **major** - Pacing and sequencing: the Advanced tier (5.9 to 5.11) introduces threads, `ConcurrentHashMap`, `computeIfAbsent`, `synchronized`, deques and three production incidents inside Chapter 5 of a book for someone who wrote their first `println` two chapters ago. The incidents are excellent but the concurrency vocabulary (thread, synchronized, atomic, thread-safe) is either defined in passing or not at all. Fix: label 5.9 to 5.11 "safe to skip on a first read; Chapter 16 returns to it", define "atomic" and "thread-safe" where used ("atomically" in the incident, "thread-safe types" in the Summary), and consider moving 5.9 and the G1 incident into Chapter 16 (where `LoginThrottle` is the topic) leaving a two-sentence pointer here.
2. **minor** - Analogies: "a dictionary" for a map (5.1) and "a box" for `Optional` (5.5) are analogies without a breakdown (STYLE 8 requires "where the analogy breaks down"). Add one sentence each: a dictionary is sorted and a `HashMap` is not; a real box can be empty and still exist, but an `Optional` variable itself must never be `null`.
3. **minor** - 5.5 says Example 5.4 shows "the three common ways of opening the box", but the example shows `orElse` and `orElseThrow` (plus `ofNullable` for creating one); no third way appears. Say "two" or add `ifPresent`.
4. **minor** - 5.6 says "which you have seen in `DocumentController.upload`" about `throws IOException`, but no earlier chapter showed that signature (Listing 5.6 begins mid-method). Reword: "you will see `throws IOException` in the real upload method".
5. **minor** - Terms used before defined or never defined: "log" (5.11: "writes the full exception to the log": one clause), "thread" (5.9, defined there; but "each request on its own thread" is first said in the introduction to the Advanced tier), "atomic/atomically" (5.9 incident), "thread-safe" (Summary), "cutoff" and "window" (5.9: defined by use), "Hibernate" (defined inline), "Docker container" (pointer), "queue" (5.2 uses deque; add "a queue lets items leave in the order they came in"), "handler of last resort" (5.11), "reference" (a random short id: defined by use).
6. **minor** - 5.11 and 5.10 describe review findings as "the reviewer" and "the product-owner reviewer" without saying they were AI review agents; Chapters 1 and 4 say so. Add a five-word reminder once in this chapter ("the AI technical-manager reviewer") to satisfy the book-wide rule.
7. **minor** - Example 5.1 through 5.8 all run as standalone files (good: a clear improvement over the first draft). Confirm each compiles as shown: Example 5.4 needs no import beyond the two shown (yes), 5.7 imports `Duration` and `Instant` (yes), 5.8 imports `ArrayDeque`, `Deque` (yes). Example 5.6 prints "parsed 12" before the returned value only because `finally` runs before `main`'s `println`; the comment "parsed 12, then 12" is correct.
8. **minor** - Listing 5.7 (`append`/`prune`) shows `synchronized` on the queue in `append` only; `prune` is called from code not shown. A careful reader will ask why `prune` is not synchronized. Add "the real code calls `prune` while holding the same lock (see the full method)" if true, or note it as a simplification; verify against `LoginThrottle` before writing.
9. **minor** - Exercises: format correct. 5.5 (★★★) is well pitched and solvable from 5.8 and 5.9. 5.2 assumes `map` on an `int` list and `printf`/`println` inside a stream; say "print inside `forEach`" or supply the shape. Solutions file not re-read.
10. **minor** - Table-less chapter: the three collection kinds would read better as a small comparison table (Table 5.1: keeps order, allows duplicates, lookup by, example in the app). Optional.
