# Review: 15-spring-security-authentication.md (window 7, expanded text, full prose review)

Verified (Grep/dossier): `UserAccountService` has `MAX_PASSWORD_BYTES = 72`, `fitsBcrypt`, and the username pattern `[a-z0-9._-]{3,32}`; `BootstrapAdmin` generates a random 20-character password with `SecureRandom` and its comment compares the approach to Spring Boot's default user; the AuthController quotes ("never contains the session id", the fixed `Invalid username or password.` message) and the test `wrongPasswordAndUnknownUserGetTheSameAnswer` exist (earlier windows); `DaoAuthenticationProvider` is constructed in `SecurityConfig`. The chapter's honest note on response-time leaks is correct for Spring Security (it hashes a dummy password for unknown users). Earlier findings RESOLVED: Exercise 15.1 now asks for a "setting", JWT is defined, "servlet" is defined, Listing 15.1 now says the other parameters are omitted, and Chapters 5, 13 and 14 are in the prerequisites. Listing exactness is covered by the coordinator's script. No secrets (`correct-horse-battery` is a labeled placeholder; hash strings are placeholders). No blockers, no majors. A strong, well-paced chapter; the sign-in walk-through (15.5) is the best teaching device in Part II so far.

## Findings

1. **minor** - Pacing: at about 4,500 words with three long `AuthController` excerpts (Listing 15.7 is 20 lines with `sessionMetadata`, `SecurityContextHolder`, `PasswordChangeRequiredFilter.SESSION_ATTRIBUTE`), a first-time reader may lose the thread in Step 6 and 7. The text already says which sections are the heart; add a one-line summary before Listing 15.7 ("in short: prove the password, then start a fresh session, then remember a few facts").
2. **minor** - Terms used before defined: "known device" and "recognisedDevice" (Listing 15.5; the concept is Chapter 16), "session registry" (15.6), "metrics" (Steps 5), "audit event" (defined in Chapters 14/27 only), "handle" (defined by use in 15.9), "principal"-style vocabulary is avoided (good). "opaque" (15.9) is glossed by use.
3. **minor** - Forward pointer: 15.8 says the audit log records every failure "(Chapter 14)", but Chapter 14 covers only why audit writes use `REQUIRES_NEW`; the audit trail itself is taught in Chapter 27. Change to "(Chapters 14 and 27)".
4. **minor** - 15.9 relies on "the HMAC from Chapter 17", which comes later; add "(you will meet HMAC properly in Chapter 17; for now think of it as a keyed fingerprint)".
5. **minor** - Exercise 15.3 asks the reader to print `UserAccountService.fitsBcrypt(...)` from "a small Java program on your own branch". Calling a project class needs the project on the classpath, which a beginner cannot do yet. Say "copy the method into your own file" (it is a static method with no dependencies), or provide the two-line copy in the exercise.
6. **minor** - Exercise 15.6 ("forgot password" and timing) goes beyond what the chapter teaches about timing (one "we simplify here" paragraph). It is a good stretch task but its solution ("a worked outline") must not overclaim; make sure the solutions file says the mitigation (same response for both cases, similar work done, sending mail asynchronously) is a design outline, not code from the project.
7. **minor** - Analogy: guard and badge reader has a proper breakdown. The session cookie has no analogy here (Chapter 8 uses the coat-check ticket): a one-line callback ("the ticket from Chapter 8") in 15.6 would tie the two chapters together per STYLE 8.1.
8. **minor** - Table 15.2 (sessions versus tokens) is fair; the sentence "Neither approach is universally better; the point is that the requirements chose" is good. It does not say that the dossier records no recorded debate about tokens (Chapter 26 says so honestly); confirm the two chapters agree on how the decision is described (chosen by the implementer within the approved plan, D3).
9. **minor** - Version note: the chapter is tagged `book-m1-accounts` but most listings come from `book-m6-final`; the note explains it, but STYLE 11.5 prefers the metadata tag to match the majority. Consider `tag: book-m6-final` in the header comment.
