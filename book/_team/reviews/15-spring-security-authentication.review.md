# Review: 15-spring-security-authentication.md (window 5, full prose pass)

Verified: every non-comment line of all numbered listings exists in the real files (script check at m1/m6 as labeled). Facts confirmed: `fitsBcrypt` in `UserAccountService` (used at sign-in in `AuthController` and when setting passwords), `DaoAuthenticationProvider` in `SecurityConfig`, `AuthController` class comment ("The response never contains the session id: it travels only in the httpOnly session cookie set by the container", present at m1 and m6), test `wrongPasswordAndUnknownUserGetTheSameAnswer` in `SecurityIntegrationTest`, single message `Invalid username or password.` in `GlobalExceptionHandler`, `Role` and `DatabaseUserDetailsService` identical at m1 and m6 (per the chapter's version note). The 30-emoji exercise is right (4 bytes each = 120 bytes > 72). No secrets. No blockers, no majors.

1. **minor** - Exercise 1 asks "Which annotation makes the session cookie unreadable to JavaScript?" but it is a configuration key (`server.servlet.session.cookie.http-only: true` in `application.yml`), not an annotation. Reword: "Which setting ...?" and fix the solution.
2. **minor** - Terms used before defined or never defined: JWT (parenthetical only: add "JSON Web Token, a signed piece of text holding your identity"), "servlet" (defined through "servlet filter" but the word "servlet" itself is not), "authority" and the `ROLE_` prefix (explained by one sentence), "delegating encoder" (15.3: explain that it chooses an algorithm by the `{bcrypt}` prefix), "normalized", "audit log (Chapter 14)": Chapter 14 covers `REQUIRES_NEW` for audit writes, not the audit trail itself; point to Chapter 27 (Part IV) or drop the number.
3. **minor** - The version note is long and technical for a beginner chapter; move the per-listing tag information to the listing captions (already done) and cut the note to two sentences.
4. **minor** - Unnumbered code snippets in 15.3, 15.4 and 15.7 (the `fitsBcrypt` method, the cookie settings, the `AuthController` excerpt) carry only parenthetical tags. STYLE 11.2 wants numbered, captioned listings.
5. **minor** - 15.4 quotes `application.yml` settings in bullets without showing the YAML (Chapter 8 already showed it). Cross-reference: "as in Listing 8.2".
6. **minor** - 15.7 "We simplify here: an attacker could still compare response times". Good honesty; but check whether the app mitigates timing (e.g., hashing a dummy password for unknown users) and say so or say "the project does not"; not verified.
7. **minor** - Exercises use inline `(★)`; STYLE 12 wants `### Exercise 15.N ★ Title`. Solutions file exists? Not checked. Analogy (front desk guard vs badge reader) has a breakdown: good.
8. **minor** - Prerequisites list Chapters 8, 11, 12, 14, but the chapter also depends on Chapter 5 (Optional, exceptions in `AuthController` excerpt) and Chapter 13 (`GlobalExceptionHandler`). Add them.
9. **minor** - Listing 15.1 contains an authored line, `public SecurityFilterChain securityFilterChain(HttpSecurity http, /* ... */) throws Exception {` (the real signature has four more parameters). The caption says "simplified" but not that the signature was shortened. Say so, or show the real signature.
