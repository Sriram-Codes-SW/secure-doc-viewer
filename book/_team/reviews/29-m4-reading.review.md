# Review: 29-m4-reading.md (window 3)

Verified at book-m4-reading: every non-comment line of Listings 29.1 (`idle.ts`), 29.2 (`WatermarkService`) and 29.3 (`SessionKeys.crockfordBase32`) exists in the real files. Facts: opacity clamp 0.05 to 0.6 and spacing clamp 0.5 to 6.0 (WatermarkService lines 49-50); second watermark line is UTC time, a middle dot and the trace code (line 90); `sessionTimeoutSeconds` is on `CurrentUser` (AuthController); viewer ignores keys with Ctrl/Meta/Alt held (line 147), PageUp/Home handlers and `replaceUrl: true` exist. History (PO-9, PO-10, PO-11, 0.28 to 0.2, Crockford I/l misreading, 68 backend / 14 frontend tests) matches the dossier (decisions D5/D9, bugs C5, timeline 60e104a). Exercise 1 answer: 30-minute timeout with 10 minutes left returns `active` (600 s > min(300, 900)); exercise 3: a spacing of 20 is clamped to 6.0. No blockers, no majors.

1. **minor** - 29.4 quotes a real trace code from a PR check ("`reader.one / 2026-09-18 17:03:21 · 28QWKS`"). It is a development session, but STYLE 16 says never to write a real session id or token, even expired. A trace code is a 6-character derivative, so the risk is low; still replace it with an obviously fake value (`reader.one / 2026-01-01 12:00:00 · ABC123`) and say "an example in this format".
2. **minor** - 29.5 says "The trace code is the start of the session's admin handle" - true per SessionKeys Javadoc at m6 ("first characters are printed in the watermark as a trace code"); confirm the six-character prefix relationship at m4 (`git show book-m4-reading:.../AuditLogService.java` or the controller that builds the trace) before keeping the claim. Also "adminHandle uses 10 bytes" matches m4/m6 (Chapter 26 correctly shows 9 bytes at m1); add a sentence in 29.5 saying it changed from 9 to 10 bytes so readers comparing Chapters 26 and 29 are not confused.
3. **minor** - Terms used before defined: `replaceUrl`, "deep link" (objective defines by example), "query parameter" (Chapter 12), "localStorage" (only in Further reading), "pure function" (Chapter 19), HMAC handle, "base64 alphabet", "clamp", "middle dot". Add glosses.
4. **minor** - 29.3 says "the time of the last activity is shared across tabs" but does not say how (browser storage event); add one sentence or cross-reference Chapter 22.6. The decision block says "sessions were per tab before"; verify with PO-9 wording ("sessions were per browser tab") - consistent.
5. **minor** - Try it exercises lack the STYLE 12 format and there is no solutions file; exercise 3 needs a running copy at `book-m4-reading` (setup at m4 differs: Spring Boot 3.3.4 / Java 21 per the chapter's own prerequisites, so say to use the tag's JDK 21).
6. **minor** - Prerequisites say "21-23 (Angular components, services and testing)" but testing is Chapter 24 per OUTLINE (23 is routing, guards, forms). Fix to "21-24" or "21-23".
7. **minor** - Figure 29.1 caption bold, should be italic (STYLE 13.1). Analogy (bookmark) has a breakdown - good.
