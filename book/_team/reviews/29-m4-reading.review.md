# Review: 29-m4-reading.md (window 7, expanded text, full prose review)

Verified (arithmetic/dossier/earlier windows): every worked number in the chapter is correct: Table 29.1 (25 minutes gives exactly 300 seconds, `warning`), the Layout example (gap 27, `stepX` 177, `stepY` 49; spacing 3.0 gives 204 and 63), the Crockford walk-through (bytes `FF 00` give `ZW00`, five bits per character, a 10-byte handle gives 16 characters), Exercise 29.3 (page 7, or page 1 with nothing saved), Exercise 29.4 (`stepX` 160, `stepY` 60), clamp of opacity 0.05 to 0.6 and spacing 0.5 to 6; 0.28 to 0.2 opacity, PO-9/PO-10/PO-11, 68 backend and 14 frontend tests, and the Crockford I-versus-l story match the dossier (D5, D9, C5, timeline 60e104a). The chapter agrees with Chapters 23 (deep links, keyboard) and 26 (handle at m1 versus m4). The AI-agent wording is present; the dossier is only in source comments. Listing exactness is covered by the coordinator's script. No blockers.

## Findings

1. **major** - Reader-visible real-looking session material: 29.11 quotes a real trace code from the pull request's live check ("`reader.one / 2026-09-18 17:03:21 · 28QWKS`", and again `28qwks` in the text and `28qw ks` in Exercise 29.6). STYLE 16 says never to write a real session id, token or secret, even an expired one; the trace code is the first six characters of a keyed session handle from a development session. It cannot be turned back into a session id and the session is long gone, but the rule is unconditional. (This was raised in window 3 as a minor; the text is unchanged.) Fix: replace with an obviously fake value in the same alphabet, for example `ABC234`, and keep "a code like this".
2. **major** - Listing caption format: `*File: ...*` is placed before each code block instead of the house `*Path: ...*` after it (see the Chapter 28 review, item 1). Applies to Listings 29.1 to 29.9. Also Listing 29.5 and others are excerpts with elided methods; captions say "excerpt", which is correct.
3. **minor** - Claims I could not verify this window (no `git show`): "the font size scales with the tile (`min(width, height) / 14`, at least 9)" (29.9; the dossier records `tileSize/14`, but "at least 9" and `min(width, height)` need the source) and "four viewer navigation tests ... four idle-timer tests" (29.11 In this project). Ask the writer to confirm against `book-m4-reading`.
4. **minor** - Terms used before defined: "discriminated union" (defined here and in Chapter 19: bold only at the first definition), "signal" (Chapter 22 pointer), "interceptor" (Chapter 22), "decorator" (defined), "normalized" (defined), "keyed hash" (Chapter 17), "distributed-systems thinking" (29.6: fine but jargon), "LIKE pattern/wildcard" (Chapter 9), "clamp" (defined), "manifest" (Chapter 25), "storage event" (explained), "pure function" (Chapter 19).
5. **minor** - The chapter says the session "belonged to one browser tab" (PO-9) and, in 29.5, that activity is now shared "so behavior doesn't depend on which tab". The mechanism section is good, but state precisely that the SERVER session was always shared by cookie; what was per tab was the browser's idle timer (the client-side state). Otherwise a careful reader will ask how a cookie-based session could be per tab.
6. **minor** - Prerequisites say "21-23 (Angular components, services and testing)" but testing is Chapter 24 (23 is routing, guards and forms). Change to "21 to 24" or "21 to 23".
7. **minor** - 29.6 says the interceptor counts as activity every successful API response and the viewer calls `touch()` itself on tile fetches; the "Common mistakes" entry repeats it. Good. Consider a small sequence diagram (browser tab A, tab B, storage event, server) for the multi-tab idea, which is abstract for beginners.
8. **minor** - 29.12: "the implementer misread ... a capital `I` as a lowercase `l`" then "The codes were base64, whose alphabet contains `I`, `l`, `O` and `0`": correct; the chapter could add that base64url also has `-` and `_` (bad for reading aloud). Optional.
9. **minor** - Exercises: format correct; 29.5 requires editing `WatermarkService` on the reader's own copy at m4 (Java 21 tag, needs a build); mark as "optional: needs the milestone running (see Chapter 28 review, item 7)". 29.6 (★★★) is well designed and solvable from 29.11.
10. **minor** - Figure 29.1 caption bold below; use italics per STYLE 13.1.
