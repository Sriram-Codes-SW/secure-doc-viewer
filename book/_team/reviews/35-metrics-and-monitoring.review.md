# Review: 35-metrics-and-monitoring.md (window 7, expanded text, full prose review)

Verified (Grep/dossier): every metric name and kind in Table 35.1 exists in `ViewerMetrics` (counters `sdv.tiles.served`, `sdv.tiles.rate_limited`, `sdv.tiles.busy`, `sdv.render.rejected`, `sdv.render.timed_out`, `sdv.sign_in{outcome}`, timer `sdv.render`, gauge `sdv.render.abandoned_running`); the quoted comments ("Counts only; who did what lives in the audit log", "Registered up front so every outcome is exported as 0 before it first happens") are real; `PAGE_VIEWED` is recorded "once per session, document and page every 10 minutes (not per tile)" and `RATE_LIMITED` "at most once per user per rate-limit window" (both from `AuditEventType` comments; this also settles the Chapter 32 question I could not verify in window 3); the lockout audit detail `rule=<rule>` exists (`AuthController`, `SIGN_IN_LOCKED`); the Actuator settings, the 401 for `/actuator/env`, the 03:30 audit purge and the September 19, 2026 product-owner sign-off agree with Chapters 28, 32, 34 and the README. The sample metrics page is honestly labeled "illustrative, not captured from a run", and the PromQL examples are labeled the book's own. The AI-agent wording for the technical-manager reviewer is present; the dossier is cited only in HTML comments. Listing exactness is covered by the coordinator's script for the captioned listings. A solutions file exists. No secrets. No blockers, no majors. A very good operations chapter: the "rates, not totals", "alert is a decision" and "scenarios" sections are exactly what a beginner lacks.

## Findings

1. **minor** - Header `status: expanded-draft` (see the Chapter 34 review, item 1); use `expanded`.
2. **minor** - Listing format: Listings 35.1 and 35.2 have no `*Path:*` line after the block (the house format and the coordinator's script expect one), and the `SecurityConfig` snippet in 35.9 is an unnumbered, uncaptioned code block. Number it (Listing 35.3), add its tag and path, and note that it is an excerpt.
3. **minor** - Wrong prerequisite pointer: "Chapter 16: the audit trail and the sign-in throttle". Chapter 16 teaches the sign-in throttle, but the audit trail is taught in Chapters 14 (transactions) and 27 (Part IV). Change to "Chapters 16 and 27".
4. **minor** - Terms used before defined: "orchestration systems" and "load balancers" (35.2, 35.4), "loopback" (35.9: "loopback only" needs "the computer talking to itself"), "TCP connection" (35.9), "JVM" (Chapter 3), "classpath" (35.5), "CIDR" (explained by example), "time series" (explained as "points over time"), "on-call" (35.7), "PromQL" (defined), "hundreds of JVM and HTTP metrics" (fine), "heap dump" (35.4: name only).
5. **minor** - Checks the reader cannot make yet: 35.3's `curl -i http://localhost:8081/actuator/health` needs the full stack (Chapter 33 is a prerequisite: good) and `curl` output shows `200` and `{"status":"UP"}`; add the real header list is not needed. Exercise 35.2 (average render time 118.6 / 31 = about 3.8 seconds; 9 failed sign-ins) is solvable from the sample page: good.
6. **minor** - Alert design (35.11) invents thresholds only as "condition ideas" and says to pick thresholds from a week of real traffic: honest and correct. Add one sentence saying the repository contains no alert rules or Prometheus configuration (the book supplies illustrations), so readers do not search for them. (35.10 says the queries are not stored in the repository; extend to alerts.)
7. **minor** - The chapter notes that `rate()` handles counter resets and that a restart resets counters, and lists `sdv_render_abandoned_running` as a gauge: correct. Add that Prometheus's `_max` for the timer is per-scrape-window, which surprises beginners (optional).
8. **minor** - Analogy (car dashboard) has a proper breakdown (the app's numbers are invisible until read). The three-signal framing is consistent with Chapters 32 and 34.
9. **minor** - Pacing: about 4,000 words in the Beginner tier of definitions (35.2 lists 14 terms in one block); consider defining the first six now and the Prometheus terms when 35.6 uses them.
