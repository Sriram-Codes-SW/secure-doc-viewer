# Review: 22-http-client-and-services.md (window 8, expanded text)

Scope and method: I read Sections 22.1 to 22.5 in full, and checked Sections 22.6 to 22.15, Common mistakes, In this project and Try it by search, cross-reference and code comparison (not word by word). Earlier windows read the whole chapter.

Verified against the repository: the interceptor logic (401 and `AUTH_PROBES`, 403 with `passwordChangeRequired`); `provideAppInitializer(() => inject(SessionService).restore())`; the nginx `X-Forwarded-For` overwrite (`proxy_set_header X-Forwarded-For $remote_addr`, with a comment saying OVERWRITE) and the CSP line (`img-src 'self' blob: data:`); fix commit `2d82253` exists ("Fix review findings: X-Forwarded-For spoofing, ...", which matches the "pull request 1" story); the e2e test "a spoofed X-Forwarded-For header cannot reset the sign-in lockout" sends six attempts and expects the sixth to be 429 (consistent with the five-failure limit in Chapter 26); the notice "This document was updated while you were reading" exists in `viewer.component.html`. Renumbering: every "Section 22.x" reference in Chapters 19, 21, 22 and 23 points to the right section (22.5 interceptors, 22.7 throttling, 22.8 session state, 22.10 `switchMap`, 22.12 nginx, 22.13 polling). The earlier reader-facing "project dossier" citation at ~line 280 is GONE (no "dossier" left in the chapter): RESOLVED. The AI-agent wording ("one of the project's AI review agents") is right. Listing captions and Path lines are in the house format. Analogy (printer) has its breakdown. No blockers, no majors.

## Findings

1. **minor** - 22.1: "`list()` returns `Observable<DocumentSummary[]>` (Chapter 19): a promise of a list of summaries." An Observable is not a promise, and Chapter 19 (19.7) takes care to distinguish them. Say "a description of a request that will produce a list of summaries once someone subscribes."
2. **minor** - 22.4, step 4: "It passes through the interceptor (Section 22.5), which adds nothing on the way out". True for reads (Angular's XSRF handling is skipped for GET). Say so in a few words (already implied by the previous sentence) and note that the interceptor is `sessionInterceptor`, so the reader can connect step 4 to Listing 22.5.
3. **minor** - The pool size: Chapter 19 (19.7) says "Chapter 22 explains why a small pool", but this chapter never explains why at most six tiles are fetched at once (the table in "In this project" only says "Tile fetch pool"). Add two sentences in 22.7 (six at a time so that one page cannot spend the reader's rate-limit budget in a burst, and so a page turn can cancel unstarted work).
4. **minor** - 22.5 uses "CSRF" with a definition (Chapter 16) and explains the cookie-to-header pattern well. The sentence "A foreign site can't read the app's cookie, so it can't set the header" is right but a beginner may ask "why can't it?"; add "the browser's same-origin rule stops another site's script from reading it".
5. **minor** - Intermediate tier begins with the italic note "On a first read you can skip to 'In this project'", but 22.5 to 22.11 contains the whole 401, CSRF and throttle story that Part IV chapters point back to. The tier is long (seven sections); consider marking 22.9 to 22.11 as optional.
6. **minor** - 22.7 lists 429 and 410 handling well. The `Retry-After` header is defined in 22.7 but first used in the earlier Chapter 26 discussion of throttles; add a cross-reference to Chapter 8's status code table.
7. **minor** - 22.12: the paragraph after Listing 22.11 packs three ideas (the overwritten `X-Forwarded-For`, the e2e test, and the Content-Security-Policy). The CSP sentence duplicates Section 22.15, which is a whole section on it; drop it from 22.12.
8. **minor** - Terms used before defined: "origin" and "same-origin" (defined in 22.12, used from 22.1; add "same origin: same scheme, host and port" at first use in 22.1), "operator" (defined in 22.3), "debounce" (22.10, defined), "signal" (Chapter 21), "returnUrl" (22.5, not explained: say "the page to come back to after signing in"), "probe" (22.5: `AUTH_PROBES` needs a gloss: requests that only ask whether you are signed in).
9. **minor** - Exercises 22.1 to 22.6 follow the house format with a solutions file (320 words). 22.5 (★★★, "a reload without the version check") depends on the 410 handling described in 22.7 and is solvable from the text; 22.4 (fallback retry time) is solvable from Listing 22.13.
10. **minor** - Pacing: about 5,500 words and 15 sections. The Beginner tier (22.1 to 22.4) is well paced, with a worked example that ties the pieces together. The Advanced tier (22.12 to 22.15) contains material that is really deployment (nginx, CSP) and is repeated in Chapters 33 and 37; keep here only what the browser side needs and point to Chapter 33.
