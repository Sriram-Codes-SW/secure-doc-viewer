# Review: 34-backups-and-operations.md (window 7, expanded text, full prose review)

Verified (Grep/dossier): the backup and restore commands in Listings 34.1 and 34.2 match the project README (README lines 300, 301, 307) character for character; Table 34.2's schedules (janitor 2 minutes then 6 hours, audit purge 03:30, known-device purge 03:45, throttle sweep hourly, `audit-retention-days` 180) match the `@Scheduled` methods confirmed in Chapter 14; the inconsistent-backup scenario and the runbook change (commit bbca423), the leftover version folder incident (F2), the restore drill list (PR #5) and the janitor's "missing current version" guard agree with the dossier (bugs-and-findings F1, F2; reviews.md); the technical-manager reviewer is described as an AI agent; the dossier is cited only in HTML comments; the invented `cron` sketch and the two-clone drill are clearly labeled as the book's suggestions, not project files (good practice). A solutions file exists. No secrets (passwords appear only as `$MYSQL_ROOT_PASSWORD`). No blockers, no majors. A clear, practical chapter with an excellent core teaching point (two stores, one moment).

## Findings

1. **minor** - Header: `status: expanded-draft` is not one of the states used elsewhere (`draft`, `expanded`); change to `expanded` (STYLE 17 expects a status the coordinator's tooling can read).
2. **minor** - Listing captions: Listings 34.1 and 34.2 name the README but have no italic `*Path: `README.md`*` line after the block; Listing 34.3 names `StorageJanitor.java` in the caption but also lacks the path line. The house format (STYLE 11.2) and the coordinator's listing script expect a `*Path:*` line under each block; without it the script cannot check the listings. Also Listing 34.3 contains an authored comment inside the code (`// ...inside removeOrphans, ...`); keep it but mark it as an omission marker in the caption (it is: "the sweep loop ... omitted").
3. **minor** - Consistency with Chapter 10: Chapter 10 taught that a secret on a command line is visible in more places than you expect and showed the `MYSQL_PWD` environment form for the health check. The runbook commands here pass `-p"$MYSQL_ROOT_PASSWORD"` to `mysqldump` and `mysql` inside the container (the expansion happens in the container's shell, so it is not in your terminal history, as 34.5 says, but it is still on the process command line inside the container). Add one sentence acknowledging that this is a trade-off the runbook accepts and that `MYSQL_PWD` would keep it off the argument list.
4. **minor** - Terms used before defined: "InnoDB" (34.5), "stored procedures and functions" (the `--routines` flag), "alpine" (a very small Linux image; used in Listing 34.1), "tar", "gzip" (the flag letters are explained: good, but define the tools once), "snapshot" (34.11: "a storage layer that supports snapshots"), "pseudo-terminal" (explained), "cron expression" (Chapter 14), "health check" (Chapter 10), "off-site" (defined), "scratch" (used as "scratch environment": define once).
5. **minor** - Windows readers: the commands use `"$PWD"` and single-quoted `sh -c` strings; they work in Git Bash and macOS/Linux terminals but not in PowerShell. Say "run these in Git Bash (or a Linux shell); PowerShell needs different quoting", consistent with Chapter 2.
6. **minor** - The whiteboard analogy has two breakdowns: good. In 34.3 the table says the secrets are "in the `.env` file"; add that the `.env` is also the most sensitive item to back up, and that backing it up with the data it protects defeats part of the point of keeping secrets apart (advice: keep it in a separate, more restricted place; the chapter's mistakes list says "stored as securely as the backup itself": expand by one sentence).
7. **minor** - 34.7 lists the drill "according to PR #5". Good. Add the caution that the drill also proves Docker volume names and ports were changed for the scratch project (the chapter does this in the last paragraph; move that paragraph up as step 0).
8. **minor** - Exercise 34.6 (★★★) asks for "the two things that would have to change ... to make a consistent online backup possible"; the answer depends on Chapter 37's storage discussion. Make sure the solution names them (database snapshot plus versioned/immutable tile storage such as object versioning) and says it is a design outline.
9. **minor** - 34.12 references "the idle-session warning (section 22.6)"; confirm that section number in the expanded Chapter 22 (not re-read in this window).
