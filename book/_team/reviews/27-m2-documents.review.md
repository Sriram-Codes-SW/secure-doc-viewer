# Review: 27-m2-documents.md (window 8, expanded text, full prose review)

The writer's question (c) is answered: the note in 27.6 about the later picker change is CORRECT. Research answer V1 in `DOSSIER.md` says exactly this, and the code confirms it: `book-m2-documents` `UserDirectoryController.search` has no minimum length and no role filter (it lists enabled users by prefix, excludes only the caller), while `book-m6-final` returns an empty list for a query shorter than 2 or longer than 32 characters and filters out `ADMIN` accounts ("Nothing for one character, and never admins (they can already see every document)"). Two precision points (items 1 and 2) follow.

Other verification (`git show book-m2-documents`, dossier, arithmetic): the sharing rules and messages (400 "No user named '<username>'.", "The owner always has access.", silent unshare, normalization, audit detail "with"/"from", after commit) match V1; `FileOperations` retries 8 times with `50L << Math.min(attempt, 4)` (waits 50, 100, 200, 400, then 800 ms, about 3.95 seconds in the worst case, so the chapter's remark that the Javadoc's "about two seconds" is approximate is right); `StorageJanitor` constants and schedule (`PT2M`, `PT6H`, `MIN_AGE` one hour); `AuditLogService.record` uses `REQUIRES_NEW`; the CSV guard; 56 backend and 6 frontend tests (PR #2); PO-4, PO-5, PO-6, PO-8, PO-12, PO-13, TM-7, TM-8, TM-9 and decision D15 ("Go ahead with Phase 2, users plus everyone"); the C3 audit-rollback bug and the C4 OneDrive incident agree with the dossier. Listing captions follow the house format; the AI-agent wording is present; the dossier is cited only in HTML comments. Earlier minors (audit purge property, terms, format) are largely RESOLVED. No secrets. No blockers, no majors.

## Findings

1. **minor** - Precision about the m2 picker (27.6): "At this milestone the picker had no minimum query length, so a single character already listed names". At `book-m2-documents` an EMPTY query also works: `q` defaults to `""`, the prefix is empty, and every enabled username matches, so up to 20 usernames are listed with no typing at all. Reword: "even an empty query listed up to 20 usernames".
2. **minor** - Stale internal reference in 27.5: "which is why the integration test in Section 27.11 exercises both paths". The integration test is in Section 27.14; 27.11 is the OneDrive/file-locks section. Fix the number.
3. **minor** - Listing 27.9 and Listing 27.10 contain bare `{ ... }` and `...` placeholders inside Java code (`moveDirectory(...) throws IOException { ... }`, `public void sweep() { ... }`, a trailing `...` in `removeOrphans`). The captions say "simplified", but bare dots are not valid code and are not the house omission marker; use `// ...` inside the braces. (Chapter 33's earlier point, applied here.)
4. **minor** - Chapter 2 (Listing 2.3) quotes the same `FileOperations` Javadoc ("retries for about two seconds") without the caveat that 27.11 adds (the real worst case is about four seconds). Add "(the comment says about two seconds; the code below actually allows a little under four)" in Chapter 2 or a cross-reference so the two chapters do not disagree.
5. **minor** - Terms used before defined: "ring buffer" (27.1: explained by "old events were dropped as new ones arrived": add the term), "Actor" and "Subject" (Listings 27.5 and 27.7 use them without explanation; one clause: "who did it, and what it was done to"), "`Viewer`" (explained after the listing), "jdbc"/"JdbcTemplate" (explained), "propagation" (explained), "trust boundary" (27.9; Chapter 8 defined it), "ISO 8601" (27.12: name only), "orphaned" (27.1 and 27.12: define "left behind with nothing pointing to it"), "regression" (not used).
6. **minor** - Exercise 27.6 says "On your own copy at `book-m2-documents`, sign in as a reader ... have the owner unshare it": this needs the milestone running with Java 21, Maven, MySQL and two accounts, none of which the book has set up (same major gap raised in the Chapter 25 and 26 reviews: add one "how to run an older milestone" box). It also asks the reader to explain a design change; the final sentence is a good ★★★ stretch but needs a hint ("think about what the browser already holds").
7. **minor** - Pacing: about 6,500 words; the Intermediate tier alone runs from access queries through sharing, the audit trail, transactions and CSV export. All of it is teachable, but 27.9 (CSV export) could move to the Advanced tier without loss, and 27.13 (the Manage page) is a paragraph of UI description with no listing: either shorten it or point to a screenshot.
8. **minor** - Analogy (locked drawer with an unreadable label) has its breakdown; the audit camera analogy (27.8) also has a good breakdown. Table 27.1's "(n/a)" cell could be explained ("a private document cannot be shared with someone who is not in the table row").
9. **minor** - Exercises 27.1 to 27.6 are in the house format and a solutions file exists (not re-read). Exercise 27.5's answers follow from Listing 27.8: `csv("=1+1")` returns `"'=1+1"` (quoted, with a leading apostrophe) and `csv("Report, final")` returns `"Report, final"` (quoted, comma kept in one cell).
