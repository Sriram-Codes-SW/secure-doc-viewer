# Review: 27-m2-documents.md (window 3)

Verified at book-m2-documents by comparing every non-comment line of Listings 27.1 to 27.4 with the real files: no line is missing or invented (27.1 `Visibility.java`, 27.2 first part of `V2__documents_shares_audit.sql`, 27.3 `DocumentRepository.findTitleIfVisible`, 27.4 `AuditLogService.record`). Facts checked: `audit-retention-days: 180`; `TILE_VIEWED` audited per tile in `TileController`; `REQUIRES_NEW` on `AuditLogService`; `StorageJanitor` and `FileOperations` are in `service/`; `UserDirectoryController`, `DocumentAccessIntegrationTest`, `StorageJanitorTest` exist; the ROWS-reserved-word comment is in V2; Boot 3.3.4 / Java 21 correct for m2. History claims match dossier (D15 quote, C3 transaction bug, C4 OneDrive locks, PR #2, findings PO-4..PO-13, TM-7..TM-9). Reviewers are described as AI review agents (good). Tiers and structure follow STYLE 7. No secrets. No blockers, no majors. Strong chapter; issues are mostly polish.

1. **minor** - Reader-facing text cites the "product owner's own words" and "the implementer observed" - fine, but 27.8 names a personal local path (`C:\dev\secure-doc-viewer`) and OneDrive on the product owner's machine. Not a secret; still unnecessary. Suggest "moved the project to a folder outside OneDrive".
2. **minor** - Listing 27.2 caption says "first 40 lines; the audit table follows in the file". Confirm the line count (the excerpt shown appears shorter than 40 lines after comments were trimmed) or say "first two tables". Same for the caption of 27.1 "comments shortened": the real file has a one-line Javadoc on the enum plus per-constant Javadocs; say "class Javadoc omitted".
3. **minor** - Terms used before or without definition: `Optional` (Chapter 5?), `JPQL` query language, "transaction" and `REQUIRES_NEW` (Chapter 14 teaches transactions; forward pointer needed at 27.6 and in the incident), "ring buffer", "join table" (defined), "CSV with spreadsheet formulas neutralized" (explain: cells starting with `=` can run formulas), "orphaned", "staging folder", "janitor" (defined by usage). Add one-clause glosses.
3b. **minor** - 27.6 says a retention purge removes events older than "a default of 180 days" - matches config; add the property name (`audit-retention-days`).
4. **minor** - Statement "Chapter 30's 'access lost' screen handles it" and "flooding problem in Chapter 30": confirm Chapter 30 (or Chapter 29) actually covers both (the PAGE_VIEWED throttle came at PR #5 per Ch 32; the access-lost screen may be Chapter 29). Cross-check when 29 and 30 are reviewed.
5. **minor** - Try it: no `### Exercise` format and no solutions file. Exercise 3 (★★★) requires two accounts, an owner and a reader, on a local copy: give setup steps, and say never to reuse real passwords (use placeholder accounts such as `reader.one`).
6. **minor** - Figure 27.1 caption is bold, should be italic (STYLE 13.1); Mermaid diagram omits `SecurityErrorResponses` etc. (fine) but shows `SEC --> DC` chained edges that read as one line; cosmetic. Table 27.1 is referenced only after it appears.
7. **minor** - 27.4: "PR #2 records a test for it" (unsharing cuts off open pages) - name the test class if it is `DocumentAccessIntegrationTest`, and check it actually contains that scenario before naming it (not verified).
8. **minor** - Analogy (locked drawer with unreadable label) has its breakdown, but it appears to argue the opposite: a clerk who says "no such drawer" is lying to protect existence; add one sentence saying that is the intended effect, and its cost (Decisions section covers cost, good).
