# Review: 17-files-images-pdfs-signatures.md (window 6, full prose pass)

Listing check (script, window 5): all five listings match book-m6-final / book-m0-mvp as labeled (the authored `// ... ...` markers inside listings are clearly marked omissions). Arithmetic checked: A4 at 150 DPI is about 1,240 x 1,754; `(1240 + 511) / 512 = 3`; 120-second TTL, `max-concurrent-renders: 2`, `render-timeout` 3 minutes, opacity clamp 0.05 to 0.6 and spacing 0.5 to 6.0 all agree with earlier verified facts (Chapters 25, 29, 32). The version claims (PDFBox 3.0.8) are right for m6. No secrets. No blockers, no majors. Bash was unavailable this window, so I did not re-check `WAIT_MILLIS` (2,000 ms) or the test name `concurrentReplacementsAreSerialisedByTheRowLock` against the source.

1. **minor** - Section 17.6 is titled "Threads, pools and limits" but neither "thread" nor "pool" is defined; only the semaphore is. Define a thread ("one line of work a program runs alongside others") in one sentence, or retitle "Limits on concurrent work".
2. **minor** - Section 17.7 is titled "staging, atomic move, cleanup" but "atomic" is never explained or shown (the text describes staging and commit). Add one sentence ("an atomic move either happens completely or not at all; `FileOperations` uses `ATOMIC_MOVE`") or drop the word.
3. **minor** - Terms used before defined: "heap" (in the quoted `loadPdf` comment), "CPU" and "thread" (17.6), "canonical string" (17.5 uses it before defining: "the fields written in a fixed order as one text"), "base64url" (glossed inline: good), "constant time" (explained), "hash" (Objective 3 promises to explain what a hash is; only Chapter 15 defined it; add a one-line reminder before HMAC), "trace code" (Chapter 29 explains; a pointer helps), "canvas"-free (good).
4. **minor** - 17.6: "(Chapter 16's cousin, `TileRateLimiter`)" is an odd cross-reference; `TileRateLimiter` is introduced in Chapter 26 and Chapter 12 territory. Write "the per-user rate limit (`TileRateLimiter`, Chapter 26)".
5. **minor** - The chapter has no analogy anywhere; for a beginner, the signed token could reuse the STYLE 8.1 established analogy (concert wristband with a hologram) with its breakdown. Add it in 17.5 (Chapter 25 already uses it).
6. **minor** - Unnumbered inline references to real code in 17.2 (`loadPdf` comment quote) and 17.7 (the upload sequence in `DocumentService`) carry no listing. Fine for prose, but the upload sequence is important enough for a short numbered listing.
7. **minor** - Exercises: inline `(★)` format instead of `### Exercise 17.N ★ Title` (STYLE 12); Exercise 1 answers 2 tiles (1,000 / 512 rounds up to 2) while the text example uses 1,240 (3 tiles): fine, but say which DPI/paper size gives 1,000 to avoid confusion. Exercise 5 is a good synthesis (session binding and rate limit or access check). Solutions file not checked.
8. **minor** - Prerequisites list Chapters 5, 11, 12 only; the chapter relies on Chapter 3 (integer division, cited), Chapter 13 (`BadRequestException` handler) and Chapter 15 (hash concept). Add them.
