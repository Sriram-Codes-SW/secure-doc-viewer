# Review: 17-files-images-pdfs-signatures.md (window 9, expanded text, full read; replaces the window-6 pass)

Method: full read; `listingcheck.py 17` (all listings verified); arithmetic checked: A4 at 150 DPI is 1,240 x 1,754 (about 2,174,960 pixels, 8.7 MB at 4 bytes; 40,000,000 pixels is 160 MB, both right); Table 17.2 (3 columns, 4 rows, last column 216, last row 218, 12 tiles) is right; a US letter page (Exercise 17.1) is 1,275 x 1,650 giving 3 columns (last 251 px) and 4 rows, also 12 tiles, matching the `application.yml` comment "~12 tiles"; `(1240 + 511) / 512 = 3`. Code claims at `book-m6-final`: `render-dpi: 150`, `tile-size: 512`, `url-ttl-seconds: 120`, `max-concurrent-renders: 2`, render queue 30 s, render timeout 3 minutes, `TileWorkLimiter` (`WAIT_MILLIS = 2_000`, fair semaphore, `0` means twice the CPU count), watermark opacity clamped 0.05 to 0.6 and spacing 0.5 to 6.0, `-Math.PI / 6` rotation, `adjacentCopiesNeverOverprintEachOther` exists in `WatermarkServiceTest`, `SignedTilePayload` canonical string of seven fields, HMAC and constant-time compare, `commit` refuses an existing folder. Figures 17.1 to 17.3 are true of the code (17.3 is the same order as Listing 32.3 plus the work limit); their source comments name the tag. The wristband analogy has its breakdown; history (G3 render timeout, TM-4 session id in token, A1 watermark overprint) matches the dossier. The AI-agent wording is right; dossier only in comments. Examples use placeholders. No secrets. No blockers, no majors.

## Findings

1. **minor** - 17.7's incident says `FileOperations` retries "for about two seconds". The code (`FileOperations`, 8 attempts, sleeps 50, 100, 200, 400 then 800 ms four times) waits about 3.95 seconds in the worst case; the Javadoc says "about two seconds". Chapters 2 and 27 quote the same Javadoc. Say "for a few seconds (the comment says about two; the code allows a little under four)" here and in Chapters 2 and 27 so the three agree.
2. **minor** - 17.8 quotes the `TileController` class comment "Passing all five still only gets you ONE tile...". Table 17.4 lists nine checks and Figure 17.3 seven, so "all five" reads as a mistake. Add "(the comment was written when there were five; the later ones are in the table)".
3. **minor** - 17.6 says the per-user rate limiter is in "Chapter 26"; the rate limiter first appears at `book-m1-accounts` (Chapter 26) but the 180/512 change is in Chapters 29/30, and Chapter 38.9 explains its algorithm. Fine; add "and Section 38.9" for the reader who wants the algorithm.
4. **minor** - Terms used before defined: "raster" and "rasterizing" (defined in 17.2), "thread" and "pool" (defined in 17.6; "pool" is used in the heading of 17.6 and defined only in the second paragraph: fine), "heap" (defined), "constant time" (explained), "base64url" (defined), "atomic" (explained by "either happens completely or not at all"), "canonical" (defined by use), "staging" (defined in bold at Listing 17.8's discussion).
5. **minor** - Exercise 17.3 asks the reader to send a tampered token with `curl` while signed in; that needs the running app and a session cookie (Chapter 16's Example 16.1 shows the header shape). Add a pointer, and note that a signed-in session is needed to reach the signature check at all (Spring Security answers 401 first otherwise, with a different message).
6. **minor** - Exercise 17.5's final question ("which single check would refuse it even if the friend also somehow signed in as the same user") has one clear answer (the session binding: the friend's session is a different session); good, solvable from Table 17.3.
7. **minor** - Pacing: the longest chapter in Part II (about 6,250 words), with three genuinely different topics (images, signatures, bounded work and files). 17.6 and 17.7 (threads and files) could be their own short chapter, but the Intermediate-tier note ("Section 17.5 is the most important one here") lets a first-time reader skip them.
