# Review: 26-m1-accounts.md (window 3)

Verified at book-m1-accounts: Listing 26.1 (V1 migration) is identical to the file. Listing 26.2 matches the `securityFilterChain` method (Javadoc removed as labeled). Listing 26.3 matches `SessionKeys.java` (tileBinding 16 bytes, adminHandle 9 bytes, base64url). Quotes from the SecurityConfig Javadoc exist. `CsrfCookieFlowTest`, its method name and `@DirtiesContext` exist. Versions correct (Boot 3.3.4, Java 21, MySQL 8.4, Angular 22, H2 still in pom). compose binds `127.0.0.1`. `UserAdminController` revokes sessions on role/password change. Angular dev proxy exists. Dossier supports: 13 PO / 20 TM findings, 45 backend / 4 frontend tests, D1, D2, D3, D7, C1, C2, TM-1/2/3/4/15. Reviewers are correctly described as AI agents (fixes the Ch 32 concern in Ch 26). No secrets. No blockers. Overall strong.

1. **major** - Reader-facing text cites the internal "dossier" as an authority in several places ("The dossier records that the reviewers suggested...", "The dossier records the outcome ... but no recorded debate about JSON Web Tokens", "which the dossier records took several steps"). The reader has no dossier. Fix: name the real source ("the pull request description", "the project's commit history") or say "the project's record"; keep the dossier reference only in `<!-- source -->` comments.
2. **major** - Terms used before defined for a beginner: httpOnly (in objectives and 26.2), SameSite=Strict (26.3), filter chain, SPA, "Vite-style development proxy", enum, base64, HMAC (taught in Ch 25 but tile "binding" needs a recap), OIDC/SSO (26.1), JSON Web Token, Docker Desktop, "double-submit pattern", "session fixation" (explained only after use). Fix: define httpOnly and SameSite where the cookie is introduced (26.2 vocabulary list), spell out OIDC/SSO on first use, one sentence on filter chain; forward pointers to Chapters 15-16 for the rest.
3. **minor** - Coat-check analogy: breakdown sentence is garbled ("a stolen ticket is worse than a stolen coat ticket"). Fix: "a stolen coat-check ticket gets one coat back; a stolen session cookie lets someone act as you until the session ends".
4. **minor** - Try it: no solutions file (`26-m1-accounts.solutions.md`) and exercises are not in STYLE 12 format (`### Exercise 26.N ★ ...`). Exercise 3 requires signing in at m1, but the reader needs a bootstrap admin password (printed in the startup log if unset) and to run the app at that tag; add setup steps and a warning not to copy real secrets. Exercise 1 answer: 403 with a JSON error body (verify via `SecurityErrorResponses` before writing the solution).
5. **minor** - 26.7 "A Vite-style development proxy makes the SPA and the API share one origin" - it is Angular's `proxyConfig: proxy.conf.json` (angular.json line 58); say "Angular's development-server proxy".
6. **minor** - 26.8 and the MySQL decision include personal setup details (WSL update, BIOS virtualization on the product owner's machine). Not needed by the reader and slightly personal; trim to "installing Docker Desktop took several steps on the product owner's Windows machine" or drop.
7. **minor** - Table 26.1 lists rules but omits `/error` (permitAll) that appears in Listing 26.2; add a row or a note.
8. **minor** - 26.5 states `TileRateLimiter` limits "per user rather than per session"; check TileRateLimiter at m1 keys on username (I did not verify). Also `LoginThrottle` counters: "per account and address and per address" - confirm against the m1 class before publishing.
9. **minor** - 26.6 "the audit log is still a small in-memory list" - consistent with dossier TM-9 (500-entry ring), but add the number or source.
10. **minor** - Listing 26.3 caption/path style matches STYLE 11.2; Figure 26.1 caption is bold and placed below (STYLE says italic); the Mermaid block is not introduced before it ("Figure 26.1 is ..." appears before, good).
