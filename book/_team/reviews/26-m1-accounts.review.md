# Review: 26-m1-accounts.md (window 8, expanded text, full prose review)

The writer's specific question (a) is answered: Exercise 26.5 is CORRECT at `book-m1-accounts`. `git show book-m1-accounts:.../TileController.java` shows the order: `verifyAndDecode` (signature and expiry), then the session check `sessionKeys.tileBindingMatches(session.getId(), payload.sessionBinding())` with the comment "the request must come from the very session the token was issued to ... a URL copied into another browser or account fails here", then `tileRateLimiter.recordAndEnforce(username)`, then load, watermark and audit. Bob (a different session) is therefore refused by the binding check, exactly as the chapter and the solution say. One nuance to add (item 3): at m1 a logged-out or revoked session never reaches the controller, because Spring Security rejects it first.

Other verification (`git show book-m1-accounts`, dossier, arithmetic): 13 `@Test` methods in `SecurityIntegrationTest` (the chapter says 13); `TileRateLimiter` is keyed by username with the quoted comment; `tile-rate-limit-per-window: 120` per 60 seconds; `LoginThrottle` limits 5 per (username, address) and 20 per address in a 15-minute window, with the quoted class comment ("so one attacker can't lock a real user out from everywhere", "spraying many usernames from one address", "only ever errs on the side of letting a request through"); the `AuthController` Javadoc ("a password change locks out anyone who had it"); `SessionKeys` (16-byte tile binding, 9-byte admin handle); PO/TM finding numbers (PO-1 everyone had admin, PO-2, PO-3, TM-1 to TM-4, TM-6, TM-15) and decisions D1 to D3 match the dossier; Exercise 26.4's answer (refused, `Retry-After` about 40 seconds) follows from Listing 26.10; solutions for 26.5 and 26.6 are correct. Listing captions follow the house format. The AI-agent wording is present; the dossier is cited only in HTML comments; the test password is a placeholder (`<test password>`). Earlier majors (reader-facing "dossier", undefined httpOnly, SameSite and OIDC/SSO) are all RESOLVED. No secrets. No blockers.

## Findings

1. **major** - Same practical gap as Chapter 25 (item 1 there): the chapter tells the reader to "start the app" at `book-m1-accounts` (Exercise 26.6, and 26.13 "the app itself still runs from your editor"), but there is no Maven wrapper before `book-m5-platform`, the tag targets Java 21 and Spring Boot 3.3.4 while the setup guide installs JDK 25 and no Maven, and the app also needs the MySQL container and a `.env` (Chapter 10) at this tag. A "How to run an older milestone" box is needed once (Part IV introduction) and referenced here: JDK 21, Maven 3.9, `docker compose up -d` for MySQL, `.env` with `DB_PASSWORD`, `DB_ROOT_PASSWORD`, `SIGNING_SECRET`, then `mvn spring-boot:run` and `npm start` in `frontend/`.
2. **minor** - 26.13: the paragraph quotes "MySQL health check no longer puts the root password on the command line" as something "Chapter 30's final-review rounds record". That sentence is a dossier note (G7), not text in Chapter 30. Either quote Chapter 10's own explanation of the `MYSQL_PWD` form (Section 10.5) or paraphrase without quotation marks.
3. **minor** - 26.10 and the "Tile endpoint" step in 26.5/26.10 say the token binding is checked "from the caller's own session id". Add one sentence that at this tag a signed-out or revoked session never reaches `TileController`, because Spring Security rejects it earlier (the controller comment says so); otherwise 25.6's statement "`TileController` performs the second check" (a session-liveness check at m0) reads as if the same code still runs.
4. **minor** - Terms used before defined: "BREACH" (26.7: "a subtle attack called BREACH"; either define in one clause, "an attack that infers secrets from the size of compressed responses", or drop the name), "ApplicationRunner" (26.4), "SecureRandom" (explained), "principal" (Decisions), "seeded first admin" (26.1: "seeded" needs a gloss: created automatically at first start), "SPA" (26.7), "Actor"-free, "interceptor" (26.9, Chapter 22 pointer), "signal" (26.9, Chapter 21/22 pointer), "lazy loading" (explained), "Docker Desktop" and "volume" (explained in 26.13), "sliding window" (defined).
5. **minor** - Pacing: the chapter is about 6,500 words with 15 sections; the Advanced tier (26.10 to 26.14) holds token binding, both throttles, admin safeguards, Docker and a test story. Strong material, but consider moving 26.13 (Docker and Compose) to the Part IV introduction or Chapter 27, since Compose is the least related to accounts.
6. **minor** - Listing 26.13 contains a bare `...` inside test code (an invalid Java line); the caption says "simplified" but not that a `...` marks omitted assertions. Use `// ...` and say so in the caption.
7. **minor** - Table 26.1 (roles) is labeled "at book-m1-accounts" but the roles descriptions (upload restricted to publishers) are the same at m6; fine. Table 26.2 includes `/error` (window-3 minor RESOLVED).
8. **minor** - The chapter mentions `PO-1` to `PO-9` and `TM-1` to `TM-15` in code font throughout. They are explained once in 26.1; consider a small table (Table 26.0) mapping each ID to a plain-words description so a reader who jumps into the chapter can decode them.
9. **minor** - Exercises 26.1 to 26.6 are in the right format with a solutions file that matches; 26.6 correctly tells the reader to use a throwaway password and never a real one. 26.3 is a good ★★ drill (`Alice` normalizes to `alice`; `a` and `Carol!` are rejected by the pattern).
