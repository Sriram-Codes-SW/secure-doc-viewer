# Review: 21-angular-components.md (window 8, expanded text)

Scope and method: I read the header, objectives, Sections 21.1 and 21.5 to 21.11 (Intermediate and Advanced tiers), Common mistakes, In this project, Try it and Summary in full; Sections 21.2 to 21.4 and 21.7's token listing I checked by search and code comparison rather than line by line.

Verified against `book-m6-final` and other tags: the contrast figures (`#6b7280` on white = 4.83:1, `#5d6470` on white = 5.96:1, computed here); `--muted`, `--surface`, `--on-accent` values in `styles.css` (light `#ffffff` on accent, dark `#0b1020`); `track doc.documentId` in `document-list.component.html`; the viewer exists at `book-m1-accounts` and draws tiles as `div`s with `background-image` (so "from its first appearance at `book-m1-accounts`, uses divs" is right); the `viewer.component.css` comment; Chapter 20's Listing 20.3 is `index.html` (the pointer in 21.1 is right); Table 21.1 exists. Section references to Chapter 19 (19.3) are right after the renumbering. Analogies (badge holder) have a breakdown paragraph. Header status `expanded`; no dossier or agent citations; no secrets. No blockers, no majors.

## Findings

1. **minor** - Listing 21.6a (line ~278): the number is an editorial patch, and this listing has no `*Path: ...*` line after the block (all others do). The coordinator's listing script will not recognise it. Renumber (all later listings shift by one, so either renumber 21.7 to 21.11 or fold this into an "Example 21.2" with no tag) and add `*Path: `frontend/src/app/core/session.service.ts`*`.
2. **minor** - The "> **Note:**" after Listing 21.6a repeats what the paragraph above it already said (signals expose who is signed in). Delete it or replace it with the more useful point that `app.html` can read `sessionService.isAdmin()` without being able to change it.
3. **minor** - 21.9: the sentence "The milestone-zero prototype drew the page on a `<canvas>` element (`book-m0-mvp`'s static page; Chapter 25)" is compressed and a beginner cannot parse "static page". Say "the very first prototype (Chapter 25) drew the page on a `<canvas>`, a rectangle you draw pixels onto with code".
4. **minor** - 21.9 (Listing 21.10) presents a comment, "the exact client-side technique discussed for why flipbook readers don't offer 'Save Image As'", that refers to a discussion the reader never saw. Add half a sentence ("Chapter 25 describes the flipbook-style readers the project studied first") or trim the quote.
5. **minor** - 21.9's closing paragraph names "Chapters 16 and 17" for the real protection (signed URLs, watermark, rate limit); the signed URLs are in Chapter 17 and Chapter 25, the rate limit in Chapter 26 and Chapter 28. Point to "Chapters 17 and 26".
6. **minor** - Terms used before defined: "pipe" (defined in parentheses in 21.1; make it bold at the definition), "directive", "decorator" (defined), "change detection" (defined), "lazy" (explained), "custom properties" and "design tokens" (21.7, defined at first use; keep), "AA threshold" and "WCAG" (21.7: one clause on WCAG 2.1 AA meaning "at least 4.5 to 1 for normal text"), "anti-aliased" (21.10: define as "smoothed by blending edge pixels"), "absolutely positioned" (21.9: one clause).
7. **minor** - Exercise 21.4 sends the reader to "Table 21.1" to compare, which gives the answer away; say "Check your numbers with Table 21.1 afterwards", or move the table to the solution. Exercise 21.2 needs `ng serve` running, which Chapter 20 taught, but at `book-m6-final` the reader also needs the backend for anything past the sign-in screen; add "(the sign-in screen is enough to see the change)".
8. **minor** - Exercise 21.5 (why track by id): solvable from Common mistakes, good. Exercise 21.6 is a good hands-on drill but overlaps Example 21.1; fine.
9. **minor** - Pacing: about 5,000 words. The Beginner tier is right-sized. 21.7 (themes and contrast) is a full design-systems lesson in the Intermediate tier; the arithmetic (4.8:1 to 5.96:1) adds value but the sentence "the old value scored 4.8 : 1 on white and only 4.5 : 1 on the page background" contains a figure (4.5) that I could not reproduce exactly; the old value on `--bg` `#f6f7f9` computes to about 4.5:1 (borderline), so the wording holds; please confirm Table 21.1 states the same value.
10. **minor** - Tiers: "On a first read you can skip to 'In this project'" appears above both the Intermediate and Advanced tiers; the Advanced tier's 21.8 is one paragraph ("Tiles, briefly") that could simply be the introduction to 21.9.
