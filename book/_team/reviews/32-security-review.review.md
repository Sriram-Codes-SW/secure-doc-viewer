# Review: 32-security-review.md (window 8, expanded text, full read; replaces the window-2 review, whose majors are RESOLVED)

Verified against `book-m5-platform` / `book-m6-final`, the dossier and arithmetic: `url-ttl-seconds: 120`; `tile-rate-limit-per-window: 180` per 60 s; `max-pages: 500`; `max-page-pixels: 40000000`; `max-concurrent-renders: 2`; `render-timeout: 3m`; upload limit 50 MB; `LoginThrottle` (5 per account and address, 20 per address, 20 account-wide for unrecognised devices, 15-minute window); the CSRF cookie is `SameSite=Strict` with `withHttpOnlyFalse()`; `SessionKeys.tileBinding` (16 bytes) and `MessageDigest.isEqual`; the gate order in Listing 32.3 matches m5/m6; commit ids `2d10e07`, `2d82253`, `82c24b6`, `1ce2c8b`, `f682716`, `782ab6b`, `6cf17fa`, `68b4945` all exist and match the dossier (reviewed at `2d10e07`, fixed in `2d82253`; TM-1/PO-2 takeover fixed in `68b4945`); the 18 of 18 live checks (with the honest note that the Caddy check was not one of them); `sdv_tiles_rate_limited_total` exists (`MetricsEndpointTest`); the ACCESS_DENIED cap (5 seconds per user) matches DOSSIER; 500 pages at 180 tiles a minute (12 tiles a page) gives the 33-minute figure ("about half an hour").

Earlier majors: the Listing 32.2 label now names every omission (RESOLVED); the AI-agent wording in 32.4 is exactly right (RESOLVED); the dossier is cited only in HTML comments. The lock-picker analogy has its breakdown sentence. No secrets. No blockers, no majors.

## Findings

1. **minor** - Header still says `status: expanded-draft`; change to `expanded`.
2. **minor** - Wrong cross-reference in 32.13 question 2: "`verifyAndDecode` (sections 17.5 and 25.3)". Section 25.3 is "Tile grid math"; `verifyAndDecode` is explained in Section 25.6. Change to "sections 17.5 and 25.6".
3. **minor** - 32.2 "Terms you need": only Asset, Actor, Entry point and Control are bold; Trust boundary, Threat, CVE and BCrypt are plain text with a chapter pointer. Bold them all, or say "Terms from earlier chapters" in a second list. The CVE example "CVE-2026-1234" is invented and should say so, and "Section 32.5 mentions three that hit a web server library" is not true as written (32.5 says "three published CVEs" without naming them); say "mentions three of them".
4. **minor** - Inconsistent pointers for the same term: CSRF is defined "(Chapter 8)" in 32.10, but CSRF is taught in Chapter 16 (and shown in 22.5). Trust boundary "(Chapter 23)" is right (23.4). Section 32.5's "reverse proxy (Chapter 16)" should point to Chapters 22 (22.12) and 33.
5. **minor** - 32.5 Round 3: the "recognised-device rule" is not defined for the beginner. One sentence: "the app remembers, for 30 days, the addresses an account has signed in from successfully".
6. **minor** - 32.5 "Later rounds": eight items in one sentence. A beginner cannot parse it; make it a bullet list, or say "Chapter 30 and Section 32.14 tell the ones that matter".
7. **minor** - 32.6 quotes `172.28.0.10` and `172.28.0.11`. Say "private addresses on the compose network" so a beginner does not read them as public.
8. **minor** - 32.10 item 2 is grammatically broken: "The session cookie is the httpOnly (Chapter 26) one, meaning JavaScript cannot read it at all." Reword: "The session cookie is `HttpOnly` (Chapter 26), so JavaScript cannot read it at all."
9. **minor** - Listing 32.2 mentions `permissionsPolicyHeader(...)` with no explanation in the text; add one sentence or drop it from the excerpt.
10. **minor** - 32.13's Listing 32.3 is from `book-m6-final` while 32.10 and the In this project table use `book-m5-platform`. State once why (the m6 excerpt matches m5 for the checks shown, so either tag is fine) or use one tag.
11. **minor** - Pacing: 32.13 and 32.14 (about 1,500 words) retell material from Chapters 26, 28 and 30 (TM-1, TM-4, TM3-1). They are good stories and the lessons are the chapter's payoff, but each *Lesson* line is repeated in the last Summary bullet; trim the Summary bullet.
12. **minor** - Exercise 32.4 (★★★) asks for "one mitigation that isn't in the app": open-ended; the solutions file should say "one good answer". Exercise 32.2 needs the m5 build: say "`./mvnw test`, as in Chapter 6". I did not re-derive the solutions file this window.
