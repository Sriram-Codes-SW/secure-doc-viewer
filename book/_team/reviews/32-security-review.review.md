# Review: 32-security-review.md (window 2)

Verified at book-m5-platform: Listing 32.1 matches SessionKeys.java lines 37-45 exactly. Listing 32.2 matches the `http` chain in SecurityConfig.java for the lines shown, but omits more than its label says (finding 1). Config claims are correct: url-ttl 120, rate limit 180 per 60 s, max-pages 500, max-page-pixels 40M, max-concurrent-renders 2, render-timeout 3m, max-file-size 50MB, TRUSTED_PROXY_REGEX 172.28.0.10, Caddy 172.28.0.11, CSRF cookie `withHttpOnlyFalse` with `SameSite=Strict`, `Referrer-Policy: no-referrer`, Crockford Base32, 503 with Retry-After, the product-owner decision dated 2026-09-19 (README). History (TM2-1, TM2-3, TM3-1, races, 72-byte BCrypt, Tomcat 11.0.24 to 11.0.26, 18/18 checks, "Expected 429, Received 401") is supported by dossier/bugs-and-findings.md and decisions.md. No secrets. No blockers.

1. **major** - Listing 32.2 label under-reports omissions. It says "imports, the session-lifetime filters, and the bean methods ... left out", but the real chain also contains `.securityContext(...)`, `.exceptionHandling(...)`, two `addFilter*` calls (SessionLifetimeFilter, PasswordChangeRequiredFilter), `.requestCache(cache -> cache.disable())`, and several source comments. Fix: name every omission, or restore them. `PasswordChangeRequiredFilter` is the "forced first-password change" control in Table 32.1; mention it or the table claim looks unsupported by the listing.
2. **major** - Honesty about the reviewers. The dossier (DOSSIER.md, decisions.md) says the TM and PO were AI review agents briefed as independent third parties. The chapter says "Two independent reviewers ... read the app" and treats them as people. Fix: say once that they were AI review agents; use the dossier wording ("Senior Technical Manager reviewer", "Product Owner reviewer").
3. **major** - Terms and tools used before they are taught: nginx, Caddy, reverse proxy, X-Forwarded-For, TCP peer, Docker Compose, Playwright, CVE, BCrypt, decompression bomb, TLS, Tomcat, CSRF, httpOnly, SameSite, session fixation, CSP, MFA, Retry-After. Prerequisites list only 15, 16, 30, but nginx is first taught in 33 and Playwright in 24. Fix: add prerequisites (8, 10, 24), add one-line glosses, define "reverse proxy" before Round 2, forward-reference Chapter 33.
4. **major** - Structure does not follow STYLE 5/6: no metadata comment (`<!-- chapter: 32 | part: V | ... -->`), a "Tag:" line instead of a proper prerequisites section, tier headings numbered (`32.1 Beginner tier`) when tiers should be unnumbered with N.M sections below them, and the "Reading the filter chain" section is unnumbered. Fix per STYLE 6.
5. **major** - No `<!-- source: ... -->` comments anywhere, although the chapter is dense with historical claims (STYLE 14 and 17, CHARTER accuracy rules). Fix: add source comments citing dossier files and commits (2d10e07, 2d82253, a51674c, 1ce2c8b, f682716, 782ab6b, 6cf17fa; PR #5 body).
6. **minor** - Round 2 heading says "(PR #5, commit `2d82253`)", but the review that found TM2-1 was made at `2d10e07`; 2d82253 is the fix. Say "reviewed at 2d10e07, fixed in 2d82253".
7. **minor** - 32.3.1 says the 18-of-18 live test showed a spoofed header defeated "through nginx, through Caddy, and directly to app:8080". The dossier records only "18/18 two-IP checks" for the lockout design (E1). Confirm what the 18 checks covered before keeping the sentence, or write "18 of 18 two-address checks".
8. **minor** - Table 32.1 row "One PAGE_VIEWED per session, page and 10 minutes; capped ACCESS_DENIED": PAGE_VIEWED exists in code, but the 10-minute window and the cap are not verified. Add a source (AuditLogService / TileController) or check.
9. **minor** - "The dossier records no debate about alternatives" in 32.4 is an internal-team remark; reader-facing text should not cite the dossier. Reword: "The project's records do not show a comparison here; Chapter 37 weighs the options."
10. **minor** - Exercises: no `### Exercise 32.N ★` format (STYLE 12), no solutions file (`32-security-review.solutions.md`), and exercise 2 (move `denyAll()` to the top) needs the tag (book-m5-platform) and a warning that it also blocks the health check. Exercise 1 needs a hint pointing to test class names.
11. **minor** - Museum analogy is also used in Chapter 1 for a different purpose; use a distinct analogy or cross-reference it. The breakdown sentence is present (good).
12. **minor** - Further reading entries have no URLs and OWASP items are not all official-docs pages; add links (docs.spring.io for Spring Security).
13. **minor** - After Listing 32.1, the text says the class derives the admin handle "with a different HMAC context"; the code path is `digest("admin-handle:", ...)` not `derive(...)`. Check that `digest` is HMAC-based or say "a different context string".
