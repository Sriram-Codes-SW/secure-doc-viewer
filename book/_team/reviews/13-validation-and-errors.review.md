# Review: 13-validation-and-errors.md (window 9, expanded text, full read; replaces the window-3 review, whose majors are RESOLVED)

Method: full read; `listingcheck.py 13` (6 listings verified; one "BAD" line is the deliberate placeholder `<test-admin-password>` in Listing 13.2, which STYLE 16 requires, so it is correct); code claims checked at `book-m6-final`: `LoginRequest` annotations, `@Min/@Max` on `AdminController.audit`, `ViewerProperties` (`@NotBlank` and `@Size(min = 32)` on the signing secret), the handlers and statuses in Table 13.2 (including `MAX_UPLOAD_MB = 50`, `HttpStatus.CONTENT_TOO_LARGE` and `ServiceBusyException` handler), the catch-all with an eight-character reference, Table 13.3's eight layers (render timeout answers `400` with "This PDF took too long to prepare", the request waits up to 30 s for a render slot then `503`), the `requirePdfSignature` first-kilobyte check and `requireWithinLimits`. Figure 13.1 is true (filter errors written by `SecurityErrorResponses`, controller errors routed to the most specific handler, `handleUnexpected` last, all through the one `error` helper). The forward-prerequisite flag from the coordinator's script is a FALSE POSITIVE, confirmed: the Prerequisites list names Chapters 5, 11 and 12 only; the "16" the script found is the sentence "CSRF is the subject of Chapter 16" in the terms note under the Prerequisites heading, which is a pointer, not a prerequisite. The bank-teller analogy has its breakdown; the incident about images (Accept header) is correctly sourced; the AI-agent wording is right; the dossier is cited only in HTML comments. No secrets. No blockers, no majors.

## Findings

1. **minor** - The pointer to Chapter 16 sits inside the Prerequisites section (a "Terms used here" paragraph) where the coordinator's script counted it as a prerequisite. Move that sentence out of the Prerequisites section (for example to the first paragraph of 13.1, "CSRF is covered in Chapter 16") so tooling and readers see only real prerequisites, 5, 11 and 12.
2. **minor** - Figure 13.1 has a source comment that names two tags (`book-m3-hardening and book-m6-final`); fine, but the figure's `SecurityErrorResponses` box exists at `book-m1-accounts` already, so say "book-m1-accounts onward" or drop the m3 name.
3. **minor** - Listing 13.3 and 13.4 and 13.5 are quoted at `book-m3-hardening` while the rest of the chapter is at `book-m6-final`. The chapter's opening note explains this; but the Table 13.4 "Tag" column mixes both, and the `listingcheck` passes for both. A reader who runs `git show book-m6-final:...` will find `ViewerProperties` with more fields: say so ("the m6 class has more settings; the lines shown are unchanged").
4. **minor** - Terms used before defined: "stack trace" (defined in the note, with a pointer to Chapter 3), "content negotiation" (13.7 describes it without the term: add it in bold), "decompression bomb" (defined in 13.9 and used in 13.1's bullet list: add "(Section 13.9)" there, which is present), "regex" (not used), "BCrypt refuses 30 emoji" in 13.1's bullet (Chapter 15 explains the 72-byte limit; the pointer is there).
5. **minor** - 13.8's claim "Angular's templates escape text by default (Part III)" is right; add the Chapter (21) so the reader can find it.
6. **minor** - Exercises 13.1 to 13.6 are in the house format. 13.4 says to unset `SIGNING_SECRET`, "and check that a `.env` file isn't supplying it": good. 13.5 and 13.6 are open-ended; the solutions say "a worked outline" (fine). 13.3 needs a `MockMvc` test with a test-only controller: point to Listing 18.5, as the exercise text half does.
7. **minor** - Pacing: about 5,100 words, well balanced. The three lines "A real incident" and the layered-limits table are the best teaching in the chapter; the 13.5 section is long (mapping table plus two listings plus a habit); consider splitting "adding a mapping is a three-step habit" into a boxed procedure.
