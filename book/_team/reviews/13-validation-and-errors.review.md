# Review: 13-validation-and-errors.md (window 3)

Verified: Listing 13.1 and the `AdminController` paging snippet match `book-m6-final` (`LoginRequest` is a nested record, indentation removed). Listing 13.2 matches `ViewerProperties.java` at `book-m3-hardening` (annotations, javadoc, `signingSecret` rules; unrelated fields elided with `// ...`). Listing 13.3 handlers and Listing 13.4 (catch-all, 8-character reference) match `GlobalExceptionHandler.java` at m3. The class-comment bug story (Accept header, tile endpoint, 401/429 turned 500) is real and dossier A3 supports it. Table 13.1 mappings all correspond to real `@ExceptionHandler`s. `ErrorContractTest` really uses a fake SQL string and path and asserts the "Something went wrong on our side. Reference: " prefix. `max-file-size: 50MB`, `max-pages: 500`, `max-page-pixels: 40000000` and the "keep in step" comment are in application.yml at m3. No secrets (the fake SQL and `C:/internal` are test data). No blockers.

1. **major** - Two quotations in 13.6 are not exact. "Uploads with more pages are rejected before anything is rendered." does not appear in m3 `application.yml` (the comment says "Upload limits, checked before anything is rendered.") and the README m6 wording is "Uploads with more pages are rejected before rendering". "stops decompression-bomb PDFs" is not in the yml either (README: "guards against decompression-bomb PDFs"). Fix: use the exact yml comment, or drop the quotation marks and paraphrase; say which file the quote comes from.
2. **major** - Terms used before taught / undefined: "Bean Validation" (defined, good), but `@Valid` vs "controller class has validation enabled" (the second half is not explained or shown anywhere: `@Validated` on the controller), `HandlerMethodValidationException`, `ConstraintViolationException` (never explained why three validation exceptions exist), "content negotiation" and the `Accept` header (13.5 uses both; Chapter 8 must define them), "log" and "stack trace" (Chapter 3), "UUID" (not taught), `Retry-After`, "decompression bomb" (defined, good), "CSRF" (appears in 13.4 side note; Chapter 16 comes later). Fix: gloss each in one clause or forward-reference.
3. **minor** - Mixed tags without a note: Listing 13.1 is `book-m6-final` while Listings 13.2 to 13.4 are `book-m3-hardening`; OUTLINE says m3. STYLE 11.5 requires a note near the start naming the versions at the tag and any listing that differs on the final stack. Add: "This chapter quotes m3 (Spring Boot 3.3.4, Java 21); at m6 `ViewerProperties` has more settings and `tileRateLimitPerWindow` defaults to 180, not 120."
4. **minor** - 13.3: "Chapter 11 showed the `secure-doc-viewer:` block of application.yml" - confirm 11.4 does (it was written after my first pass; I did not re-verify). Also "relaxed binding" claim ("Spring matches the two spellings") is fine; the glossary request in Ch 11 names it.
5. **minor** - 13.2 says a rule failure "raises a MethodArgumentNotValidException. Section 13.4 shows where that becomes a 400" but 13.4 only lists it in Table 13.1; add the real handler lines (81-89) or reword.
6. **minor** - "A handler that catches a validation failure picks only the first message ... `username: must not be blank`": I did not verify the first-message logic or the exact text; check lines 81-107 of GlobalExceptionHandler at m3 before keeping the example string.
7. **minor** - Exercise 4 ("start the application without SIGNING_SECRET") needs the tag and steps (which command, from which folder) and says nothing about `.env` fallbacks (`spring.config.import: optional:file:.env`), so a reader with a `.env` will not see the error; add a hint. Exercise 3 has no solution guidance; there is no solutions file (`13-validation-and-errors.solutions.md`). Exercises use `(★)` format, not `### Exercise 13.N ★`.
8. **minor** - Bank-teller analogy has a breakdown; good. The "most dangerous message is the helpful one" line overstates; suggest "a helpful error message can be dangerous".
9. **minor** - Table 13.2 is not referred to before it appears and Table 13.1 is referenced correctly. Further reading URLs: verify the Spring links resolve (structure of Spring docs changes).
