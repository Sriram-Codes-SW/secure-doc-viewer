# Review: 18-testing-the-backend.md (window 6, full prose pass)

Listing check (script, window 5): Listings 18.1 (`TileGridTest`) and 18.2 (`MySqlIntegrationTest`) match book-m6-final; the flyway history assertion (`"1","2","3"`), `mysql:8.4`, `-03:00`, `disabledWithoutDocker = true` are real. The other snippets (`ErrorContractTest` header, `apiRequiresSignIn`, the `CountDownLatch` lines) and the test names in 18.5 were not re-verified line by line this window (Bash unavailable; earlier windows confirmed `wrongPasswordAndUnknownUserGetTheSameAnswer`, `stateChangingRequestsNeedACsrfToken`, `CsrfCookieFlowTest`, `SecurityHeadersTest`). The flaky-test story (PR #9, commit `ec6c1c5`, the render-slot assertion) is consistent with the dossier (DOSSIER item 9). No secrets. No blockers.

1. **major** - The chapter tells the reader to run `mvn test` (18.2 and Exercise 1), but the book's own Chapter 6 says the project is built with the Maven wrapper (`./mvnw`) and the reader "does not need Maven installed at all". A reader following Chapter 6 has no `mvn` command. Fix: use `./mvnw test` everywhere, and say once that Docker must be running for the MySQL test (Exercise 1 asks which class is skipped when Docker is off: good, but state how to run with and without Docker).
2. **minor** - The concurrency section asserts that the burst test fires "12 wrong-password sign-ins" and expects "exactly `MAX_FAILURES_PER_ACCOUNT` (5)" with the rest `429`, but the shown snippet contains only the latch lines. Verify the 12 and the 5 against `SecurityIntegrationTest` and the constant name before publishing (the 5-per-account-and-address rule is in the dossier).
3. **minor** - Terms used before defined: "assertion" (defined inline: good), "test context/Spring context" (Chapter 11 taught application context; add "the running application" gloss), "profile" (Chapter 11), "MockMvc" (defined), "latch" and "thread" (CountDownLatch is explained by use; define thread here or in Chapter 17), "flaky" (defined by use), "CI" (Chapter 6), "H2" (Chapter 14), "package-private constructor" (Chapter 11 explained), "deterministic" in the heading (define: "the same result every run").
4. **minor** - The pilot's checklist analogy has its breakdown: good. The pyramid is described but the project's real counts are not given; if a number is quoted anywhere (68/114 tests appear in dossier), give the m6 count with a source, or leave it out.
5. **minor** - The unnumbered snippets in 18.4, 18.5 and 18.7 are real test code but captioned only by a trailing parenthetical; number them (Listings 18.3 to 18.5) per STYLE 11.2. The 18.7 snippet is explicitly labeled simplified: good.
6. **minor** - 18.4 says `application-test.yml` holds "a test signing secret and a known bootstrap admin". Those are test-only values; make sure the chapter never quotes them (it does not: good) and add "test-only values, never used outside tests".
7. **minor** - Exercises: inline `(★)` format instead of STYLE 12's `### Exercise 18.N ★ Title`; Exercise 5 (★★★) is well designed. Prerequisites should add Chapter 10 (Docker) for Testcontainers and Chapter 17 (`TileGrid`).
8. **minor** - The incident in 18.7 is told with the required structure (problem, how found, cause, fix, lesson) and a source comment: good. The reader-facing "PR #9" needs a plain gloss ("the ninth pull request") since Part V teaches PRs later; Chapter 7 already explains pull requests, so a pointer suffices.
