# Review: 18-testing-the-backend.md (window 7, expanded text, full prose review)

Verified (Grep/dossier): `mvn test` is now `./mvnw test` throughout and the exercise uses the wrapper (the window-6 major is RESOLVED); `SecurityIntegrationTest` has 22 `@Test` methods (chapter says 22); the burst test uses 12 threads and a `CountDownLatch` and asserts on `MAX_FAILURES_PER_ACCOUNT`; `MySqlIntegrationTest` runs two threads for the concurrent-replacement test; `unit-test-secret` is 16 characters (the chapter's remark about the 32-character rule is correct); `StorageJanitor.MIN_AGE` (1 hour) and `TileGridTest.noiseImage(612, 792)` exist; `ConcurrentServingIntegrationTest` exists; the flaky-test story (PR #9, commit ec6c1c5, `TileGenerationServiceTest`) and the CSRF-helper story (C1/C2) match the dossier; the dossier appears only in source comments. Listing exactness is covered by the coordinator's script. No blockers.

## Findings

1. **WITHDRAWN (my error, corrected later in window 7)** - I first reported that `checkAllowed` does not exist. It does: `LoginThrottle.checkAllowed(...)` is a public synchronized method (LoginThrottle.java line 95, called by `reserve` at line 81 and by `LoginThrottleTest`). My search had been truncated. Exercise 18.4 is correct as written; no change needed. (Lesson for later reviews: never conclude a name is absent from a truncated search.)
2. **major** - Secret-handling rule: Listing 18.4 prints the contents of `application-test.yml`, including a signing secret value (`integration-test-signing-secret-0123456789`) and a bootstrap admin username and password (`bootstrap-admin-password`), and Listing 18.2 quotes `"unit-test-secret"`. They are throwaway test fixtures, and the text says so, but STYLE 16 says passwords and secrets in the book are always placeholders in angle brackets and never real values, and the CHARTER forbids quoting files that hold a password or secret. A reader may also copy `bootstrap-admin-password` as a real password. Fix: replace the three values with `<test-signing-secret>`, `<test-admin-password>` and `<unit-test-secret>` and disclose it in the captions ("secret values replaced by placeholders"); keep the explanation that they exist only in the test profile. (If the editor rules that clearly fake fixtures may be quoted, add that rule to STYLE 16 so writers are consistent.)
3. **minor** - Pacing: the chapter is about 5,000 words and its Beginner tier now ends at 18.4 with a "Common mistakes" section before the reader has seen an integration test; consider moving 18.4 to the end of the chapter. Section 18.7's "Common mistakes with integration tests" and 18.11's second list are good but split the mistakes in three places; the chapter's own 18.4 promises "Section 18.7 shows how tests that share one application avoid stepping on each other" (it does).
4. **minor** - Terms used before defined: "mock" and "Mockito" (defined in 18.5: good), "profile" (Chapter 11), "H2" (Chapter 14), "latch" (defined), "CI" (Chapter 6), "harness"-style vocabulary is avoided, "classpath"/"context" (`@SpringBootTest` builds "the full application context" with a Chapter 11 pointer), "poll" (18.11 "Poll for a condition": define), "drift" (18.11), "assertion message" (explained), "fixture" not used (good), "in-memory database" (defined).
5. **minor** - The chapter presents Example 18.1 (an invented test) as an Example (good) but its method name (`aPageOfOneThousandPixelsNeedsTwoTilesOfFiveHundredTwelve`) is not in the repository; the text says so. Fine. The sample Maven output block says "something like" and uses `N` for counts: good practice.
6. **minor** - Listing 18.3 (`StorageJanitorTest`) calls a helper `dir(...)` and constants `KNOWN` that are not shown; the story explanation is good. Add "(`dir` creates a folder with a chosen age; `KNOWN` is a made-up document id)".
7. **minor** - Exercise 18.1 says "Which test class was skipped, if any, and why? (If you don't have Docker running, one should be.)" and Exercise 18.3's helpers require copying private methods from `SecurityIntegrationTest`; say "copy the `user` and `login` helpers from Listing 18.7" (they are shown there). Exercises 18.5 and 18.6 have "a worked outline" solutions; confirm they exist in `18-testing-the-backend.solutions.md`.
8. **minor** - Analogy: the pilot's checklist has a good breakdown. Figure 18.1 (a text pyramid) is a figure with an italic caption; fine.
9. **minor** - Length and tier balance: the Advanced tier holds four sections (18.8 to 18.11) and would fit a "skip on first read" label (it has one). The sentence "Chapter 14 listed three things the project wanted to check on the real engine" (18.9) should be verified against Chapter 14 (it names migrations, the row lock and UTC; I confirmed the row lock and UTC discussion there).
