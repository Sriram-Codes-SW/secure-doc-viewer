# Review: 09-sql-and-mysql.md (window 5, full pass on 9.1 to 9.5 prose; 9.6 onward listings-only)

Verified: Listing 9.1 (`V1__create_app_user.sql`), 9.2 (`V2`, simplified with a `-- ...` marker), 9.3 (indexes from `V2`), 9.4 (`DocumentRepository`, m6) and 9.5 (`V3__tile_versions_and_account_security.sql`, m6) contain no line absent from the real files. `V1` and `V2` are byte-identical at m2 and m6 (empty `git diff --stat`), so quoting m2 is harmless; MySQL 8.4 correct. The table/column/key explanations, the `password_hash` note, `ON DELETE CASCADE` and join-table explanations are accurate and match Chapter 27. No secrets ('<password-hash>' placeholder used). No blockers, no majors.

1. **minor** - Listings 9.1 to 9.3 are labeled `book-m2-documents` although STYLE 11.5 says Parts I-III quote `book-m6-final` by default. Since the files are unchanged, say "(book-m6-final; unchanged since book-m2-documents)" or relabel to m6, to avoid readers thinking they are at an old tag.
2. **minor** - Listing 9.2's `document` table is the m2 shape. At m6, migration `V3` adds columns (tile version and others) that the reader will see in their own copy. Add one sentence: "Migration V3 later adds columns to this table (Section 9.7)".
3. **minor** - Terms used before defined: "migration" is used in 9.2 ("the very first migration") before 9.7 defines it (one clause at first use: "a numbered SQL script that builds the database, Section 9.7"); "index" (9.5, defined there), "transaction" (9.6, defined there), "ORM/JPA" forward pointers to Chapter 14 (present), "LIKE/LIMIT" mentioned in 9.3 without explanation, "precision" of `DATETIME(6)` (defined as microseconds), "schema" (used in 9.7 title).
4. **minor** - The share-picker query mention names a Java method (`findTop20ByEnabledTrueAndUsernameStartingWith...`) in a SQL chapter before Java data access is taught; keep as a forward hint only, or drop the long method name.
5. **minor** - Spreadsheet analogy has its breakdown (database enforces links). Good. 9.5/9.6 analogies not checked in this pass.
6. **minor** - Exercises 9.1-9.3 need a MySQL instance or the `V*.sql` files only? 9.2 "Write a query" and 9.3 "Write a migration" - check that they say how to run SQL (Chapter 10 comes after this chapter; running MySQL needs Docker). If they need a running database, move them after Chapter 10 or provide an in-browser/`sqlite`-free alternative; otherwise say they are pencil exercises. Not verified.
7. **minor** - Sections 9.6 and 9.7 prose (row locks, `SELECT ... FOR UPDATE`/`PESSIMISTIC_WRITE`, Flyway checksum rules) were not read line by line; claims there about locking and immutability of applied migrations should be checked against Flyway docs and `MySqlIntegrationTest` in the next pass.
