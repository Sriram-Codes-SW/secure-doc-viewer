# Review: 09-sql-and-mysql.md (window 7, expanded text, full prose review)

Verified (Grep/dossier/arithmetic): six tables at m6 (`app_user`, `document`, `document_page`, `document_share`, `audit_event`, `account_known_ip`) match V1 to V3; `@Column(name = "tile_rows")` and `tile_cols` are in `DocumentPage.java`; V3 arrived in PR #5 (commit 00e0619), so "V1 and V2 only at m2" is right; the audit-log-in-its-own-transaction incident (C3, commit f648f33), the versioned-tiles-under-a-row-lock design (TM2-5, 00e0619), the 25 wrong-time-zone audit rows corrected by one approved UPDATE (C6, not reproduced verbatim: compliant) and H2-vs-MySQL testing (TM2-8, commit 5fc0faf) match the dossier. The chapter never prints a password (`-p` prompts interactively: good practice). Listing exactness is covered by the coordinator's script. Solutions not re-read. No secrets. No blockers.

## Findings

1. **major** - The reader cannot run the chapter's SQL. 9.3 starts MySQL and says `SHOW TABLES;` is empty "because the app creates the tables through its migrations when it starts", but Chapter 6 said the app cannot yet start, and the whole chapter then asks the reader to `SELECT`, `INSERT` and `UPDATE` `app_user`, `document` and `audit_event`, which do not exist. Fix: in 9.3 show how to create the tables now, either by running the app (after Chapter 10's full stack) or by piping the three migration files into the client, for example `docker compose exec -T mysql mysql -u securedocs -p securedocs < src/main/resources/db/migration/V1__create_app_user.sql` (and V2, V3, in order), noting that Flyway would normally do this and that this shortcut skips its bookkeeping. Alternatively mark the hands-on parts "after Chapter 10".
2. **minor (downgraded from major: the front-matter setup guide already installs Docker and checks `docker compose version`)** - Sequencing: 9.3 uses Docker Compose commands and a `.env` password; Chapter 10 explains them properly. Add a two-line pointer: "Docker was installed in the setup guide; Chapter 10 explains what these commands do".
3. **minor** - Forward reference not supported: 9.6 says the `:username` placeholders "are also the protection against SQL injection (Chapter 16)". Chapter 16 (and every Part II chapter) does not discuss SQL injection. Either add a short "SQL injection" explanation in Chapter 14 or 16 (a good candidate, since this is a security book) or explain it here in two sentences (an attacker types SQL as input; placeholders keep input as data, never as code) and fix the pointer.
4. **minor** - Terms used before defined: "UUID" (defined inline in 9.6: good), "denormalized" (defined in 9.9), "normalization" (used in the last paragraph of 9.9 right after; define: "avoiding copies by keeping each fact in one place"), "leftmost columns of a multi-column index" (9.8: needs a one-line example), "JPQL" (named, Chapter 14), "rolled back/committed" (defined in 9.10), "checksum" (9.12 error text "Migration checksum mismatch": explain "a fingerprint of the file's contents"), "wildcard", "alias" (defined by use), "distinct".
5. **minor** - Analogy: the spreadsheet analogy has its breakdown. The index-of-a-book comparison (9.8) is also an analogy with no breakdown; add "unlike a book index, the database keeps its index up to date automatically, which is the write cost".
6. **minor** - Example 9.3 (the timezone-fix UPDATE) is a teaching example that resembles a real repair of the project's own data. It is labeled a teaching example and does not reproduce the real statement: compliant. Add "do not run this on data you care about" and note that `NOW(6)` depends on the server's time zone (the very bug being fixed).
7. **minor** - Length and pacing: about 4,500 words with 12 sections; the Beginner tier (9.1 to 9.5) is well paced. The Intermediate tier (9.6 to 9.9) mixes joins, JPQL, `GROUP BY`, indexes and design in one run; consider splitting 9.9 (design choices) into the Advanced tier heading. Table 9.2's "9 quintillion" is correct for BIGINT.
8. **minor** - Consistency with Chapter 14: Chapter 14 also teaches `V3` and locks; here 9.10 gives the same lock listing. Cross-reference rather than repeat, and keep one definition of "row lock" for the glossary.
9. **minor** - Exercises: 9.2, 9.4, 9.5 are solvable from the text once tables exist (item 1); 9.3 (★★) is good; 9.6 (★★★) is open-ended and needs "what a good answer contains" in the solutions file. Format complies.
10. **minor** - Listing captions: 9.1, 9.2, 9.4, 9.5 quote `book-m2-documents` while STYLE says Parts I to III quote `book-m6-final` by default; since V1 and V2 are unchanged at m6 add "(unchanged at book-m6-final)" to each caption, or relabel to m6 (Listing 9.3, 9.6, 9.7 already use m6).
