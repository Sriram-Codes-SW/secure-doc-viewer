# Review: 20-node-npm-angular-cli.md (window 3)

Verified at book-m6-final: Listing 20.1 is byte-identical to `frontend/package.json`; Listing 20.2 is identical to `proxy.conf.json`. Versions: Node 24 (`node:24-alpine`), TypeScript `~6.0.2` (lock 6.0.3), RxJS lock 7.8.2, Vitest `^5.0.1`, jsdom `^30.0.1`, Playwright `^1.63.0`. The note about older tags is right (m5: Vitest 4.0.8, jsdom 28; Playwright arrives at m5). `package-lock.json` is 7,972 lines ("about 8,000" ok). angular.json: `proxyConfig`, budgets 500kB/1MB and 4kB/8kB, entry `src/main.ts`, `src/styles.css` all correct. tsconfig facts correct (no `"strict": true`, target ES2022, module preserve, the four extra checks; Chapter 19 does carry the matching note). `npm ci --no-audit --no-fund`, digest-pinned images, `.prettierrc` (100, single quotes), Dependabot ignore of TypeScript major/minor, nginx `^~ /api/` and `expires 1y` all real. Solutions are consistent with the code. No secrets. No blockers. High quality chapter.

1. **major** - Wrong cross-references to nginx. 20.3 says "the nginx setup in Chapter 22 relies on that" (cache headers) and 20.4 says "(Chapter 22)" for the `location ^~ /api/` block. OUTLINE puts the Dockerfiles and nginx stack in Chapter 30.2 and nginx as the front door in 33.2; Chapter 22 is HttpClient. Fix: point to Chapters 30 and 33.
2. **minor** - Prerequisites omit Chapter 10 (Docker) although 20.1 relies on it ("the project's production image uses two stages (Chapter 10)") and 20.7 assumes base-image digests; add Chapter 10, or add a one-line gloss of "image" and "stage". Also Chapter 14-level terms not needed. Exercise 4 needs Node and npm installed: point to the setup front matter.
3. **minor** - Terms used without definition: bundler, "bundle", registry, LTS, "minified/hashing filenames", "budget", "pull request" (Chapter 7 covers), "digest". Glossary requests exist? Not seen in requests.md for Ch 20; the bolded terms (Node.js, npm, packages, Angular CLI, development server, origin, proxy, semantic versioning) need requests.
4. **minor** - 20.8: "ignores Node's non-LTS lines (per the file's own comment, odd-numbered Node releases never become LTS)": I saw the file's comment about only LTS lines being proposed but did not confirm the exact odd-number wording or the Node ignore rule in `.github/dependabot.yml` (lines 1-10, 26-36). Verify or reword; the statement about the "history" (PRs 11 and 12) is supported by the dossier (DOSSIER.md item 9).
5. **minor** - "which is why Vitest 5 appears only at book-m6-final" is a small logical stretch (grouping of PRs is not the reason; the reason is timing of PR #11). Reword to "because they arrived in pull requests 11 and 12, after m5".
6. **minor** - Print-shop analogy has a good breakdown; add that Node is more than "electricity" (it also runs the presses' instructions). Optional.
7. **minor** - Try it exercises are not in `### Exercise 20.N ★` format (STYLE 12); solutions file exists and is accurate. Exercise 1 hint (Playwright needs a running stack and admin credentials via environment variables) must not lead readers to supply real passwords in the book; solutions say "supplied through environment variables" only, which is fine.
8. **minor** - Table of layout (20.5) is a small reference table and unnumbered; fine per STYLE 13.2. `<app-root>` and `Host` header mentioned without definition; add a clause.
