# Review: 28-m3-hardening.md (window 3)

Verified at book-m3-hardening: Listings 28.1 to 28.3 contain no line that is absent from the real files (`ViewerProperties.java`, `GlobalExceptionHandler.java`, `SecurityConfig.java`). Facts: 500 pages / 40M px / `%PDF-` check / 50 MB / JSON 413 (yml and `TileGenerationService`); the source PDF is deleted before the tiles are committed (`Files.delete(source)` with the comment "The PDF itself must never be committed alongside its tiles"); `/actuator/health` permitted, other endpoints denied by `denyAll`; CSP, Referrer-Policy, Permissions-Policy in SecurityConfig; 66 backend tests and the PR #3 evidence are in dossier/milestone-briefs.md (m3 entry). Findings TM-5, TM-11, TM-12, TM-15, TM-18, PO-8 are in the dossier. AI reviewers correctly named. No secrets. No blockers.

1. **major** - 28.5 says the `@Size(min = 32, ...)` on the signing secret is "visible in the diff" and the source comment cites the "ViewerProperties diff at book-m3-hardening", but `git show` shows `@NotBlank` and `@Size(min = 32)` are already present at book-m1-accounts and unchanged at m2 and m3. The m3 diff does not contain them. Fix: "the validation added at milestone 1 is still what protects the secret (see `ViewerProperties`)"; correct the source comment. Also 28.5 says the placeholder "remains in git history, which the dossier records as informational": reader-facing dossier mention; say "the old placeholder value remains in the repository's history, so treat it as public".
2. **minor** - 28.4 says "The PR also lists `X-Frame-Options: DENY` and `X-Content-Type-Options: nosniff`", but `git grep` finds neither configured in `src/main` at m3. They are Spring Security's default headers, so the claim is probably true at run time. Fix: "Spring Security adds `X-Frame-Options: DENY` and `X-Content-Type-Options: nosniff` by default", and confirm with `SecurityHeadersTest` (named in Table 28.1) before publishing.
3. **minor** - 28.2 defines decompression bomb but the explanation "A PDF page can declare a huge page size" should say pixels = width x height at the render DPI and give the 40M-pixel figure in an example (e.g., roughly A1 at 150 DPI, per the yml comment). Ties to Chapter 13 (already covers the limits); avoid repeating verbatim and cross-reference Section 13.6.
4. **minor** - Chapter 28 duplicates much of Chapter 13 (validation limits, GlobalExceptionHandler, catch-all reference). That is acceptable for a milestone chapter, but add "Chapter 13 teaches this in depth" pointers to Listings 28.1 and 28.2 and keep this chapter to what changed at m3.
5. **minor** - Terms used before defined: Actuator, "health endpoint", CSP, Referer, "staging", "streamed". Add glosses or pointers (Chapter 16 or 35).
6. **minor** - Try it exercise 3: "stop MySQL and fetch health" requires Docker Compose commands (Chapter 10) and a local copy at the tag; add the commands or a pointer, and note that the reader must not expose the actuator publicly. "Why is `/actuator/env` still closed?" - the answer is `denyAll` plus no exposure config (expected: 401 or 404 varies; check the PR evidence says 401 unauthenticated). No solutions file; exercises not in `### Exercise` format.
7. **minor** - Figure 28.1 caption bold, should be italic (STYLE 13.1). Live-evidence block quotes PR results "51 MiB upload"; keep as history but add the source comment naming PR #3 (present).
8. **minor** - 28.6 refers to `cd0f5c2` and `1ce2c8b` in Chapter 30, but Chapter 32 describes these as PR #5 fixes; check Chapter 30 covers both.
