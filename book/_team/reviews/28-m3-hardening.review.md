# Review: 28-m3-hardening.md (window 7, expanded text, full prose review)

Verified (arithmetic/dossier): A4 at 150 DPI is about 1,240 x 1,754 (2.2 million pixels); A0 (2,384 x 3,370 points) is about 4,967 x 7,021 = 34.9 million pixels, so it passes the 40 million limit (the chapter correctly notes the yml comment "roughly A1" understates the limit); a 200,000-point page becomes about 416,667 px per side = 173 billion pixels, hundreds of gigabytes at 4 bytes a pixel; Exercise 28.3's page (1,000 x 1,500 points) is about 6.5 million pixels (7 million to the nearest million) and passes. The listings' behaviors (1,024-byte `%PDF-` scan, 500 pages, 40M pixels, source PDF deleted before commit with the code comment "The PDF itself must never be committed alongside its tiles", `ErrorContractTest` fake SQL/path) agree with the sources checked in earlier windows and with PR #3 as recorded in the dossier (TM-5, TM-11, TM-12, TM-15, TM-18, PO-8; 56 to 66 backend tests). The earlier major about the `@Size(min = 32)` rule is RESOLVED: 28.12 now correctly says it has been in `ViewerProperties` since milestone 1. The AI-agent wording is present; the dossier appears only in source comments. Listing exactness is checked by the coordinator's script. No secrets (the fake `secret_table`/`C:/internal` strings are test data). No blockers.

## Findings

1. **major** - Listing caption format differs from the house format: this chapter (and Chapters 25 to 27, 29 to 31, per a search of `part-4-building-the-app`) puts a `*File: ...*` line BEFORE each code block, while STYLE 11.2 and the coordinator's listing script expect an italic `*Path: ...*` line AFTER the block. Listings in the wrong format may be skipped by the script and by the editor's tooling. Fix: move to `*Path: `...`*` under each block and keep the caption tag `(book-m3-hardening)`. Two of the listings are also not plain file copies (Listing 28.4 is a diff, 28.1 and 28.8 use `...` lines); the captions say so, which is fine, but use a consistent marker (`# ...` and `// ...`) instead of bare `...`.
2. **minor** - Claims I could not verify without `git show` this window: (a) "At milestone 2 [the multipart settings] were 100 MB" (28.3); (b) `SecurityHeadersTest` asserts that the health body contains neither `components` nor `details` and that `/actuator/env`, `/actuator/beans`, `/actuator/configprops` and `/actuator` are refused (28.10). Ask the writer to confirm both against `book-m2-documents` and the test file before release (the writer's header notes say the listings were checked).
3. **minor** - Version note missing: Listing 28.9 uses `.permissionsPolicy(...)`, which is the Spring Security 6 (Boot 3.3.4) name; Chapter 8 shows the same setting at `book-m6-final` as `.permissionsPolicyHeader(...)` (Spring Security 7). STYLE 11.5 asks a chapter on m0 to m4 to say which listings differ on the final stack; add one sentence with a pointer to Chapter 30.
4. **minor** - Internal finding IDs (`TM-5`, `TM-11`, `TM-12`, `TM-15`, `TM-18`, `PO-8`) appear repeatedly in reader-facing text and in the Decisions section. Chapter 27 explains once that the book cites the reviewers' IDs in code font; Chapter 28 does not. Add one sentence at 28.1 ("TM numbers are the technical-manager reviewer's findings, PO the product-owner reviewer's; both were AI review agents") or replace the IDs with plain descriptions, as the brief requires.
5. **minor** - Terms used before defined: "request thread" (28.1), "heap memory" (28.6: "held in heap memory"), "CPU", "clickjacking" (Table 28.2), "liveness/readiness" (quoted comment), "probes" (`probes: enabled: true` in Listing 28.10 is never explained), "Referer" (explained by the table), "multipart" (Chapter 8), "signature" used for both `%PDF-` and cryptographic signatures (28.5: say "file signature, also called a magic number, is not the same as the signed URLs of Chapter 25").
6. **minor** - Pacing: the chapter is about 5,000 words. The Beginner tier alone holds the limits, a worked example, two code listings and the whole staging sequence; the reading load is fine because each step is short and numbered, but 28.6's five-step list (Listing 28.5) could be preceded by a one-sentence summary ("copy to a private folder, check, render, delete the PDF, clean up on failure").
7. **minor** - Exercise 28.5 asks the reader to call `GET /api/auth/me` on "your own copy at `book-m3-hardening`" without saying how to build and start that older tag (Java 21, Spring Boot 3.3.4; needs the setup from Chapter 6 and a database from Chapter 10) or to sign in. Add a short "how to run this milestone" box to Chapter 25 or the Part IV introduction and refer to it. Exercise 28.6 (stop MySQL) has the same dependency. The other four exercises are solvable from the text.
8. **minor** - Decisions and challenges: the "options considered" statement for the background-job decision ("do the rendering in a background job ... or bound the work") is from the PR text (good, cited). The "Evidence" entry is labeled a decision-style block but is really a testing lesson; consider moving it to "Advanced tier" only (28.13 already covers it) to avoid repetition.
9. **minor** - Figure 28.1 caption bold, placed below; STYLE wants italics. The Mermaid nodes contain long text (headers listed inside the SEC node); shorten for readability.
