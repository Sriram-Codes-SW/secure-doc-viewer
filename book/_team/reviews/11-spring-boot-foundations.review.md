# Review: 11-spring-boot-foundations.md (window 9, expanded text, full read; replaces the window-2 review, whose majors are RESOLVED)

Method: full read; `listingcheck.py 11` (all listings verified); code claims checked at `book-m6-final`: `SecurityConfig` declares exactly eight `@Bean` methods; `KnownDevices` has a public `@Autowired` constructor and a package-private one taking a `Clock`; `ViewerProperties` is `@Component` + `@ConfigurationProperties` + `@Validated`; `server.port: 8080`; `spring.config.import: optional:file:.env[.properties]`; compose sets `DB_HOST: mysql`; `pom.xml` pins `tomcat.version` 11.0.26 with the three advisory ids in the comment; Boot 4.1.1 on Java 25. Figure 11.1 checked against the constructors: `DocumentController` (`DocumentService`, `RequestActors`), `RequestActors` (`SessionKeys`), `SessionKeys` (`ViewerProperties`), `DocumentService` (`DocumentRepository`, `AppUserRepository`, `TileGenerationService`, `AuditLogService`, a `TransactionTemplate` built from `PlatformTransactionManager`): true, and the caption says "part of". The analogy (fitted-out kitchen) has its breakdown sentence; terms are bold at definition (framework, library, inversion of control, annotation, bean, application context, property placeholder, profile, starter, log). Pattern note pointer to 38.2 is right. No dossier or agent citations in reader text; no secrets. No blockers, no majors.

## Findings

1. **minor** - Figure 11.1 has no `<!-- source: ... at book-m6-final -->` comment (the brief for the new figures asks for a tag in the source comment). Add one naming the five constructors.
2. **minor** - 11.3: "A repository is a class that reads and writes the database" is not bold (the other two kinds, controller and service, are), and Table 11.1 has no row for it. Bold the term and add one row ("Spring Data interface, no label needed; `AppUserRepository`").
3. **minor** - 11.2 says `@SpringBootApplication` "allows this class to declare extra configuration" without saying what that means; one clause ("you may put `@Bean` methods here") would connect to 11.3.
4. **minor** - 11.5: the sentence about Flyway starts "With the Flyway starter present, it runs the migrations at startup (Chap" and continues in the file; fine. But "Spring Boot 4 splits the old all-in-one starters" is quoted from the `pom.xml` comment, and the chapter never says what changed for a reader who knows Boot 3 (`spring-boot-starter-web` became `webmvc`, `flyway-core` became `starter-flyway`). Add "(at `book-m0-mvp` to `book-m4-reading`, Boot 3.3.4, the starters were `spring-boot-starter-web` and `flyway-core`)" so the reader is not lost when reading Part IV's `pom.xml` diffs.
5. **minor** - 11.4 states "a value from an environment variable overrides the same property in `application.yml`, and a value from `application.yml` overrides a default written in code", correct, but a `.env` file (imported) sits between them in a way the text does not rank: the `.env` import is a lower priority than real environment variables and higher than plain `application.yml`? (In Spring Boot an imported file has higher precedence than the importing file and lower than environment variables.) Say so in one sentence, or the comment "real environment variables win" is the only guidance.
6. **minor** - Exercise 11.6 (break the startup three ways) is good but needs a running app at `book-m6-final` with MySQL; add "(you need the setup from Chapter 10 and Part IV's box, 'How to run an older milestone', is not needed for this tag)". Exercise 11.4 depends on the app starting; say "on your own copy at `book-m6-final`".
7. **minor** - Terms used before defined: "YAML" (defined by example), "classpath" (11.5; one clause: "the list of libraries and folders Java can load classes from"), "connection pool" (11.5, defined in Chapter 14 14.6), "proxy" (11.8, defined there), "SLF4J" (defined), "starter" (defined), "auto-configuration" (defined).
8. **minor** - Pacing: about 5,000 words. The Beginner tier is right. 11.8 (proxies) and 11.9 (Tomcat incident) are Advanced-tier; both are short and well told. 11.9 mentions `GHSA-` advisories with no gloss: "GitHub security advisory ids, public numbers you can look up".
