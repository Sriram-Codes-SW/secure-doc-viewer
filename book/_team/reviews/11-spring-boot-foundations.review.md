# Review: 11-spring-boot-foundations.md (window 2)

State: partial draft (sections 11.4+ not written). Reviewed what exists.
Verified at book-m6-final: Listing 11.1 is byte-identical to the file. Listing 11.2 matches lines 23-25 and 33-38 of DocumentController.java. DocumentService constructor deps (DocumentRepository, AppUserRepository, TileGenerationService, AuditLogService, PlatformTransactionManager) are correct. KnownDevices really has a public @Autowired constructor plus a package-private one taking a Clock. Boot 4.1.1 / Java 25 correct for m6. No secrets.

1. **major** - Listing 11.2 caption/label: says "simplified: only the first lines of the class are shown", but the listing silently skips two nested records (`UpdateDocumentRequest`, `TransferOwnershipRequest`, lines 26-31) between the class line and the fields. Fix: say "simplified: two nested records omitted; only the constructor and fields shown", or add `// ...` markers where lines were removed. Also add the italic full-path line (`src/main/java/com/example/securedocviewer/controller/DocumentController.java`) under both listings per STYLE 11.2.
2. **major** - Objectives vs content: objectives promise configuring via `application.yml` and environment variables, starters, and auto-configuration; Section 11.2 forward-references "Section 11.5" which does not exist yet. The chapter cannot be accepted until 11.4-11.6, In this project, Try it (with solutions file), Summary and Further reading exist. Fix: finish, or trim objectives until written.
3. **minor** - 11.3 "Where the analogy breaks down": it refers to "handing over parts like a stock room", but no stock-room analogy was set up in the text. Introduce it in the dependency-injection paragraph first (or use the kitchen analogy: a supplier delivering what the chef lists).
4. **minor** - Prerequisites: the chapter relies on Chapter 3 (`main`, imports) and Chapter 5 (exceptions/generics not used, fine) - add Chapter 3 to the list. Also `final` is used without definition here; confirm Chapter 3/4 teaches it.
5. **minor** - Terms used before defined: "package" / "package-private" (last paragraph), "test" and "fixed clock", "controller" and "service" (used in 11.3 before Chapter 12 defines controller), "repository", "JSON" not used. Add one-clause glosses or move the KnownDevices detail to the intermediate tier.
6. **minor** - 11.3: "one instance of each bean ... by default" is correct (singleton scope) but the term singleton is not given; fine for beginners, add "Chapter N covers other scopes" only if the app uses them (it doesn't; leave out).
7. **minor** - `@EnableScheduling` claim ("cleanup jobs that later chapters describe") is consistent with the editor-approved Ch 14 plan; add a source comment or cross-reference to Chapter 14.
8. **minor** - Glossary: requests.md has terms for Ch 11 but names only; the editor asked for definitions. Also "constructor injection" and "component scanning" are bolded (fine).

---
## Second pass (window 3, chapter now complete: 11.1 to 11.8, In this project, Try it, Summary, Further reading)

Resolved from pass 1: item 1 (Listing 11.2 now marks the omitted records and methods with `// ...` comments; caption still says "only the first lines" - acceptable), item 2 (11.4 to 11.6 and the rest now exist and match the objectives), item 3 (stock-room sentence replaced by "A note on how Spring chooses"), item 5 in part (package-private explained as "visible only inside its own package").
Verified new content at book-m6-final: `application.yml` excerpts (port 8080, session timeout 30m, cookie name `SDV_SESSION`, http-only, same-site strict, `${SESSION_COOKIE_SECURE:false}`, `open-in-view: false`, `ddl-auto: none`) exist; `spring.config.import: optional:file:.env[.properties]` exists; starters (webmvc, security, data-jpa, flyway) and the pom comment "Spring Boot 4 splits the old all-in-one starters" exist; `application-test.yml` exists and `@ActiveProfiles("test")` is used; `BootstrapAdmin` logs at info and reads two `@Value` settings; `DocumentService` has no `@Transactional`. No secrets. No blockers, no majors remain.

New findings:
9. **minor** - Listing 11.3 is labeled "two excerpts, comments trimmed" but also silently omits `spring.jpa.properties: hibernate.jdbc.time_zone: UTC` between `open-in-view` and `hibernate:` and the `tomcat`/`forward-headers-strategy` keys under `server:`. Fix: add `# ...` lines where keys were removed, or mention "other keys omitted".
10. **minor** - 11.4: "a git-ignored `.env` ... That is why secrets such as `SIGNING_SECRET` never appear in the repository" - true for the value, but the placeholder demo secret exists in git history at m0 (dossier TM-6 note). No change needed in this chapter; keep in mind for Chapter 26 wording ("never" is safe here).
11. **minor** - 11.4 says the test profile "points the datasource at an in-memory H2 database instead of MySQL", but `MySqlIntegrationTest` also uses `@ActiveProfiles("test")` (against real MySQL via Testcontainers). Say "most tests use ...", or verify the profile override in that test before keeping the general statement.
12. **minor** - 11.8: "The wrapper only runs when the call comes from outside the bean" is the self-invocation rule of Spring proxies, stated without the word "proxy" being defined for a beginner; fine but add one clause ("a stand-in object placed in front of the real one"). Also "DocumentService doesn't use the annotation; it manages transactions explicitly" is correct (uses `TransactionTemplate`), but the chapter should name it in one clause.
13. **minor** - Exercise 4 (★★★) asks why `KnownDevices` needs `@Autowired`; the answer is in the chapter text (two constructors); mark as ★★ or ask for something the text does not spell out. No solutions file yet (`11-spring-boot-foundations.solutions.md`).
