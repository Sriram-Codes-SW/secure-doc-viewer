# Review: 30-m5-platform.md (window 8, expanded text, full prose review)

The writer's question (b) is answered, with one real error found. Table 30.1 shows an "After" column only, and for Boot, Java and PDFBox it adds "(from ...)", so it does not imply anything about the "before" versions of Spring Security, Hibernate and Flyway. But the note "Brings Spring Security 7, Jackson 3, Hibernate 7 and Flyway 12" is WRONG about Flyway. The Spring Boot 4.1.1 dependency BOM in the local Maven repository (`~/.m2/.../spring-boot-dependencies-4.1.1.pom`) manages `flyway.version` 12.4.0 (and `hibernate.version` 7.4.5.Final, `jackson-bom.version` 3.1.5, `spring-security.version` 7.1.1, `tomcat.version` 11.0.24); the project's `pom.xml` does not override any of them; and `flyway-core` 12.4.0 is present in the local repository. For comparison, Boot 3.3.4 manages Flyway 10.10.0, Hibernate 6.5.3.Final, Jackson 2.17.2, Spring Security 6.3.3 and Tomcat 10.1.30. The dossier (`versions.md`) says "Flyway 11" was taken from the PR text and "not verified from a dependency tree". So "Flyway 12" (also in the CHARTER, STYLE 11.5, the Part II introduction, and Chapter 37) is incorrect: it should read Flyway 12 (12.4.0). See finding 1.

Other verification (`git show book-m5-platform`, dossier V3 to V5, arithmetic): the gate order in Listing 30.9 matches the code (signature, session binding, rate limit, access, version, capacity); the trace-code comment ("enough to single out one sign-in in the audit log's session column, too short to be of any other use") is real; `UserDirectoryController` at m5 already has the 2-to-32-character rule and the `ADMIN` filter; the 18 of 18 two-address checks and their contents match V5; the 15 commits, TM2-1, TM2-6, TM3-1, PO2-5 and D6 to D11 match the dossier; the audit-times bug and the one-off UPDATE (25 rows, not in the repository) match C6; the 33 minutes vs 2.4 hours and 15 pages a minute figures match D6. Listing captions follow the house format. The AI-agent wording is present; the dossier appears only in HTML comments. No secrets (digests are public pins; the attackers' passwords are said to have been passed through an environment file and never printed). No blockers.

## Findings

1. **major** - Wrong version fact: Table 30.1 says the Boot 4.1.1 upgrade "Brings ... Flyway 11". Boot 4.1.1 manages Flyway 12.4.0 (verified in the BOM and the local Maven repository), so the row should say "Flyway 12". Fix the same text in the CHARTER version list, `STYLE.md` 11.5, `part-2-backend/00-part-introduction.md` line 55, and Chapter 37 (a search finds all four), and consider adding the "before" versions from the Boot 3.3.4 BOM (Security 6.3, Hibernate 6.5, Flyway 10.10, Jackson 2.17, Tomcat 10.1) so the upgrade story is complete. Also record the correction in `dossier/versions.md` (ask research). This is cross-cutting: addressed to the coordinator, editor, writer-app, writer-backend and writer-production.
2. **minor** - Stale cross-reference in 30.12: "Compare it with Listing 25.4". The m0 `TileController` listing in Chapter 25 is now Listing 25.9 (Listing 25.4 is `TileUrlGrid`). Fix the number; check other "Listing 25.x" references in Chapters 26 to 31.
3. **minor** - Inconsistency in 30.12: the heading says "five gates later", but Table 30.3 lists six gates. Say "six gates".
4. **minor** - The writer's question (c) side effect: Chapter 27.6 tells the reader that the picker change (minimum 2 characters, admin accounts hidden) is covered in "Chapter 30", but Chapter 30 never mentions the picker (a search finds no "picker", "directory leak" or `PO2-10`), and the code already has the change at `book-m5-platform`, not only "by m6". Add two sentences to 30.8 or 30.11 (the product owner's re-review found the directory leak) or change the pointer in Chapter 27 to "a later round".
5. **minor** - Duplication: the backend and frontend Dockerfile listings (30.1 and 30.2) and much of their explanation appear again in Chapter 10 (Listings 10.6 and 10.7) and Chapter 33 (Listings 33.1 and 33.2). Three copies will drift. Keep the milestone-specific facts here (what changed at m5 and why) and refer to Chapter 10 for the line-by-line reading.
6. **minor** - 30.6's worked example ends with "Two additional copies of your data are volumes: `mysql-data` and `app-storage`": the sentence is garbled ("copies" is wrong: volumes hold the data itself). Reword: "Your data lives in two volumes, `mysql-data` and `app-storage` ...".
7. **minor** - Terms used before defined: "GA" (defined inline), "system user" (Section 30.4), "IPv6 /64 prefix" (30.11 comment: gloss "the first half of an IPv6 address, which identifies a network, not one device"), "privacy addresses", "TOCTOU" (defined by the phrase "time-of-check to time-of-use"), "keyed hash" (Chapter 17), "fair semaphore" (explained), "subsample" (30.14), "buffer to temporary files" (explained), "upsert" (defined), "Prometheus endpoint" (Chapter 35), "ultrareview" (Chapter 31).
8. **minor** - The chapter quotes the product owner's words twice ("Start Phase 5 but can we use Spring Boot 4 if possible? ..." in 30.2 and "I will go ahead with the current setup for the rate-limits and see how things go" in 30.15). They are from the private build conversation (dossier D6, D10). Paraphrase ("the owner asked for the newest standard versions"; "the owner accepted the defaults and said they would watch how it went") and keep the README as the public source of the sign-off.
9. **minor** - "The secrets rule from Chapter 28" (30.6): the `.env` rule is taught in Chapter 2 (2.6) and the secret-validation rule was introduced in Chapter 26 (present since m1); Chapter 28 only recaps it. Point to Chapter 2.
10. **minor** - Pacing: about 8,000 words and 16 sections, the longest chapter in the book. The Beginner tier is Docker basics (already taught in Chapter 10) and CI (Chapter 36); the real milestone story (proxy trust, lockout redesign, versioned tiles, bounded work) starts at 30.8. Consider shortening 30.3 to 30.7 to a summary with pointers, or splitting into two chapters.
11. **minor** - Exercises 30.1 to 30.6 are in the house format, with a solutions file. 30.6 (design a cap on concurrent CSV exports) has no reference answer in the text; make sure the solution says "one good answer" and shows a `Semaphore` with `tryAcquire` and a 503-with-`Retry-After` response.
