# Review: 07-git-and-github.md (window 5, full prose pass)

Verified: the seven tags exist; merge commits for PR #1 and PR #5 exist (`f166d37`, `d1b1086`); commit `b6aef4e` is the first commit; `2d10e07` is the Phase 5 commit that added the Dockerfile and the Maven wrapper; `.gitignore` excerpt (Listing 7.1) matches the top of the file at book-m6-final (the listing shows lines 1-8 and 17-ish with comments; the real file also has an "IDE" block between, so the caption "excerpt" is honest but the cut has no marker); `git switch -c my-m2 book-m2-documents` is correct syntax. `git show <tag>:<path>` explanation is right. No secrets (repository address left as a placeholder). No blockers.

1. **major** - The reader cannot perform the first step. 7.2 uses `git clone <repository-address>` ("the address you were given") but the book never says where the address comes from; the project repository is private per the project plan. Also 7.4 and the exercises rely on `gh pr view` for PRs the reader may not be able to open. Fix: say in 7.2 where the address is published (front matter or README) and whether it is public; add a fallback: "If you cannot access the pull requests, the book quotes what you need from them".
2. **minor** - 7.2 shows sample `git log --oneline` output ("a27e069 Merge pull request #12 ...") that matches book-m6-final's history only when the reader is on that tag; a reader on `main` sees the same or newer commits, and the book-branch commits (e.g. "Book: ...") exist on `book/draft`. Say "output depends on where you are; at `book-m6-final` the top line is ...", and confirm `a27e069` is indeed the tip of `book-m6-final` (`git rev-parse --short book-m6-final`; not verified here).
3. **minor** - Listing 7.1 omits the "IDE" block between the storage lines and `.env`, without a marker. Add `# ...` or say "some lines omitted".
4. **minor** - Terms used before definition: "HEAD" (in "detached HEAD": defined only as "you're on no branch"; add "HEAD is Git's pointer to where you are now"), "uncommitted", "rebasing"/"stashing" (named only to be skipped, fine), "remote" (defined), "merge conflict" (never mentioned; exercise 7.3 will not trigger one, fine), "diff/`--stat`" (explained by flag).
5. **minor** - `git checkout <tag>` and `git switch --detach <tag>` are named, but the safe routine then only shows `git switch -c`; fine. Say `git checkout book-m2-documents` is what the root README suggests, so readers who followed it are not confused.
6. **minor** - 7.4: "gh pr view 5 if you have GitHub's command-line tool" - `gh` is not installed by the setup chapter (check front matter); define "GitHub's command-line tool (`gh`)" and add the install pointer or make optional.
7. **minor** - Video-game analogy has its breakdown; good. Add that commits are permanent unlike game saves you overwrite (already implied later at .gitignore). Optional.
8. **minor** - Exercises 7.1 and 7.3 assume a clone; Exercise 7.3 says "switch back to main" - for a reader who cloned the book branch first the default branch is `main` only if they did not check out `book/draft`. State "the branch you started on (usually `main`)".
