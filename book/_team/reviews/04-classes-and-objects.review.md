# Review: 04-classes-and-objects.md (window 7, expanded text, full prose review)

Verified (Read/Grep, dossier): `AuditEventType` has 22 values (chapter says 22); the stale `<canvas>` comment in `PageInfo` is disclosed honestly in the text; the incident in 4.10 matches the dossier (TM2-3, commit 65f2530; round 4 = commit f1bb3a8, "TM line-by-line read and code-review dry run"); the AI-agent wording is compliant; the dossier is cited only in source comments. `currentRoles` is attributed to f1bb3a8 via `git log -S` (a primary source) but the dossier itself does not mention `currentRoles`; acceptable, but see item 6. Example values (`198.51.100.30` documentation address, `kd-bob`) are safe. Listing exactness is covered by the coordinator's script. Solutions not re-read. No secrets. No blockers.

## Findings

1. **major** - Pacing/level: Chapter 4 is now the densest chapter of Part I for a novice. In one chapter the reader meets classes, constructors, encapsulation, immutability, records (five listings), enums, interfaces, `@Override`, dependency injection, `Clock`, packages, access levels, annotations, inheritance versus composition and a security incident. Sections 4.6 to 4.10 (injection, `Clock` test, access levels, annotations, the `currentRoles` incident) use JPA, Spring, `Optional` chains, method references and stream calls that Chapters 5, 11 and 14 have not yet taught. Fix: keep 4.1 to 4.5 and 4.7 here; move 4.6 (dependency injection and the `Clock` test) to Chapter 11, where the same idea is taught properly, and move the 4.10 incident to Chapter 16 or Chapter 27; or at least mark 4.6 to 4.10 clearly as "read now, revisit later".
2. **major** - Listing 4.12 (`currentRoles`) contains `AppUser::isEnabled`, a method reference, and a chain of `filter`, `map`, `orElse` on an `Optional`; the text says "Every step is one you now know", but method references and `Optional.filter/map/orElse` are not taught before this point (Chapter 5 comes after). Fix: either teach them in Chapter 5 and move the listing there, or say "we simplify the syntax here; Chapter 5 explains `::` and Optional chains".
3. **minor** - Terms used before defined: "abstract class" (4.6: `Clock` is described as one), "session handle" and "client address" (Listing 4.6; explain "the app's stand-in for a session id, Chapter 16"), "database library"/"framework" (framework is defined inline: good), "stale" (defined by use), "stream" (`anyMatch`, Listing 4.5; Chapter 5 pointer given), "static method" (defined inline), "subclass" (Table 4.3), "generic" (Chapter 5 pointer), "Optional" (`orElseThrow` in Listing 4.9; Chapter 5 pointer), "`normalizeUsername`" (unexplained helper).
4. **minor** - Listing 4.11 is a bare excerpt: `jdbc`, `properties`, `KnownDevices` and the test method header are not shown, and `Clock.fixed`, `ZoneOffset`, `Instant.parse` and `plus/minusSeconds` are new APIs. The prose explains the story well; add "the lines before this set up `jdbc` and `properties`; ignore them" and gloss `Clock.fixed` ("a clock that always says the same time").
5. **minor** - Listing 4.1 label lists the omitted fields correctly now, and the text notes that `role` has setters. Good. Listing 4.8's label about "six of the eight query methods" is accurate but confusing to read; simplify to "six other query methods omitted (marked `// ...` where they were)".
6. **minor** - 4.10: "A later review round (the fourth)" needs one plain sentence saying what the rounds were (reviews of the fifth pull request in stages, told fully in Chapter 32); and since the dossier does not mention `currentRoles`, keep the source comment citing `git log -S currentRoles` (commit f1bb3a8) and add the round-4 timeline entry (`timeline.md`, "Round 4: fixes from the TM line-by-line read") as corroboration.
7. **minor** - 4.6 says `RequestActors` and dependency injection "are explained fully in Chapter 11" and the section title says "Objects that need other objects": fine as a preview, but the paragraph "In the app, the framework creates the objects ... That is called dependency injection" defines a bold term that Chapter 11 also defines; make Chapter 4's the short version and make sure the glossary has a single entry.
8. **minor** - Analogy (form and copies): the breakdown paragraph is long and contains three separate breakdowns plus a "Finally" that is not a breakdown (classes carry behavior). Trim to two and move the behavior remark to the text.
9. **minor** - Exercises: 4.3 requires Chapter 7 (stated); 4.4 (`Counter` with a maximum) is well judged; 4.5 and 4.6 are solvable from the text. 4.2 says run with `java TileRefDemo.java`: a record nested in a class works, fine. `Try it` uses the required `### Exercise` format now. Solutions file must contain 4.4 to 4.6 (not re-read).
10. **minor** - Tables 4.1 to 4.4 have italic captions below them and are referenced before appearing; consistent. Table 4.3's "Protected: the same package, and subclasses" is correct; add that beginners can ignore `protected` except for the database-library constructor shown.
