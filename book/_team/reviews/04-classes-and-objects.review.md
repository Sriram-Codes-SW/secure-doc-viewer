# Review: 04-classes-and-objects.md (window 3)

Verified at book-m6-final: Listing 4.1 matches `AppUser.java` (shown lines identical). Listing 4.2 is identical to `PageInfo.java` (including the Javadoc line about `<canvas>`, see item 2). Listing 4.3 matches `SignedTilePayload.java` minus the class Javadoc and one inline doc comment (label says "comments omitted": correct). Listing 4.4 is identical to `Role.java`; the `Visibility` description matches its Javadoc. Listing 4.5 matches `DocumentRepository.java`; "six methods omitted" is the right count (8 total, 2 shown). Table 4.1 lists exactly the nine packages under `src/main/java` at m6. `AppUserRepository`, `DocumentSummary`, `Viewer` (records), `AuditEventType` (enum) exist. Solutions are correct (`TileRef[row=2, col=1]`). No secrets. No blockers, no majors.

1. **minor** - Listing 4.1 label says "four fields and their accessors omitted", but the omitted accessors also include `getRole`/`setRole`, `isEnabled`/`setEnabled`, `getCreatedAt` etc. The text claim "There is a setter for passwordHash, because passwords change" reads as if it is the only setter; the real class also has `setRole`, `setEnabled`, `setMustChangePassword`, `setLastSignInAt`. Fix: "Notice the getter for `username` but no setter; other fields such as `role` also have setters (omitted here)". Also the constructor uses `createdAt`, whose declaration is in the omitted part: mention "`createdAt` is one of the omitted fields".
2. **minor** - Listing 4.2's real Javadoc says the frontend lays tiles out "on a `<canvas>`", which is out of date (the Angular viewer uses positioned elements; only m0 used a canvas). It is real code, so keep the listing exact, but add a one-line note ("this comment is older than the code: the Angular viewer no longer uses a canvas, Chapter 21") and tell the code owner/coordinator it is a stale comment worth fixing in the repo (same root cause as README line 6, see request to editor).
3. **minor** - Listing 4.5 shows two methods with a single `// ...` between them; in the real file `findVisibleTo` comes before and four more methods and `findAllIds` sit around them. Fix: say "simplified: six methods and all comments omitted; the two shown are not adjacent in the file".
4. **minor** - Terms used but not defined in this chapter: `Long` vs `long` (wrapper type), `Instant`/`Instant.now()`, `List<Document>`/generics (deferred to Chapter 5, ok), "database library" (fine), `equals`/`hashCode` mentioned in 4.3 without explanation (add "equals decides whether two objects count as the same; hashCode is a fingerprint used by collections, Chapter 5"). The Listing 4.1 annotations (`@Entity`, `@GeneratedValue`, `@Enumerated`) appear before 4.7 but the text tells the reader to wait; OK.
5. **minor** - 4.5 claims "There is no class that implements this interface anywhere in the project" (true) and "Spring builds one at startup" - more precisely Spring Data JPA does; add "(a part of Spring called Spring Data)" to prevent confusion with the core framework.
6. **minor** - Objective "Explain ... why the app depends on interfaces" is thinly supported: the only project example is a repository interface where there is no alternative implementation, while the fake/real `TileStore` example is invented (correctly labeled as a teaching sketch). Fine, but add a real project example of swapping (e.g. a `Clock` passed into `KnownDevices` for tests, as Chapter 11 mentions) or say the app uses interfaces mainly via Spring Data.
7. **minor** - Exercises: format matches STYLE 12. Exercise 4.1 asks about "the result of a search (a title and a page count)"; solution says record; fine. Exercise 4.2 could say to use Java 25 with `java TileRefDemo.java`. Exercise 4.3 asks the reader to open `Visibility.java` (needs a clone; Chapter 7 teaches cloning): prerequisites list only Chapter 3, so add a pointer or move the "open the repo" step.
8. **minor** - Analogy (class = form, object = copy) has its breakdown, but "the form can also carry behavior" is weak as a breakdown; add that the same form can produce many copies at run time and that objects live in memory only while the program runs. Optional.
