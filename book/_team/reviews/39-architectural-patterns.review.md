# Review: 39-architectural-patterns.md (window 9, new chapter, full read)

Method: full read; `listingcheck.py 39` (3 listings, all verified, 0 problems); every "where it lives" claim checked against `book-m6-final`; figures 39.1 to 39.6 checked for numbering, mention-before-use and source comment; cross-references to Chapters 6, 8 to 10, 14, 16, 18, 22, 27 to 28, 30, 32 to 37 and Section 37.x checked; the "Pattern note" pointers from other chapters to Sections 39.4 to 39.15 all resolve correctly.

Verified true at `book-m6-final`: seven controllers plus `GlobalExceptionHandler` (Figure 39.2 says "7 controllers and the error handler"); `TileController` has eleven constructor collaborators; `UserAdminController` holds a `DocumentRepository` and `UserDirectoryController` an `AppUserRepository` (so both skip a layer, as the text says; Figure 39.2's dotted arrows are right, but see item 5); `DocumentController` depends only on `DocumentService` and `RequestActors`; imports: `document` imports `account`, `audit`, `exception`, `model`, `service`; `account` imports only `exception` (so "accounts don't know about documents" is right); `audit` imports `security` exactly once; there is no ArchUnit or module descriptor (the "by convention, not by machinery" claim is right); `SessionLifetimeFilter` is registered before and `PasswordChangeRequiredFilter` after `AuthorizationFilter` (Listing 39.1) and both first appear at `book-m5-platform`; the audit table has one insert and one purge (`delete from audit_event where occurred_at < ?`), `audit-retention-days: 180`; Listing 39.2 and Figure 39.5 match `replaceFile` step for step; `application.yml` uses `${DB_HOST:localhost}` etc. and `import: optional:file:.env[.properties]`; compose ports are all bound to `127.0.0.1`; containers run as non-root; the CI token is `contents: read`; `SESSION_COOKIE_SECURE` defaults to `false`; the metrics names in 39.15 all exist; the lockout story (D7: first fix for the spoofed address added the account-wide counter, TM3-1 showed the denial of service, final rule in `82c24b6`) and the README quotation are right; "Deferred: background upload processing (PR #3)" is in the dossier. Honesty is strong: every "approximates" (layers with two shortcuts, monolith by convention, audit log not event sourcing, reverse proxy not gateway, RED partial) is stated plainly. AI-agent and dossier rules are respected. No secrets. No blockers, no majors.

## Findings

1. **minor** - Header says `status: draft`; change to `expanded` when the writer considers it final.
2. **minor** - Table captions break STYLE 11.6 (caption ABOVE the table, in bold: `**Table 39.1 — ...**`). Table 39.1 has an inline bold "Table 39.1" in the sentence and an italic caption below; Tables 39.2 to 39.5 have italic captions below. Move each caption above its table in bold (the coordinator's listing/table tooling reads the bold form).
3. **minor** - Figure 39.6 (the decision framework) is never mentioned before it appears ("Here is a method ... It has five steps." then the block). Add "Figure 39.6 shows the loop". Also its source comment names no tag (it is the book's own method, so say "(no code; the book's own method)").
4. **minor** - Length: about 7,000 words against a brief of 3,500 to 4,500 prose words. The chapter is clear, but 39.13 to 39.15 are short pattern notes that repeat Chapters 33, 36 and 35, and 39.19/39.20 are tables. Consider moving 39.20 (Table 39.5) to the Part VI introduction or the appendix.
5. **minor** - Figure 39.2's dotted-arrow label "UserAdminController and UserDirectoryController skip a layer" is attached to the arrow to `AppUserRepository`; but `UserAdminController` reaches `DocumentRepository` (not the user repository) and `UserDirectoryController` reaches `AppUserRepository`. Split the label, or write it on both arrows ("UserDirectoryController" on one, "UserAdminController" on the other).
6. **minor** - 39.5: "`TileController` coordinates eleven collaborators ... so it does the work of a business-logic layer as well as HTTP." True and honest. Add that Chapter 32 (32.13) walked through exactly this method, so the reader can see why it is not thin.
7. **minor** - Terms used before defined: "HSTS" (Figure 39.1; the term is defined in Chapter 33 but this chapter gives no pointer at first use), "TLS" (Figure 39.1), "CQRS" and "service mesh" (Table 39.4: one clause each), "copy-on-write" (defined), "row lock" (defined in Chapter 14), "JVM" (39.6), "CIDR" is not used. "least privilege" is defined in Chapter 36 (36.x) and "defense in depth" in Chapter 28 (28.11); both pointers are right.
8. **minor** - 39.9: the text says "a keyed hash of the issuing session ... and the request must also present that session's cookie". Figure 39.4 shows the cookie on every step but not the binding; add a note "(the URL's token contains the binding)" to the last arrow so the figure carries the twist the text describes.
9. **minor** - 39.12, Table 39.2 lists "the factors that the repository evidences", five of twelve. A reader who has never heard of twelve-factor will not know what the other seven are; either list them in one sentence or say "the other seven do not apply or are not evidenced".
10. **minor** - 39.17 says "(a) is rate limiting by two keys" and later "(b) the same with a wider key": fine, but Chapter 32 (32.5) says the first lockout was "the first fix for password guessing", while the dossier (D7) and Chapter 37 (37.14) say it was the first fix for the forged-address finding. The dossier is authoritative; this chapter's wording ("the fix for a different problem") is safe, so fix Chapter 32.5 rather than this chapter.
11. **minor** - Exercises 39.1 to 39.6 are in the house format, but none of them has a `*Solution:*` line in the chapter (Chapter 38 has one per exercise, "Appendix C, Exercise 38.n"); the solutions file has all six exercises. Exercise 39.4 ("name three pieces of state") is solvable from Table 39.2 plus Chapter 37; 39.6 is open-ended (say "one good answer" in the solution).
12. **minor** - The Part VI introduction and this chapter both say Chapter 39 should be read "after Part V"; Prerequisites lists Chapters 32 to 36 and 38 but not Chapter 27 or 30, which the text cites (audit transactions, versioned tiles). Add them or drop those pointers.
