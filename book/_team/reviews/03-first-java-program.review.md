# Review: 03-first-java-program.md (window 7, expanded text, full prose review)

Verified (Read/Grep): password constants `MIN_PASSWORD_LENGTH = 12` and `MAX_PASSWORD_LENGTH = 128` (UserAccountService.java); the round-trip test's 613 x 457 image is in `TileGridTest.java` (lines 69-70); `GlobalExceptionHandler` really contains a `switch` expression (line 131), so the forward reference to Chapter 13 is honest. Arithmetic checked: 251 leftover, 3 columns by the `while` loop, `(1275+511)/512 = 3`, `(1024+511)/512 = 2`, 612/50 = 12.24 to 13, 792/50 = 15.84 to 16, `contraseña` 10 characters / 11 bytes, 12 tiles. Listing exactness is covered by the coordinator's script. Solutions file not re-read in this pass. No secrets. No blockers.

## Findings

1. **major** - Pacing and prerequisites for a first-time programmer: Chapter 3 now teaches, in one sitting, the toolchain, variables, strings/bytes/encodings, `if`, `switch` on an enum, `for`/`while`, methods, pass by value, compiler errors, exceptions, a JUnit test and a validation method with `private static void`. The Beginner tier stops at 3.4, but 3.4 already asks the reader to reason about UTF-8 bytes and BCrypt (a hashing method not yet defined) inside Listing 3.2. A total beginner will stall there. Fix: move 3.4's bytes material to the Advanced tier (next to 3.10, where the password rule is used) and keep a one-paragraph teaser in 3.3; or split the chapter.
2. **minor** - Terms used before defined: "BCrypt" and "hashing" (3.4; Chapter 15 pointer given but no gloss), "processor" (3.1), "standard library" (defined), "token" (glossed), "array" (glossed inside a sentence in 3.6 but the earlier use in the 3.3 error text is not), "Dockerfile" and "JRE" (3.1; Chapter 10 pointer), "enum" (3.5: pointer to Chapter 4), "lambda" (3.9: pointer to Chapter 5), "synthetic image" (3.9; explain "a test image made up by the test, not read from a real PDF"), "UTF-8" (Chapter 2 introduced it), "camel case" (defined), "casting/conversion" (mentioned only in an error message). Acceptable with the pointers, but add glosses to "BCrypt" and "processor".
3. **minor** - 3.2 shows `openjdk version "25" ...`, but real output depends on the vendor and patch level (`25.0.x`, or `java version "25..."` for Oracle). Say "the exact text varies; what matters is the 25". Also `javac -version` prints `javac 25.0.x`.
4. **minor** - Example 3.3 contains a non-ASCII letter (`ñ`). If a reader saves the file in an editor that uses another encoding, or the terminal cannot display it, the output can differ; the printed numbers are only lengths, so it works, but add a note that the file must be saved as UTF-8 (Chapter 2, 2.7) and that on very old setups you can use `"contraseña"`.
5. **minor** - Listing 3.2's excerpt starts with a Javadoc line and shows `password == null ||` (treats null as acceptable): the text explains it ("nothing to reject here"). Consider adding one sentence on why null is not rejected in this method (other checks reject it), since a beginner will read it as a bug.
6. **minor** - Listing 3.3 is a fragment whose variables (`issuable`, `sessionBinding`, `documentId`, `page`) come from earlier in the method; the text says "names like ... are calls to code defined elsewhere" but does not mention the variables. Add "`documentId`, `page`, `issuable` and `sessionBinding` were set earlier in the method".
7. **minor** - Analogy (recipe and cook) has a two-part breakdown: good. "Since Java 11 you can run a single file with `java Hello.java`" is correct; add that Examples 3.5 and 3.9 also run this way but that a file with an enum inside `Roles` is fine.
8. **minor** - Exercises: 3.5 says the title column size is "the size of the `title` column in Chapter 9" (a forward reference to something the reader has not read; give the number 200 and say Chapter 9 explains the column). 3.6 requires catching an exception (Chapter 5) even though the chapter shows only a stack trace and `assertThrows`; either add a five-line `try`/`catch` example to 3.8 or mark 3.6's last part as "optional; Chapter 5 teaches this". 3.4 is fine, 3.1 to 3.3 solvable from the text.
9. **minor** - "Common mistakes" is strong; add "A `String` compared with `==` sometimes appears to work" (string pool) only if you want; otherwise skip. The "Forgetting `static`" entry says "non-static method cannot be referenced from a static context": verify the wording is what Java 25 prints.
