# Review: 03-first-java-program.md (window 3)

Verified at book-m6-final: Listing 3.1 identical to `SecureDocViewerApplication.java`. Listing 3.2 matches `PageTileUrlController.java` lines 57-63 (indentation kept). Listing 3.3 matches `TileGrid.tileCount` including its doc comment. `<java.version>25</java.version>` correct. The TileGrid class comment supports the "kept separate so it can be tested with a synthetic image" claim. Arithmetic checked: 1275 - 1024 = 251; 1275/512 = 2 (integer); 1275 x 1650 page at 512 = 3 columns x 4 rows = 12 lines (Exercise 3.3). Solutions correct. The stack-trace example is honestly labeled "teaching output, not from the project". No secrets. No blockers, no majors. Well-scoped, well-explained chapter.

1. **minor** - Listing 3.2 sits in the Beginner tier but uses `String[][]`, `issuable.tileVersion()`, `sessionBinding` and a six-argument call, none of which the reader can decode. The text says Chapter 4 will explain, but the excerpt starts mid-method so `issuable`, `pageInfo`, `signedUrlService` and `sessionBinding` appear from nowhere. Fix: add one sentence naming them ("`issuable` and `sessionBinding` were set earlier in the method; ignore them for now") or replace with a simplified Example that uses `tileCount`-style variables.
2. **minor** - 3.1: "the standard library" and "processor" are used lightly; fine. "web server" (chapter intro) and "unit-tested"/"tests this method" (3.7) are used before Chapter 18 / Chapter 8; add a clause ("a test is a small program that checks another") or pointer.
3. **minor** - 3.2: the expected `java -version` output shows `openjdk version "25" ...`; real output is `openjdk version "25.0.x" ...` and varies by vendor (Temurin, Oracle). Say "something like `25.0.1`; the exact text depends on the vendor".
4. **minor** - 3.2 says the class name must match the file name; strictly this holds for `public` classes. The sentence is correct in context; optional footnote.
5. **minor** - Solution 3.2 says the `mian` run fails "with an error that the class has no `main` method"; show the actual wording (`Error: Main method not found in class Tiles, please define the main method as: ...`) so readers recognize it. Verify on Java 25 (behavior around instance `main` methods changed in Java 25 with compact source files) before finalizing.
6. **minor** - Terms bolded (program, source code, compiler, bytecode, JVM, JDK, variable, type, statically typed, operators, if statement, loop, method, parameters, return value, stack trace, exception, doc comment, token) are all new; glossary requests are in requests.md for Ch 3 (I saw one line). Confirm it includes `token`, `string`, `class`, `main method`, `doc comment`.
7. **minor** - The Advanced tier is a single small section; fine, but "In this project" says the test is in `TileGridTest.java`; verified that path exists at m6? (path listed at m0 verified; m6 not checked). Confirm.
8. **minor** - Objective 1 ("Explain what ... the JDK and JVM are") is covered by 3.1; no exercise tests it. Add a ★ question (e.g., "Which tool produces `.class` files?").
