# Review: 25-m0-the-tiled-viewer.md (window 2)

Verified at book-m0-mvp: Listings 25.1-25.4 match the real files exactly apart from the stated removals (imports, Javadoc, unused private helpers, constructor/fields). Versions (Boot 3.3.4, Java 21, PDFBox 3.0.3) correct. application.yml values correct (tile-size 256, dpi 150, ttl 120, session 1800; demo-only signing secret is a visible placeholder). m0 really uses a canvas in index.html and an `X-Session-Id` header. Tile file layout, `/api/tiles?token=` paths and the Javadoc note about uploader role are real. History claims (TM-1, TM-2, TM-4, TM-6, TM-8, TM-17, D4, D5, later watermark iterations) are supported by dossier/bugs-and-findings.md, decisions.md, reviews.md, timeline.md. Quality is high. No secrets; the signing-secret placeholder is described, not quoted. No blockers.

1. **major** - Prerequisites list wrong chapter numbers: "8 (Spring Boot), 11-12 (HTTP and REST), 17 (testing basics)". Per OUTLINE: HTTP is Chapter 8, Spring Boot 11, REST 12, files/signatures 17, testing 18. Fix: "Chapters 3-6 (Java), 8 (the web), 11-12 (Spring Boot, REST), 17 (signatures, PDFs), 18 (testing)".
2. **major** - Source comments cite the scratchpad file `previous-review-findings.md` and "PR #1 body" as sources in three places; readers and later editors cannot reach the scratchpad, and CHARTER says the dossier is the single source. Fix: cite `dossier/reviews.md` / `dossier/bugs-and-findings.md` (which contain the same TM facts) instead.
3. **major** - Terms used before they are taught to a beginner: base64url, HMAC (defined), UUID, PNG, DPI (defined in Ch 1), Javadoc, "in-memory", "manifest", "canonical", "controller/service" (Ch 12), "session id". Base64url in particular is used in the token description without a definition. Fix: one-sentence gloss for base64url ("a way to write bytes using only URL-safe letters") and manifest; glossary requests for HMAC, base64url, token, constant-time comparison (none seen in requests.md).
4. **minor** - 25.2 Try it preview and the numbered Try it exercise both use `tileCount`; fine. But 25.2's "Try it (preview)" duplicates the exercise style; STYLE 12 wants exercises only in the Try it section. Fix: turn the preview into a worked example (Example 25.1) with no "Try it" label. Also exercises lack the STYLE format (`### Exercise 25.1 ★ ...`) and there is no solutions file (`25-m0-the-tiled-viewer.solutions.md`); exercise 25.1 arithmetic: 1240/256 -> 5 columns (last 216 px wide), 1754/256 -> 7 rows.
5. **minor** - Exercise 25.2 asks the reader to alter a token and observe an exception, but the app returns an HTTP error, not visible exception; also STYLE 16 wants attacks against the reader's local copy (it says so) and a note about which tag to run. Fix: say "run book-m0-mvp locally" and state the expected HTTP response (check GlobalExceptionHandler mapping for InvalidTokenException before writing the solution).
6. **minor** - Listing 25.2 shows two methods extracted from a class and is labeled simplified (good); add the italic full-path line under each listing per STYLE 11.2 (`src/main/java/com/example/securedocviewer/service/...`). Listings 25.1-25.3 caption bold placement OK, but 25.4's heading says "TileController.getTile" - consistent; add path line.
7. **minor** - Figure 25.1: caption appears after the code block (good) but the figure is bold not italic; the text refers to it ("Figure 25.1 shows...") - ok. Table 25.1 is never referred to before it appears; the sentence after it is a weak reference. Fix: refer first ("Table 25.1 lists ...") then show it.
8. **minor** - Analogy in 25.2 (bathroom tiles) is not in STYLE 8.1 but has a breakdown; wristband analogy matches STYLE 8.1 and has a breakdown. Good. 25.3 breakdown says "Chapter 26 binds tokens to a session more tightly" - accurate per TM-4.
9. **minor** - Decision D4 "the alternatives ... aren't recorded" - honest; okay. But "Challenge" section is told at the end of a chapter about m0 though the findings belong to later reviews; that is clearly flagged ("Later, independent reviews"). Consider a forward pointer to Chapter 26 and 32.
10. **minor** - Chapter says a "Learning objectives" bullet "Read TileGrid, SignedUrlService and TileController ... say what each guards", but the chapter never states what TileGrid "guards" (it is math, not a guard). Reword the objective.
11. **minor** - Missing STYLE 17 items: Prerequisites/Beginner-Intermediate-Advanced tier headings absent (chapter uses numbered sections without tier headings). For Part IV, confirm with editor whether tiers are required; if so, add.
